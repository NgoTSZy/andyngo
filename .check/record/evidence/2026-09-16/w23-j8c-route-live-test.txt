W23 J8c 留证 · 「开始」→ TASK 的真实调用验证
时间：2026-09-16T07:55Z（本地 15:55）· 作业：W23 J8c · 类型：**真实调用验证**（**不改任何源文件**）

──────────────────────────────────────────────────────────────────────
一 · 验证什么
──────────────────────────────────────────────────────────────────────
SEAL `seal-153834.txt` §十一 第 2 条写着：
> **「用户说『开始』时真能路由到 TASK」未实测** —— 需要一次真的 `/andyngo 开始 …` 调用。

本作业做这次调用。

──────────────────────────────────────────────────────────────────────
二 · 怎么调的
──────────────────────────────────────────────────────────────────────
`Skill(skill="andyngo", args="开始 验证「开始」这个触发词能否路由到 TASK")`
→ **skill 加载成功**（`Base directory for this skill: C:\Users\<user>\.workbuddy-ai\skills\andyngo`）。

──────────────────────────────────────────────────────────────────────
三 · 结果：**判事件 = TASK** ✓
──────────────────────────────────────────────────────────────────────
依据 SKILL.md 判事件表**第 1 行**：
```
| 做/建/写 + 新对象；启动/开始 | TASK | A |
```
「**开始**」被该行接住 ✓ —— **路由成功**。

──────────────────────────────────────────────────────────────────────
四 · ★ 这次调用暴露的一件事（**比验证本身更重要**）
──────────────────────────────────────────────────────────────────────
**skill 加载的是 `skills/andyngo/SKILL.md`，不是桌面那份 `andyngo build.md.txt`（规格）。**
证据：skill 返回的第一行 `Base directory for this skill: C:\Users\<user>\.workbuddy-ai\skills\andyngo`。

→ **推论 1**：**W23 J8 改规格 = 一致性修复**（让两处同形），**路由行为本来就对** ——
   SKILL.md 第 19 行**早已含**「启动/开始」（W23 J2 补的，`CHG-101`）。
→ **推论 2**：**规格是权威参考，SKILL.md 是执行入口**；**只有后者参与实际路由**。
→ **推论 3**（对判据设计的意义）：**「规格与 SKILL.md 一致」这件事，没有任何判据在管** ——
   `probe-crossref.js` 的 X1–X6 都不查这一对；而它俩**现在**同形**全靠 W23 J8 手工对齐**。
   **W23 J8 的 diff 是「一次性对齐」，不是「持续一致」。**

──────────────────────────────────────────────────────────────────────
五 · 边界（**必须说清**）
──────────────────────────────────────────────────────────────────────
1. **「路由」不是代码，是「主代理读表后判断」** —— 本次验证证明的是「**表里有那行 + 我判对了**」，
   **不能证明机械正确性**（没有机械层可证）。
2. **本次语句含两个触发词**：「开始」（→ TASK，判事件表第 1 行）+「**验**」（→ VERIFY，判事件表 `测/验`）。
   我按**主意图**判 **TASK**。**规格没有明文消歧规则**，但有**兜底**：「判不出 → ASK×1，列 10 个」。
   **这是主代理的语义判断，不是机械匹配的结果** —— 换个主代理可能判 VERIFY。
3. **未测**：「一句话含两个**指向不同事件**的触发词」时，规格期望怎么判 —— **它没写**。

──────────────────────────────────────────────────────────────────────
六 · 仍不能验的
──────────────────────────────────────────────────────────────────────
1. **「判不出 → ASK」的实际行为未测** —— 本次判得出，没走到那一行。
2. **「空启动 `/andyngo`（无参数）→ AUDIT」未测**。
3. **其余 9 个事件未做真实调用** —— RESUME / RETRO / FIX / DEPLOY / VERIFY / SECURITY / AUDIT /
   EVOLVE / SEAL，**本次只验了 TASK 一条**。
4. **「规格 ↔ SKILL.md 持续一致」没有判据**（见 §四 推论 3）—— 本次是**一次性对齐**。
5. **`routing-coverage.js` 仍未找到**（SEAL `seal-153834.txt` §十一 第 1 条）—— 本次验证**不是**它跑的。

──────────────────────────────────────────────────────────────────────
七 · 本作业改了什么
──────────────────────────────────────────────────────────────────────
**只加了本留证文件 + 一条 EVT。未改任何源文件、未改任何记录层文件（issues/decisions/changes 均未动）、
未改规格、未改 SKILL.md、未改冻结件。**
→ 故本作业**无 CHG**（CHG 记的是源文件/记录层文件的变更）。
