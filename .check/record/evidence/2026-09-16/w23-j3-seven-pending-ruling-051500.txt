W23 J3 · 2026-09-16T05:15Z · 七项待裁决的「全貌 + 建议」（**只报告，不裁决**）
用户指令：「待你裁决仍是 7 项……分别是什么？请列出建议。等待我判。」
================================================================================

本文件是**只读调查**的结果：把 SEAL v8 §九 那 7 项逐条摊开成「是什么 / 现状实测 / 选项 / 代价 / 建议」。
**没有翻任何一条的状态**，没有改任何源文件 —— 用户要先判。

---------- 一 · 两项**前提被实测推翻**（这改变了问题本身，不是给了个答案）----------

【第 ③ 项 ISS-025】原问题：「`skills/constraint-mode/SKILL.md` 不存在但被 **7 处引用** → 建文件 还是 改引用？」
**实测：7 处全是「提及」，活指针 0 条。两条路都不用走。**

逐条读原文（不是 grep 计数）：
  ① `.check/baseline.json:24` —— 在 `"missKnown_why"` 这个**说明字符串**里，内容是解释
     「这三条变异的锚点就是 `skills/constraint-mode/SKILL.md` 这个引用文本，Wave 6 改名的必然代价」。**提及**。
  ② `.check/baseline.json:39` —— 在 `"c5count._note"` 里，解释 C5 计数自测的起因。**提及**。
  ③ `.check/check-protocol.js:549`（原记 544，行号已漂移）—— **JS 注释块**里，同一段历史的说明。**提及**。
  ④–⑦ `.check/mutations-thirdparty2.js:251,252,258,264` —— `edit: t => t.replace('`skills/…/SKILL.md`', …)`
     的**字符串字面量**，是**变异夹具**，不是路径引用。

**而且这三条夹具已经死了**（这是本轮新测到的数，见 §二）。
**关键：`grep -rn 'constraint-mode/SKILL.md' .check/` 这个判据口径不对** ——
  · C5（活引用可达性）的**检查面只有 7 段**：`MODE.md` + `refs/A-sources.md` … `refs/F-execution.md`
    （实测 `buildFileSet().segs.length = 7`），**不含 `.check/`**；
  · 所以 C5 **从来没有报过**这 7 处；「7 处」是**一次 grep** 数出来的。
  · **这与 ISS-082 是同一个形状**（判据分不清「引用」与「提及」）—— 第 **6** 次同族。
  · 真正的活指针**只有 5 处，Wave 6 已全部改成 `MODULE.md`**（`baseline.json:24` 原文自述），
    实测确认：`MODE.md:26` 已是 `skills/constraint-mode/MODULE.md`。

**建议：ISS-025 按「前提不成立」结项**，正文追加勘误（不改原行）：
  「7 处 = 提及，活指针 0；C5 从不覆盖 `.check/`，该数是 grep 数出来的」。
  **既不建 `SKILL.md`（规格里没有这个文件），也不改那 7 处**（改了会破坏历史说明与夹具命名的一致性）。
  **唯一值得单独处理的是那 3 条死夹具** → 见 §二。

---------- 二 · 本轮**新测到**的一个数：三套电池里 3 条变异体是「空转」----------
命令（用**正确的文件集**，不是 MODE.md 单文件）：
```
node -e "const {buildFileSet}=require('./.check/fileset.js');const base=buildFileSet().text;
const M=require('./.check/mutations-thirdparty2.js');let n=[],e=[];
for(const m of M){const o=m.edit(base);(o===base?n:e).push(m.id);}
console.log('总 '+M.length+' 有效 '+e.length+' 空转 '+n.length+' → '+n.join(' '));"
```
实测：**总 41 · 有效 38 · 空转 3 → `N36 N38 N40`**。
**这三条正是替换 `` `skills/constraint-mode/SKILL.md` `` 的那三条** —— 目标文本已不在文件集里 → `replace` 是空操作。
**已登记**：`baseline.json:23` 的 `"missKnown": ["N36","N38","N40"]`，理由写得很清楚
（「引用必须指向真实存在的文件，所以改名是对的；锚点失配是它的**必然代价**，不是回归」），
且 `run-battery.js` **能识别空转**（`if (t === base) { miss++; … }`，并打印 `锚点失配明细`）。
**所以这不是缺陷，是一条已经付过账的代价** —— 但**代价是真实的：41 条里 3 条永远不再被测**。
**建议：不修**（改夹具等于改「证据快照」的语义，且会与 `missKnown` 的理由自相矛盾）。
若将来要回收这 3 条覆盖，做法是**新增** 3 条锚在现有文本上的变异，**不是**改旧的那 3 条。

---------- 三 · 另 5 项的全貌（现状 + 选项 + 代价 + 建议）----------

【第 ① 项 ISS-042（并发写者）】原问题：「另一半（记录层全局加锁 / 改自动化时间）仍待裁决」。
**实测：那一半已经落地了 —— DEC-030 的写权锁。**
  · `.check/write-lock.js` 存在且可用：`status` / `acquire <who> [ttlMin]` / `release <who>`；
  · automation 提示词 **§0.3** 已写死「改源文件前必须取写权锁」，`rc=2` = 别人在写 → 只做只读 + 记录；
  · TTL 由**持有者声明**（默认 30 分钟），超期可被抢占（`LOCK takeover`）；守卫规则收拢在 **DEC-032**
    （「已过期」是允许清的唯一理由，名字不参与判定）。
  · 本轮实测：`acquire w23-j3 20` → rc=0 → 收尾 release → `LOCK free`；残留锁 0。
**残余（ISS-042 自己就写明了，且锁解决不了）**：**非本工具的写者** ——
  对方若用 `>>` 或 Edit 直接写，锁拦不住。2026-09-16 那次撞号 5 处，对方正是用「锚点插入」。
**建议：**
  (a) **先把 ISS-042 的状态按事实复核**（「记录层加锁」这一半已由 DEC-030 交付），
      在正文追加勘误指明「已由 DEC-030 覆盖，残余只剩『非本工具写者』」；
  (b) **不建议**再去做「记录层全局加锁」的新机制 —— 会与 DEC-030 重复；
  (c) 「改自动化时间」这条**建议保持不做**：它把「两个会话同一时刻跑」当成病因，
      而实测病因是「两个写者同时写」—— 错开时间只是概率降低，**锁才是机制**。

【第 ② 项 ISS-043（退出码 2 语义重载）】原问题：「`acceptance.js`『2 = 有 DRIFT/UNVERIFIED』vs
  `.check/tests/*`『2 = 拒跑』—— 哪一套是全局口径？」
**实测两套都在跑，都写在源码里：**
  · `.check/acceptance.js:499` → `const EXIT = c('FAIL') ? 1 : (c('DRIFT') || c('UNVERIFIED') ? 2 : 0);`
  · `.check/tests/i5-bidirectional-test.sh:26` → 「**拒跑与失败是两件事，退出码必须分开（0/1/2）**」
  · `write-lock-test.sh` → `0 全通过 / 1 有失败 / 2 拒跑`；`probe-record-shape.js` → `2 有文件缺失`。
**问题的真实形状**：这不是「两套口径打架」，而是**同一个数字在两个不同的工具族里承载不同含义**。
**建议：不改任何代码，改口径文档** —— 在 `MODE.md`（或 `refs/F-execution.md`）里写一行**分区表**：
  「**验收类工具**（`acceptance.js`）：0 通过 / 1 FAIL / 2 DRIFT 或 UNVERIFIED；
   **测试类工具**（`.check/tests/*`、`probe-*.js`）：0 通过 / 1 失败 / 2 拒跑」。
  理由：**改退出码会破坏已有调用方与历史 SEAL 的可比性**（历史 SEAL 里 `EXIT=2` 是 DRIFT 语义）；
  而**写一行表**零风险、且把「同一数字两种含义」变成**明示**而不是**隐患**。
  **代价**：仍要靠人查表。**但换成任何统一方案，都要重写历史 SEAL 的读法 —— 那更贵。**

【第 ④ 项 规格 §六 是否补 `resume` / `evolve`】
**实测**：规格 `andyngo build.md.txt` 第 1185 行起「第六部分 · 一页速查卡」列了 9 条显式命令
  （`start` / 空启动 / `fix` / `verify` / `security` / `deploy` / `retro` / `seal` / `issue`），
  **缺 `resume` 与 `evolve`**；而同一份规格的判事件表（第 152 行起）**有** `RESUME` 与 `EVOLVE` 两行。
  `SKILL.md:43-46` **已补**（`/andyngo resume` `/andyngo evolve` 在列）—— 即 DEC-016 说的「只改了一半」。
**建议：补规格那一行**（把 `/andyngo resume` `/andyngo evolve` 加进速查卡）。理由：
  ① **这是「补」不是「改」**，符合「取超集、任何名字都不删」；
  ② **代价极低**：一行字，不动任何判据；
  ③ 不补的**真实代价**已经出现过 —— 本会话我自己就只读了 `SKILL.md` 而没读规格，
     于是漏掉整段 G1–G7 流程（ISS-086）；**两份文档不一致时，读哪一份决定你漏什么**。
**边界（必须写明）**：规格是**权威文本**，DEC-016 判过「执行者单方面改规格 = 自己给自己发许可」。
  **所以这条必须由用户裁决**，执行者只准备**逐字改动**（一行），不自行落笔。

【第 ⑤ 项 ISS-035（三态 vs 五态）】原问题：「`event-protocol.md:12/26` 三态 vs `SKILL.md:60/61` 五态，
  两份文档要不要统一？」
**实测（含一处必须更正的漂移）**：
  · `references/andyngo-event-protocol.md:12` → `| status | 子代理 | OK/FAIL/BLOCK |`；`:26` → 「status 三态：OK / FAIL / BLOCK」
  · `skills/andyngo/SKILL.md` **现在是 `:83` / `:84`**（**不是 :60/:61** —— SKILL.md 本轮从 64 行变 115 行，行号漂移了）
    → `OK / FAIL / ASK / PROGRESS / BLOCK` + 「PROGRESS 不消耗 ASK 配额」
  · **规格自己两套都写了**：速查卡里 `三态 OK / FAIL / BLOCK` 与
    `输出形态 OK / FAIL / ASK / PROGRESS / BLOCK` **并列**（第 1200-1201 行附近）。
**这条是本轮**证据最强**的一项**：规格**明文并列**两套 → 「取超集」不是我的偏好，是**规格自己的写法**。
  I6 判据已按五态实现（DEC-006），并写明出处与冲突。
**建议：不统一、只补一行对照** —— 在 `event-protocol.md` 的三态处加一句
  「（**子代理回传用三态**；`SKILL.md` 的**输出形态**另有 `ASK`/`PROGRESS` 两态，见规格速查卡）」。
  理由：两份文档**服务两个不同的对象**（子代理回传 vs 主代理输出），
  **把它们统一才是错的** —— 那会把 `SKILL.md` 明文授权的 `PROGRESS`/`ASK` 变成 FAIL（过紧假红）。
  **代价**：多一行字；**收益**：把「看起来矛盾」变成「看起来是两件事」。

【第 ⑥ 项 ISS-026（`evidence/` 下非约定命名文件是否纳入检查面）】
**实测（数字已大幅变化，这是本轮最该被看到的漂移）**：
  · `.check/record/evidence/2026-09-16/` **共 45 个文件**，其中**约定命名 25 个**（`w*-j*-*.txt`）、
    **非约定命名 20 个**；
  · ISS-026 立条时（2026-09-16T00:48Z）是「11 个文件，A 集只收其中 7 个」；
    SEAL v8 记「今天从 3 个变 **5 个**」→ **现在 20 个**。
  · **而且增长是协议自己造成的**：automation §三 硬约束「改任何文件前先归档改前版本到
    `.check/record/evidence/<date>/<原名>.pre-<slug>-<hhmmss>`」——
    **每执行一次归档就多一个非约定命名文件**。本轮 W23 又贡献 3 个
    （`SKILL.md.pre-w23j1-router-045500` · `record-shape.js.pre-iss085-dup-043200` ·
    `probe-record-shape.js.pre-iss085-shape-043000`）。
**这条的性质**：**不是缺陷，是「协议与检查面不一致」** —— 协议**要求**生产这类文件，
  而检查面**只认** `w*-j*-*.txt`。后果：这 20 个文件**不会被误报，也永远不会被发现丢失或损坏**。
**建议：纳入，但用「清单式」而不是「命名式」** ——
  ① 让 A 集之外的 evidence 文件进入一份**可核对的清单**（例如每次收尾列出 `ls` 结果与数量），
     判据只做「**数量只增不减 + 每个文件非空**」两条；**不要求命名合规**（协议要它们这么命名）；
  ② **不要**改 A 集定义 —— 那会连带影响 D1/D2/D3/D6 与双向测 29 个用例（ISS-026 原文已写明）。
  **代价**：多两条弱判据；**收益**：把「隐形」变成「可见」—— 而「看不见的东西丢了不会被发现」
  正是 ISS-026 的原话。
  **若用户想更省事**：也可以**只登记不改判据**（在 SEAL 里每次报一个数字，漂移靠人看）——
  但那就是「展示层数字」，而本项目已实测「只用于展示的数字最容易腐烂」。

【第 ⑦ 项 CHG-025「冻结 md5」→ 建议改版本表】
**实测：更好的机制已经存在，CHG 行里的 md5 只是它的一个过期快照。**
  · `node .check/file-history.js --list .check/tests/i5-bidirectional-test.sh` 实测输出：
    `v1 34b3b3a5 cf295906dcb0f891f019fc6e382ff810 194` ← **正是 CHG-025 冻结的那个 md5**
    （v2 300 行 / v3 328 行 / v4 399 行 …，`DISK md5=5a2eea14e8a80bf89557ecaf146965ff`）
  · 即：**产品一直在留版本，而 CHG 行把某一版钉成了常量** → 文件一改就「失效」，
    SEAL v8 记这是**第 6 次失效**。CHG-025 冻结 `cf295906…`(194 行)，CHG-024 改成 `9691b053…`(197 行)，
    现盘 `5a2eea14…` —— **已经过期两轮**。
**建议：CHG 行不再写「冻结 md5」，改写「版本锚点」** —— 格式：
  「归档时 = `file-history` 的 **v1**（`cf295906…` / 194 行）；**历次版本见
   `node .check/file-history.js --list <路径>`**（取某一版加 `--conv <会话前缀>`，见 ISS-059）」。
  理由：**把常量换成指针** —— 常量会腐烂，指针不会；而且这个指针**已经有人维护**（产品自己）。
  **代价**：读的人要跑一条命令；**收益**：不会再出现「第 7 次失效」。
  **注意**：`.check/record/` 下**目前没有** `versions.md` 之类的版本表；
  若用户更想要一张**静态表**，那就得**手工维护**它 —— 而手工维护正是 CHG-025 失败的原因。**建议用指针。**

---------- 四 · 本文件**没有**做的事（边界）----------
- **没有翻任何一条 ISS 的状态**（025/026/035/042/043 全部仍 `open`）；
- **没有改任何源文件**（本轮只读 + 本文件 + 一条 EVT + 一条 ISS-087 记录上述实测）；
- **没有改规格**（第 ④ 项按 DEC-016 的原则必须由用户裁决）；
- **没有改 automation 提示词**。
- 未知项如实标注：第 ③ 项的「3 条死夹具」**是否值得回收覆盖**是策略问题，不是事实问题，未判。

---------- 五 · 一句话总结 ----------
7 项里，**2 项的前提被实测推翻或大半已落地**（③ 活指针 0 条 · ① 已由 DEC-030 交付），
**2 项证据强到可以直接判**（⑤ 规格自己并列两套 · ⑦ 更好的机制已存在），
**1 项是必须由用户落笔的越界项**（④ 规格是权威文本），
**2 项是策略取舍**（② 口径分区表 · ⑥ 隐形归档件纳不纳入）。
**全部 7 项都不需要新机制** —— 需要的是**把已有的东西写对、写清、指向对**。

判据：
  `node -e "const {buildFileSet}=require('./.check/fileset.js');console.log(buildFileSet().segs.length)"` → **7**（不含 `.check/`）
  `grep -n 'constraint-mode/SKILL.md' MODE.md refs/*.md` → **空**（活指针已全改 `MODULE.md`）
  `sed -n '499p' .check/acceptance.js` → `const EXIT = c('FAIL') ? 1 : (c('DRIFT') || c('UNVERIFIED') ? 2 : 0);`
  `grep -n 'resume\|evolve' "C:/Users/<user>/Desktop/andyngo build.md.txt"` → 速查卡区无命中
  `grep -n 'status\|OK / FAIL' skills/andyngo/SKILL.md` → `:83` `:84`（**已漂移，非 :60/:61**）
  `ls -1 .check/record/evidence/2026-09-16/ | wc -l` → **45**；非约定命名 **20**
  `node .check/file-history.js --list .check/tests/i5-bidirectional-test.sh` → `v1 … cf295906… 194`
  `node .check/run-battery.js .check/mutations-thirdparty2.js` → `锚点失配明细 N36,N38,N40`
EVT 026
