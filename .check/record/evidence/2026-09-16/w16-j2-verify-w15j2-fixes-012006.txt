================================================================================
w16-j2 · 独立复验 W15 J2 的五条修法（判据 v3.8 / 双向测 v3.5）+ ISS-029 结案
时间：2026-09-16T01:20Z（09:20 GMT+8）
工作根：C:\Users\<user>\.workbuddy-ai
前序：w16-j1-converge-regression-011526.txt（本自动化第一段）
本段性质：**独立复验 + 记录层追加**。**零源文件改动**（DEC-022）。
================================================================================

【0】结论先说

**W15 J2 报的五条修法（F-H1…F-H5），我用自己写的夹具独立复验，五条全部成立。**
**我没有只重跑它的 R34/R35/R36 夹具** —— 那样只是「同一把尺子再量一次」。
下面每条都给一个**它没写过的输入**。

被测件（md5 跑前=跑后，DEC-021）：
```
e8d1386e91e983c4961f5796a2486558  .check/andyngo-integrity.js          537 行 · v3.8
5a2eea14e8a80bf89557ecaf146965ff  .check/tests/i5-bidirectional-test.sh 576 行 · v3.5 · 36 assert
```

--------------------------------------------------------------------------------
【1】五条独立探针（全部自建夹具，`mktemp -d`，用完即删）

### 探针 A —— F-H1：`evidence_ptr` 指向**目录**（我自己搭的，不是 R34）
夹具：`record/evidence/2026-09-16/w1-j1-adir.txt` **是一个目录**（名字合规），事件行指向它。
```
$ node .check/andyngo-integrity.js <夹具日志>          RC=1
[I3] evidence_ptr 存在   OK    全部 evidence_ptr 真实存在（`--` 已放行）
[I5] 事件行 ↔ 作业留证   FAIL  D5 留证不是非空文件: w1-j1-adir.txt（**是目录**） ｜ A=1 B=1 豁免=0
```
**三条断言全部成立**：① I3 判**存在**（v3.7 的 `isFile` 在这里会报红）；② D5 的标签是「**是目录**」
（不是 v3.7 那版在 Windows 上会报的「零字节」—— **标签与事实相符**）；③ 约束没有消失，只是
**归位**到了 D5。**F-H1 的实质是「口径归位」，不是「放宽」—— 这一点被实测证实。**

### 探针 B —— F-H2：I6 的五态保留 + 可证伪性保留
```
B1  status=GARBAGE-STATUS   RC=1
    [I6] FAIL  非法取值 1 处: 第1行(GARBAGE-STATUS)（合法值来自 SKILL.md 输出形态: OK/FAIL/ASK/PROGRESS/BLOCK）
B2  status=PROGRESS         RC=0
    [I6] OK    全部 status ∈ {OK/FAIL/ASK/PROGRESS/BLOCK}
```
**B1 证明它仍可证伪**（W9 那句「不可证伪的过宽」确实过强 —— W15 J2 自己已修正这句措辞）；
**B2 证明它没有收紧到 `event-protocol.md` 的三态**（收紧会让 `SKILL.md:61` 明文授权的 `PROGRESS` 变红）。
**两处互相印证、取超集、不删名字** —— 与用户裁决「保留原本的要求」同法。

### 探针 B3 —— **反向**：撤回 `isFile` 有没有把真正的检查也一起撤掉？
```
B3  evidence_ptr = <不存在的路径>   RC=1
    [I3] FAIL  缺失 1 个: …/record/evidence/2026-09-16/NOPE-missing.txt
```
**这是本段我最想验的一条**：F-H1 是「撤回一条约束」，而**撤回约束的修法有对称风险**——
撤多了就把该抓的也放了（同族于「修法的残留」，只是方向相反）。
实测：**存在性仍然被抓住**，撤回的只是「必须是文件」这一条规格里没有的约束。**没有撤过头。**

### 探针 C —— F-H4：环境变量不得能跳过 R33
```
$ ANDYNGO_BIDI_NO_NEST=1 bash .check/tests/i5-bidirectional-test.sh
  EXIT=0 · 276 PASS / 0 FAIL
  双向测结论: 全通过（0 失败 · 跳过 0 项）        ← 跳过计数 = 0，**没有被跳过**
  [PASS] R33 前置：目标目录尚未创建
  [PASS] R33 未拒跑（未创建的临时目录被接受，守门人不再自相矛盾）
  [PASS] R33 子进程真的跑完了（输出含结论行）
```
**这是 W9 的原复现命令原样重跑**（`ANDYNGO_BIDI_NO_NEST=1`）：v3.4 下断言数 264→263 且结论照样
「全通过」（**把没跑报成通过**），v3.5 下 **R33 照跑、`跳过 0 项` 显式打印**。
**「跳过必须可见」这一半比「不能跳过」更重要** —— 后者只是堵住今天这个入口。

### 探针 D —— F-H5：状态**按列位置**读，且元断言把「换子串不是修法」固化
```
$ ANDYNGO_BIDI_NO_NEST=1 bash … | grep R36
  [PASS] R36 口径三正确数出 1 条非 OK
  [PASS] R36 口径一确实漏数（证明修复必要）
  [PASS] R36 口径二确实假红（证明「换子串」不是修法）
```
**R36 是本轮最有价值的一条夹具**：它把**三个口径放在同一段合成输出上**，并断言
**只有口径三是对的**。意义在于它测的不是「现在的代码对不对」，而是
**「为什么不能用换子串的办法修」** —— 把一次踩坑变成了一条**永远会红的反面证据**。
（对照 DEC-019：「永远绿的断言」不会被看见；R36 把两个错误口径都变成了**必须为红**的断言。）

### 探针 E —— F-H3：一个探针，多个调用者（结构核对）
```
165:  function statOf(p) { try { return fs.statSync(p); } catch (e) { return null; } }
185:    if (!statOf(p)) missing.push(ev);                    ← I3
388:    const st = statOf(path.join(evDir, a));              ← D5
492:    const st = statOf(p);   // 与 I3 **同一个探针**（v3.8，修 F-H3）  ← I7
```
**一份实现、三个调用者**。原先 I3 用 `fs.existsSync`、I7 用 `existsSync + statSync` 两段 ——
两个探针在**设备路径**（`//./NUL`）上结论相反，导致 `oddArt` 分支在 Windows 上**不可达**
（= 一条永远绿的断言）。**这条只能靠读代码验，不能靠跑夹具验** —— 故如实标为「结构核对」。

--------------------------------------------------------------------------------
【2】ISS-029（F-C 分歧）**结案**：W9 自己撤回了它，并给出了规格原文依据

W9 第五轮的 F-H1 原文（W15 J2 留证 §0 引述）：
> 「**这与 F-C 恰好相反**：F-C 我要求收紧 I7，而 W9 当时承认『我实测过收紧的后果』；
>   现在同一把尺子量 I3，I3 才是**超规的那个**。
>   **你在同一个改动里犯了两次**：I7 放宽、I3 收紧 —— 而 I3 的收紧**没有任何规格依据**。」

规格原文（`skills/andyngo/references/andyngo-event-protocol.md`）：
```
第 14 行  | evidence_ptr | 子代理 | 必须真实存在 |
第 24 行  2. 证据写磁盘，路径放 evidence_ptr（必须真实存在）
```
**只说「必须真实存在」，没有说必须是文件。** 这与第 13/23 行对 `artifact_ptr` 的措辞**完全同构**。

**ISS-029 的开放条件（「若 W9 能给出规格原文依据 → 立刻收紧」）现在以相反方向被满足**：
不是 I7 该收紧，而是 **I3 本来就不该收紧**。现盘口径：
```
[I3] evidence_ptr 存在   → 只判存在（探针 A/B3 双向验证）
[I7] artifact_ptr 存在   → 只判存在 + 目录逐条点名
[D5] 留证非空文件        → 「是目录 / 不是普通文件 / 零字节」三标签分开
```
**三个问题归三个判据**（存在性 / 命名 / 非空文件）—— 这正是 DEC-018「口径由规格决定，
不由对称美学决定」的完整版，而不是它的一半。

--------------------------------------------------------------------------------
【3】ISS-035 编号撞号（**并发写者这条 ISS 自己又制造了一个实例**）

W15 J2 的留证里写着「…并把两份文档要不要统一记为新 **ISS-035**」。
而我在 **01:16:11Z** 已把 **ISS-035** 用给了「并发会话写同一批文件」。
**两个写者、一个 ID 空间、没有锁** —— 这是 ISS-035 自己的形状，又一次现身（只是换到了记录层）。

**处置（append-only，不改历史）**：
- ISS-035 = 并发写者（本自动化，已写入）
- ISS-036 = 退出码 2 语义重载（本自动化，已写入）
- ISS-037 = 勘误批（本自动化，已写入）
- **ISS-038 = W15 J2 计划中的「两份文档 status 枚举不一致（三态 vs 五态）」** —— 由本次代为登记
- **ISS-039 = ISS-029 结案**（本节 §2）

**规则（DEC-023）**：并发写者存在时，记录层新 ID **必须先读后写**；**撞号以先写入者为准**，
后写者**追加勘误**、**不重编号、不删行**。

--------------------------------------------------------------------------------
【4】现盘回归（在 v3.8 / v3.5 上）

| 项 | 结果 | 退出码 | 说明 |
|---|---|---|---|
| `node .check/andyngo-integrity.js`（全部日志） | `14 OK / 0 FAIL / 0 UNVERIFIED` | **0** | 判据 `e8d1386e…` |
| `i5-bidirectional-test.sh`（裸跑） | `276 PASS / 0 FAIL · 跳过 0 项` | **0** | 测试 `5a2eea14…` |
| `i5-bidirectional-test.sh`（`ANDYNGO_BIDI_NO_NEST=1`） | `276 PASS / 0 FAIL · 跳过 0 项` | **0** | 与裸跑**逐项相同** |
| `judge-selftest.sh` | 全通过（0 失败） | **0** | `fa1a8eba…` |
| ACCEPTANCE（`audit-010807.txt`，01:08–01:11Z） | `11 OK / 0 FAIL / 0 DRIFT / 1 UNVERIFIED` | 2 | **不受本段改动影响** —— 见下 |

**为什么 ACCEPTANCE 不用重跑**：`grep -n 'integrity\|i5-bidirectional' .check/acceptance.js` → **零命中**。
验收 [1]–[10] 的被测对象是 `check-protocol.js`（md5 `4f0aa2d0…`，自 09:06 未变）、
`mutations-*.js`、`selftest-*.js`、`battery-judge.js`、hub 标价 —— **都不在本次改动面内**。
故 01:08 那次结论对现盘仍然成立。**这是推理不是复跑，如实标明。**

--------------------------------------------------------------------------------
【5】仍不能验的（**不许合并进 OK/FAIL**）

| 项 | 为什么 |
|---|---|
| 探针 E（F-H3 单探针） | **结构核对**（grep 三处调用同一 `statOf`），**不是行为验证** —— 我没造出「两个探针在设备路径上结论相反」的环境（Windows 上 `//./NUL` 的行为不宜当夹具）。 |
| 并发写者是否已收工 | 只测到「截至 01:20Z 连续 5s 稳定」+ 对方会话日志最后一次写入 01:17:27Z。**这不等于它结束了** —— 它可能只是暂停。 |
| ISS-038 该往哪边统一 | 规格内部矛盾（`event-protocol.md:12/26` 三态 vs `SKILL.md:60/61` 五态），**不由执行者单方面决定**（同 DEC-005 / ISS-025）。 |
| `[98]` | 仍是「**未到点**」。 |

================================================================================

--------------------------------------------------------------------------------
【勘误 · 追加于 2026-09-16T01:22Z（不改上文，只追加）】

**上文 §3 的仲裁建议已被 `w16-j3-id-collision-012200.txt` 取代。**
§3 写的时候（01:20:06Z）我看到的是「W15 J2 计划用 ISS-035」，故建议把 F-H2 条目登记为 ISS-038。
**但 01:19–01:20 之间，另一会话已把 ISS-035…ISS-041 七条写进 `issues.md`**（插在 ISS-034 之后），
于是 §3 建议的那个号**也已被占用**。**真正的撞号比 §3 估计的严重**：实测 5 处。
本节保留原样，理由：**证据记录的是「当时观察到了什么」，不是「事后什么是真的」** ——
修正走新文件，不改旧文件。
