W23 J5 · 2026-09-16T05:28Z · 执行用户裁决「丙类 · 去加判据，不是去问人」
================================================================================

用户指令原文：
> 丙类 · 根本不该由人判：应该由判据判的 → 去加判据，不是去问人

即：把七项待裁决里的 **②③⑤⑥⑦**（我上一轮归为「丙类」的五项）**从「问人」变成「判据答」**。

---------- 一 · 新建的判据：`.check/probe-crossref.js`（445 行）----------
**主题只有一个**：**A 与 B 的关系有没有被写下来。** 六条规则，每条只问这一件事：

| 规则 | 问什么 | 对应 | 形态 |
|---|---|---|---|
| **X1** | 每个含 `exit 2` 的活文件，**有没有在 `exit-codes.md` 里登记它的语义** | ISS-043（②） | 判红 |
| **X2** | `event-protocol.md` 出现「三态」处，**有没有对照声明** | ISS-035（⑤） | 判红 |
| **X3** | `evidence/` 各日期目录文件数**只增不减且非空** | ISS-026（⑥） | 判红 |
| **X4** | **比基线更新的** CHG 行，第 2 字段是不是合规 ts | 新发现（见 §五） | 判红 |
| **X5** | 记录里的 md5 与磁盘**差多少** | CHG-025（⑦） | **只报告** |
| **X6** | 活引用检查面（C5）**覆盖哪些文件** | ISS-025（③） | **只报告** |

**为什么两条只报告**：它们指向的**全是历史行（append-only）**，判红会变成永远红的断言 ——
而「永远红的断言会被训练成忽略」（DEC-019）。**只报告**让腐烂可见，且不会钝化红灯。

---------- 二 · 四条**量出来的修正**（先量误伤，再定规则 —— 顺序不能反）----------
1. **X1 第一版**：要求「每处 `exit 2` ±3 行内有语义词」→ 命中 13 处，**横跨 3 个 skill**，
   而多数**本来就写了**（「用法」「拒绝在错的根上产出」「基线不干净，先停」）→ 假红。
   **改成登记制**（一处 `exit-codes.md`）→ 命中 0。
2. **X1 的注释识别**：`verify-ledger.js:13` 是**块注释里的文档行**（不以 `*` 开头）→
   按行首判注释会漏 → 必须**先剥注释再判**（`stripComments`）。
3. **X3**：第一版把子目录 `_superseded` 当文件、`statSync().size` 在目录上返回 **0** →
   报「0 字节（归档件损坏）」= **纯假红**。**「目录的 size 是 0」是平台行为，不是损坏。**
4. **X4 的前两版（都废弃，负面结果）**：
   - v1「凡有 md5 的行都要有指针」→ 命中 **47/61（77%）**，全是「改前/改后 md5」这个**格式字段** → 废。
   - v2 收紧到「声称不变」的词表（冻结/永久/不变/以此为准/锁定）→ 12 条，抽样 6 条里
     `永久 UNVERIFIED` · `行数不变` · `永久红` **三条是普通叙述** → **假红率 ≈ 50%，不可判红**。
   - **结论：「哪条 md5 是不变式」在文本层面与普通叙述同形 —— 这条做不成判据。**
   - v3 改判**机械确定**的东西 → 见 §五。
5. **X6 的判红版也废弃**：「凡出现『N 处引用』必须写明口径」→ 16 个候选里 6 个命中，
   抽看即知多数是普通叙述里的「N 处」+「引用」**同形** → 同样做不成判据。
   **改成每次跑打印检查面**（口径永远可见，不需要谁记得问）。

---------- 三 · 三重验证（**判据会红、会绿、双向测两边都动过**）----------
**① 双向测 `--inject`**：**16/16 符合**（X1 六条 · X2 两条 · X4 四条 · X5 四条），RC=0。
  ★ 夹具本身也踩了一个坑并修掉：第一版把**中文标签直接拼进被测字符串**（例如「（合规 ts）」），
    导致 **13 条里 7 条假失败** —— 那不是判定体坏了，**是尺子自己带着脏东西**。
    修法：元组改成 `[规则, 标签, **输入**, 判定体, 期望]`，**标签与输入分开**。
**② 反向验证（不能省 —— 「只会说 OK 的判据」和「没有判据」长得一样）**：
  · 移开 `exit-codes.md` → `[X1] 检查 15 处 · **命中 15** · **RC=1**`
  · 移开 `crossref-baseline.json` → `[X4] 检查 **101** 处 · **命中 11** · **RC=1**`
  · 复原 → `[X1..X6] 全 OK · **RC=0**`
**③ 接入**：`skills/andyngo/scripts/andyngo-audit.sh` 加 `=== CROSSREF (X1-X6) ===` 段，
  跑 `--scan` + `--inject`。**不改 automation 提示词**（那属「动用户配置」= 乙类）。
  **为什么加在这里**：该脚本第 21-24 行自己写着 ——
  **「一段没人调用的判据 = 一条没有判据的规则，只是换了个地方藏着」**，而它正是 AUDIT 的载体。
  ⚠ **未验证**：**端到端跑一次 `andyngo-audit.sh` 没有做**（它会连带跑 acceptance.js，约 9m38s）。
  已验证的只有：`bash -n` 语法通过 + `probe-crossref.js` 单独跑 RC=0。**不合并进 OK。**

---------- 四 · 新建的两个数据文件 ----------
- `.check/record/exit-codes.md`（43 行）：**退出码 2 的分区表**，逐文件写清 `2` 是
  **甲 · 拒跑**（参数错/路径不对/数据源缺失/基线不干净/锁被占）还是
  **乙 · 结果类**（`DRIFT` / `UNVERIFIED`）。**16 行登记，15 个活文件命中，悬空 0。**
  写明了**已知边界**：用 `process.exit(code)` 变量的文件（`verify-ledger.js`）**抓不到**，靠它自己的头注释。
- `.check/record/crossref-baseline.json`（9 行）：`evidence`（X3 的各日期文件数）+
  `changesMaxId: CHG-101`（X4 只判比它更新的行）。
  **唯一的纪律点写进了 `_how_to_update`：「每写一条新 CHG 都要重跑 `--baseline`」** —— 如实标注。

---------- 五 · 测量意外挖出的**真缺陷**（比原目标更值钱）----------
`changes.md` 的 CHG 行**第 2 字段没有任何判据管** → 实测 **11 条不合规**：
**CHG-073…082 共 10 条第 2 字段直接是文件路径**；**CHG-072 是 `2026-09-16T02:02:00Z`（带了秒）**。
`eventlog` 有 **I4** 管 ts，**`changes.md` 没有**；`probe-record-shape.js` 只查「前缀+3位号+字段数下限 9」，
**缺 ts 的行字段数照样 ≥9 → 全绿通过**。
**危害本轮亲历**：X5 第一版用 `awk` 式解析，把 **md5 当成了文件路径** → 报出 **4 条 `GONE` 假象**
（`8a47c95d…` / `3f3192d5…` / `3ec79d94…` / `a06b4a2f…`）。
→ 立 **ISS-088**（resolved）：X4 上线 + 历史 11 条由基线只报告。
**顺带修正上一轮报告的一处推断**：`acceptance.js:56` 的 `exit(2)` **不是**参数错，是
`结论：0 OK / 0 FAIL / 0 DRIFT / **1 UNVERIFIED**` 那一支，与 `:499` 的 `EXIT` 定式**同义** ——
已按「读原文」写进 `exit-codes.md`，**不靠行号猜**。

---------- 六 · ★ 一处纪律偏差，如实记 ----------
**本轮改前没有先归档**（automation §三 硬约束），是**改完之后**才补救的：
`node .check/file-history.js --get <路径> <vN> --conv 34b3b3a5` 取回改前版本，实测**两件都取到且 md5 逐字符一致** ——
`andyngo-audit.sh` = `547677857f12ea47e95a8c1002b80bff` / 35 行 ·
`andyngo-event-protocol.md` = `7ab03d9cbd333ac9631cf270011d48f2` / 70 行；
归档到 `.check/record/evidence/2026-09-16/{andyngo-audit.sh,andyngo-event-protocol.md}.pre-w23j5-crossref-052800`。
**所以「改前版本不可回溯」在本机再一次不成立**（DEC-029 / ISS-054→ISS-056）——
**但这次是靠事后补救，不是靠流程正确。下次仍要先归档。**

---------- 七 · 改了哪些文件（改前 md5 已核对）----------
| 文件 | 前 md5 | 后 md5 | 行数 |
|---|---|---|---|
| `.check/probe-crossref.js`（**新增**） | `--` | `35e796647cf91693650e56af651b8791` | 0 → **445** |
| `.check/record/exit-codes.md`（**新增**） | `--` | `c9724de957062dd2640b3d9be1b70c6c` | 0 → **43** |
| `.check/record/crossref-baseline.json`（**新增**） | `--` | `6d5bfd08b2467d79e844d6b3ac5922d9` | 0 → **9** |
| `skills/andyngo/references/andyngo-event-protocol.md` | `7ab03d9cbd333ac9631cf270011d48f2` | `9c3903e4250d2f5532f1d071b395950c` | 70 → **75** |
| `skills/andyngo/scripts/andyngo-audit.sh` | `547677857f12ea47e95a8c1002b80bff` | `395512dd19eab102c72f6087d91ad53b` | 35 → **56** |

**冻结件三件 md5 零改动** ✓（`e8d1386e…` / `4f0aa2d0…` / `5a2eea14…`）→ W9 第六轮独立复验继续有效。

---------- 八 · 仍不能验的（**不许合并进 OK/FAIL**）----------
1. **端到端没跑 `andyngo-audit.sh`**（约 9m38s）→ 接入只验到「语法通过 + 单独跑通」。
2. **X4 的「新行」路径本轮验不到** —— 基线已到 `CHG-101`，而本轮新写的 `CHG-102/103` 是合规的；
   **一条不合规的新行**要等下次才可能出现（反向验证是移开基线做的，那是「历史全判」路径）。
3. **X4 的基线机制有纪律点**：忘了重跑 `--baseline` → 新行落进「历史」而不受判。**已写进 `_how_to_update`，但那是纪律不是机制。**
4. **X1 抓不到用变量退出的文件**（`process.exit(code)`）—— 已知边界，写在表里。
5. **X5/X6 不判红** → 它们**不会阻止**任何东西，只让腐烂可见。**这是刻意的**（见 §一），
   但**必须说清：⑦ 和 ③ 并没有被「判据」锁住，只是被「看见」了。**
6. 沿用未结：豁免表可被扩大而无判据能防滥用 · skill 发现链路末端无判据（ISS-083）·
   ISS-084（automation §0.5 物理位置仍在 §0.3 之后）· 写权锁抢占路径无并发压测 ·
   **W9 第六轮独立复验结论仍未回**。

---------- 九 · 记录层变化 ----------
`ISS-025 / 026 / 035 / 043 / 087` → **resolved**（就地翻 status 列，时间列未动）· 新增 `ISS-088`（resolved）·
新增 `CHG-102`（3 个新文件）· `CHG-103`（2 个改文件）· 新增 `EVT 027`。
**待用户裁决仍剩：① ISS-042（只需复核，已由 DEC-030 交付）· ④ 规格 §六 补 `resume`/`evolve`（乙类，权威文本）。**

判据：
  `node .check/probe-crossref.js --scan`（**RC=0** · X1 15 处登记 · X4 历史 11 条只报告 · X5 27 文件/过期 11 · X6 检查面 7 段）
  `node .check/probe-crossref.js --inject`（**16/16 · RC=0**）
  `node .check/probe-crossref.js --baseline`（重跑基线）
  `md5sum .check/andyngo-integrity.js .check/check-protocol.js .check/tests/i5-bidirectional-test.sh`
EVT 027
