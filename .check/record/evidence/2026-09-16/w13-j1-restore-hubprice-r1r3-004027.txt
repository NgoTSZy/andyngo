================================================================================
w13-j1 · skill 层还原 + hub 标价归零 + R1/R3 路由覆盖收口
时间：2026-09-16T00:40Z
触发：用户裁决「二、能。完整运作的前提下，用最小的改动。并保留。原本的要求。」
================================================================================

【0】本条对应的是用户裁决的执行，不是我自己发现的问题。三条改动都服从同一个约束：
    **完整运作 + 最小改动 + 保留原本的要求（取超集，任何名字都不删）。**

--------------------------------------------------------------------------------
【1】skill 层还原（选项二「重建 skill 层」）

先演练、再落盘。**演练不写任何东西**：
  $ cd .backup-skills-20260915-2135 && node restore.js
  [DRY-RUN] 写入=47 跳过(已一致)=3 冲突=1 异常=0
  目标: C:\Users\<user>\.workbuddy-ai\skills
  备份: …(22 目录 / 51 文件 / 626009 字节)
  --- 问题 ---
    目标已存在且内容不同(需 --force): andyngo\SKILL.md

**唯一冲突 = `andyngo\SKILL.md`** —— 备份里的是**旧版单文件 skill**，现盘的是
按规格新建的 **22 文件 `/andyngo` 系统**。两者不是同一个东西。

→ **判断：不加 `--force`。** `restore.js` 的默认行为（跳过已存在且不同的）**就是**最小改动：
   它把缺的补回来，不碰任何「已经不一样」的东西。
   「保留原本的要求」在这里的落地 = **新的 /andyngo 系统一个字节都不动**。

落盘前先给现盘做快照（可回滚）：
  $ cp -r skills .backup-skills-pre-restore-20260916-002854     # 25 文件
  $ md5sum .backup-skills-pre-restore-20260916-002854/andyngo/SKILL.md skills/andyngo/SKILL.md
    653570d7ce66e5ff46a1319422073f8c  （两者相同 = 快照忠实）

执行：
  $ node restore.js --apply
  [APPLY] 写入=47 跳过(已一致)=3 冲突=1 异常=0

落盘后核对：
  $ md5sum skills/andyngo/SKILL.md
    653570d7ce66e5ff46a1319422073f8c   ← **与改前一致，新系统未被触碰**
  $ find skills/andyngo -type f | wc -l
    22                                   ← 仍是 22 文件
  $ ls -d skills/*/ | wc -l ; find skills -type f | wc -l
    22 ; 72                              ← 22 个目录 / 72 文件
  $ for p in skills/hub/routes.md skills/hub/price.js skills/constraint-mode/MODULE.md; do … done
    OK   skills/hub/routes.md
    OK   skills/hub/price.js
    OK   skills/constraint-mode/MODULE.md
    MISS skills/constraint-mode/SKILL.md   ← 见下

⚠ **`skills/constraint-mode/SKILL.md` 仍然缺** —— 因为**备份里本来就没有**（备份只有
  `constraint-mode/MODULE.md`）。所以这不是还原没做全，而是**清除之前就已经悬空**的引用。
  引用它的地方：`.check/baseline.json:24,39` · `.check/check-protocol.js:544` ·
  `.check/mutations-thirdparty2.js:251,252,258,264`。
  → 记为新发现，见【4】。

★ **还原的效果（这才是「完整运作」的证据）**：
  验收整体  4 OK / 3 FAIL / 1 DRIFT / 4 UNVERIFIED   →   **10 OK / 1 FAIL / 0 DRIFT / 1 UNVERIFIED**
  逐项： [1] FAIL→OK（PASS 7/FAIL 1 → PASS 8/FAIL 0） · [2] UNVERIFIED→OK（96/96）
         [3] UNVERIFIED→OK · [4] FAIL→OK · [5] FAIL→OK · [6] DRIFT→OK
         [10] **UNVERIFIED→FAIL** ← 见【2】，这一项是「从永远测不了」变成「真的测出来了」
  也就是说：那些红/UNVERIFIED **不是被「改文档」消掉的，是被「文件真的回来了」消掉的**。
  这反过来证明「改文档」那条路只会让红在纸面上消失 —— 判据要的是真实存在的文件。

--------------------------------------------------------------------------------
【2】hub 标价归零（验收 [10]）—— 恢复后**第一次真正跑起来**的判据

恢复前：[10] 恒 `UNVERIFIED 输出解析不出（退出码 1）`（`skills/hub/price.js` 不存在）。
恢复后：`FAIL  登记 21 本 · 磁盘 22 本 · 数值不一致 1 · 缺登记 0`

细节：
  $ node skills/hub/price.js --check
    x andyngo  表 1136/1136  ≠  实测 548/6602

根因：`routes.md` 里 `andyngo` 那一行还是**旧版单文件 skill** 的数（1,136/1,136）；
      重建后的 22 文件系统实测 548/6,602。

第一次修（只改数字）：
  第 73 行  `1,136 | 1,136` → `548 | **6,602** ⚠`
  `--check` → `数值不一致 0` ✓

**然后我改了 `SKILL.md`（见【3】）→ 表立刻又旧了：**
  $ node skills/hub/price.js --check
    x andyngo  表 548/6602  ≠  实测 561/6616
    rc=1

**这是本条的真正教训：顺序错了。**
  「模块改动后要重算标价」这句话 `routes.md` 自己写着，但**我把它执行成了
   「先更新标价 → 再改模块」**。标价是模块的**函数**；模块一动，标价就必须重算。
  **「改完再核对」如果发生在「改完之前」，等于没核对。**
  → 已把这条顺序规则写进 `routes.md` 维护节，不只是记在这里。

第二次修（顺序修正后）：
  第 73 行 `548 | **6,602** ⚠` → `561 | **6,616** ⚠`；正文两处同步
  $ node skills/hub/price.js --check
    登记 21 本 · 磁盘 22 本 · 数值不一致 0 · 缺登记 0     rc=0 ✓

同时修掉一处**因改动而变假**的断言：原文写
  「`national-fund-architect` 是**唯一**标价与完整成本差一个量级的独立 skill」
—— `andyngo` 现在 561 → 6,616（约 12 倍），**「唯一」不再为真**。
  改前把「唯一」留着 = 留一句假话在权威表里。已改为「**两本**」并各给数字。

--------------------------------------------------------------------------------
【3】R1 / R3 路由覆盖收口（规格 §3.1 与判事件表之间的最后两个洞）

R1：frontmatter 声明 13 个触发词，「启动」「开始」在判事件表 11 行里**无任何一行接住**。
R3：`RESUME` / `EVOLVE` 是判事件表里的事件，但**无同名命令名**。

改法（**取超集，任何名字都不删** —— 与 DEC-006 同法）：
  第 19 行  `| 做/建/写 + 新对象 | TASK | A |`
         → `| 做/建/写 + 新对象；启动/开始 | TASK | A |`
  第 32 行  显式入口追加 `` `/andyngo resume` `` `` `/andyngo evolve` ``
  （**就地改，仍 64 行** —— 不新增行、不重排）

改前归档：`.check/record/evidence/2026-09-16/SKILL.md.pre-r1r3-003500`
  md5 `653570d7ce66e5ff46a1319422073f8c` → 改后 `aa4d141bddca99d72dce980d49835d3b`

验证（两种独立口径）：
  ① 关键字包含关系（node 实现，读 frontmatter 触发词 × 判事件表关键字）：
       13 个触发词全部接住 · 未接住 **0** 个（改前 2 个：启动、开始）
       （第一次我用 `grep -w` 式的字面匹配，把「测一下」「验收」误报成 MISS ——
        因为表里写的是「测」「验」。**那是我的判据太窄，不是被测对象的缺陷。**）
  ② 速查卡 8 入口覆盖（既有判据，防回归）：
       缺口数 = 0   VERDICT: PASS（速查卡 8 个入口全部被接住）   rc=0

★ R3 的「另一半」仍未做：所称「速查卡也缺 `resume`/`evolve`」指的是**规格原文**
  （`andyngo build.md.txt` §六）。改规格原文超出「执行者单方面改」的边界 → 见【4】。

--------------------------------------------------------------------------------
【4】本轮新发现（未修，交后续/独立验收）

  (a) `skills/constraint-mode/SKILL.md` **不存在**（备份里也没有，清除之前就悬空）
      被 7 处引用：`.check/baseline.json:24,39` · `.check/check-protocol.js:544` ·
      `.check/mutations-thirdparty2.js:251,252,258,264`。
      现状：验收 [1] 仍 OK（PASS 8/FAIL 0）→ 说明这些引用**没有**被判据当活引用抓。
      **要么**建这个文件，**要么**把引用改成 `constraint-mode/MODULE.md`（真实文件名）。
      → 需要判断哪个才对：`MODULE.md` 是实际存在的入口，`SKILL.md` 是被引用的名字。
      **未动**：改判据/基线会动到验收 [1][3][6] 的被测对象。
  (b) `evidence/` 目录下的**非约定命名文件**（`SKILL.md.pre-r1r3-003500` ·
      `SKILL.md.as-spec-220801` · `andyngo-audit.sh.as-spec-pre-wiring-1449` 等）
      **完全不被任何判据看见** —— I5 的 A 集只收 `^w\d+-j\d+-.+\.txt$`，
      其余一律隐形。**归档件需要一个家，但「隐形」意味着「无人看管」**：
      任何人都可以往 `evidence/` 里丢任意文件而不触发任何信号。
      → 观察项，未修。
  (c) 规格原文（`andyngo build.md.txt` §六速查卡）没有 `resume`/`evolve` 两个入口，
      而判事件表有这两个事件 → R3 只补了 SKILL.md 一侧。**改规格原文需用户裁决。**

--------------------------------------------------------------------------------
【5】本批改动清单（改前 md5 → 改后 md5）

  skills/andyngo/SKILL.md   653570d7ce66e5ff46a1319422073f8c → aa4d141bddca99d72dce980d49835d3b  (64 行 · 就地 2 处)
  skills/hub/routes.md      6226007d1f61c0bfc494f42e7afd7946 → 1bd391b8cf01f981fb7a5ba79117512f  (127 行)
  skills/ 目录              0 个目录/25 文件 → **22 个目录/72 文件**（还原 47 文件 + 跳过 3 + 冲突 1 未覆盖）

  归档（改前原件，均未删）：
    .check/record/evidence/2026-09-16/SKILL.md.pre-r1r3-003500        (653570d7…)
    .backup-skills-pre-restore-20260916-002854/                       (还原前现盘快照 · 25 文件)
    .backup-skills-20260915-2135/                                     (还原源 · 22 目录/51 文件/626,009 字节)

--------------------------------------------------------------------------------
【6】仍未验的
  · 本批改动后的**完整验收**正在跑（本留证先落，数字随后补）
  · `skills/constraint-mode/SKILL.md` 该建还是该改引用 → 未判断完
  · 规格原文 §六是否补 `resume`/`evolve` → 待用户裁决
  · W9 第三轮独立验收（判据 v3.5 + 双向测）→ 已回派，结论未回
================================================================================
