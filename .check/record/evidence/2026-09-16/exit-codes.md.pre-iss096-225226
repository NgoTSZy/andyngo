# 退出码语义登记表（ISS-043 的「分区表」）

**为什么有这张表**：退出码 `2` 在本仓库里承载**两类互不相同的语义**，两套都合法、都有人在用 ——

| 类 | 含义 | 谁用 |
|---|---|---|
| **甲 · 拒跑** | 工具/判据**拒绝运行**：参数错、路径不对、数据源缺失、基线不干净、锁被占。**它不说被测对象坏，它说这一轮没跑成** | `.check/tests/*` · `.check/*.js` 的守卫 · `alloc.sh` |
| **乙 · 结果类** | **跑了，但有未定项**：`DRIFT` / `UNVERIFIED` | `acceptance.js` · `card-entry-coverage.sh` |

**判据不要求统一它们**（统一要重写历史 SEAL 的读法，更贵）；判据只要求**每一处都登记**。
判据：`.check/probe-crossref.js` 的 **X1** —— 凡活工具里含 `exit 2` / `process.exit(2)` 的文件，
**必须出现在本表里**；本表里有、磁盘上已不含 `exit 2` → **悬空登记，rc=2**。

**铁律**：
1. **不得为了让 X1 变绿而往本表里加行** —— 加行的唯一正当理由是「这个文件真的在用 2，且语义没被写下来」。
2. 新增含 `exit 2` 的文件 → X1 会**自动报红**，强制登记。**这是本表存在的意义。**
3. 本表**只登记语义，不改任何文件的退出码**。
4. 一个文件里 `2` 有两套语义时，**必须在本表同一行的备注里写清是哪一行是哪一套**。

| 文件 | `2` = 哪一类 | 语义 | 备注 |
|---|---|---|---|
| `.check/acceptance.js` | **乙 · 结果类** | `DRIFT` 或 `UNVERIFIED` | `:499` 定式 `EXIT = c('FAIL')?1 : (c('DRIFT')||c('UNVERIFIED')?2:0)`；`:56` 的 `exit(2)` 是「0 OK / 0 FAIL / 0 DRIFT / **1 UNVERIFIED**」那一支，**同义**，不是参数错 |
| `.check/check-protocol.js` | **甲 · 拒跑** | 参数错（`:63` `--file` 缺路径 · `:65` 路径不存在）· 基线不干净（`:1402`） | **DEC-031 冻结件**（`4f0aa2d0c63c2d1a46bd3e86cdbeebc6`）—— 登记，不改 |
| `.check/file-history.js` | **甲 · 拒跑** | 同一文件有多个版本块、**歧义无法消解**（`:138`，提示加 `--conv`） | ISS-059 的修法 |
| `.check/mutations-independent.js` | **甲 · 拒跑** | 抽取失败（`:22` 找不到分隔注释 · `:29` `run` 不是函数）· 基线不干净（`:43`） | 电池自带跑手 |
| `.check/probe-crossref.js` | **甲 · 拒跑** | 参数/环境不自洽（本判据自己的出口） | **自指项**：本表由它来判，所以它自己也要登记 |
| `.check/record/alloc.sh` | **甲 · 拒跑** | 参数个数不对 · 正文不是 `@文件` 形式 · 正文文件不存在 · 记录文件不存在 · 正文为空 | **一律「未写入」** —— 拒跑 ≠ 写了一半 |
| `.check/record/tmp/spec-sync-measure.js` | **甲 · 拒跑** | 未给规格路径（`:9` 用法错）—— **只读测量脚本，不写任何东西** | **W23 J12 的临时诊断脚本**（量规格 §3.N ↔ 磁盘的漂移）。**它是被 X1 抓出来的**：写完时未登记，X1 报 HIT 后按本表铁律 2 补登 —— **这一条正是「新增含 `exit 2` 的文件会被强制登记」的一次真实生效**。另见 DEC-034（结论：§3.N 的漂移**不判**） |
| `.check/tests/spec-ref-coverage.js` | **甲 · 拒跑** | 读不到规格（路径/用法错，`:205`）—— **只读判据，不写任何东西** | **W23 J13 的新判据**（规格 §3.N 的每个文件 ↔ 引用点）。**它也是被 X1 抓出来的**：写完时未登记，X1 报 HIT 后按本表铁律 2 补登 —— **这是该铁律第二次真实生效**（第一次是上一行的 `spec-sync-measure.js`）。三态：`0` 全部有引用点 / `1` 有零引用点 / `2` 拒跑。**路径容错做在判据自己身上**（POSIX `/c/...` 自动转 Windows 形式）—— 理由见它自己的文件头：让每个调用方都记得转，是把正确性押在纪律上 |
| `.check/run-battery.js` | **甲 · 拒跑** | 缺参数（`:37` 用法）· 电池没有导出数组（`:39`） | 电池跑手 |
| `.check/tests/andyngo-manifest.js` | **甲 · 拒跑** | 根不对（根下无 `SKILL.md`）—— 「拒绝在错的根上产出一份看起来很全的清单」 | ISS-018 |
| `.check/tests/card-entry-coverage.sh` | **乙 · 结果类** | `VERDICT: UNVERIFIED`（**判据空转，不报 PASS**） | **同一个「2」在这个脚本里是乙类，不是甲类** —— 这正是 ISS-043 要写下来的东西 |
| `.check/tests/i5-bidirectional-test.sh` | **甲 · 拒跑** | 缺数据源 / 临时目录不可用 —— 「拒绝在错的路径上跑出一片假红灯」 | **DEC-031 冻结件**（`5a2eea14e8a80bf89557ecaf146965ff`）—— 登记，不改 |
| `.check/tests/judge-selftest.sh` | **甲 · 拒跑** | 四项参数任一缺失 —— 「拒绝把没跑成伪装成判据坏了」 | ISS-017 |
| `.check/tests/write-lock-guard.sh` | **甲 · 拒跑** | 进不去仓库根（`:43`）· **守卫判定为「有人在写」（`:69`）** | DEC-032：读不出状态也一律拒跑 |
| `.check/tests/write-lock-test.sh` | **甲 · 拒跑** | 与生产共用同一把锁，持锁时守卫拒跑 | ISS-073 的起因 |
| `skills/national-fund-dispatch/scripts/dispatch.js` | **甲 · 拒跑** | 缺任务文本（用法） | — |
| `skills/national-fund-dispatch/scripts/verify-freshness.js` | **甲 · 拒跑** | 缺 `NATIONAL_FUND_ROOT`（`:46`）· 有 N 项**无法校验**（`:100`） | 注意 `:100` 是「我没法校验」不是「校验不通过」 |
| `.check/tests/alloc-idempotent-test.sh` | **甲 · 拒跑** | 进不去仓库根（`cd "$ROOT"`）· 缺 `alloc.sh` 或 `newid.js` | **W23 J14 的新尺子**：`alloc.sh` **幂等追加**的双向测，结 ISS-068 / ISS-069 / ISS-079（沙箱 bypass 重跑 → 10 对逐字相同记录）。**它也是被 X1 抓出来的**：写完时未登记，X1 报 HIT 后按本表铁律 2 补登 —— **这是该铁律第三次真实生效**（前两次是 `spec-sync-measure.js` 与 `spec-ref-coverage.js`）。三态：`0` 全过 / `1` 有断言不符 / `2` 拒跑。**`1` 与 `2` 的分工是本条的重点**：`1` = 尺子跑成了、有断言不符（幂等失效，或门被写成「什么都写不进去」）；`2` = 尺子**根本没跑成**（夹具建不起来 / 被测件不在），**它不说被测对象坏** |

**已知边界（X1 抓不到的，如实写下来）**：
- **用变量退出的文件不进本表**。例：`skills/national-fund-ledger/scripts/verify-ledger.js` 是
  `process.exit(code)`（`:39` / `:44`），**源码里没有字面 `exit 2`** → X1 看不见它，登记它会变成**悬空登记**。
  它的语义靠**自己的头注释**（`:11-14`：`0` = 一致 / `1` = STALE / `2` = NO-SNAPSHOT / `4` = 根目录问题）——
  **那是一个正面例子：三个码各管一件事。**
- **X1 只判「有没有登记」，不判「登记得对不对」** —— 判据不问 A 对不对、B 对不对，只问 A↔B 的关系有没有被写下来。
