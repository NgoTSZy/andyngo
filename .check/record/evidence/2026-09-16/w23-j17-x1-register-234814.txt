===== 留证：W23 J17 · X1 补登（`exit-codes.md` ← `fix-evt-line.js`）· ISS-118 修法收尾 =====
作业      W23 J17
时间      2026-09-16T23:48Z（UTC）· 本机 2026-09-17 07:48（UTC+8）
载体报告  .check/audit/2026-09-16/audit-234152.txt（**rc=0** · 18 段全 `EXIT=0`）
上游留证  w23-j17-iss118-locklog-232300.txt（ISS-118 修法本体）· EVT 056

§0 本留证是什么
  ISS-118 修法（给写权锁加 append-only 日志）实施完之后，**记录层定稿**再跑载体，
  得到 `audit-233302.txt` **rc=1** —— 唯一红灯是 **X1**。本留证记「从红到绿」的完整实证：
  报红原文 → 修法 → 复绿原文 → 载体定稿读数 → **我自己的偏差** → 仍未做的。

§1 X1 报红原文（`audit-233302.txt`，逐字）
  [X1] HIT 退出码语义有一处显式登记（ISS-043）  检查 22 处 · 命中 1
          .check/record/tmp/fix-evt-line.js  含 `exit 2` 但未在 exit-codes.md 登记它的语义
  ---- 豁免（逐条打印；豁免的正当理由只有「记录层 append-only」）----
  （无）
  结论：命中 1 处 · 豁免 0 条 · 悬空 0（豁免 0 + 规则 0）
  EXIT=1

§2 修法（两处改动，都落账；**先跑单项 X1 确认，再跑整轮载体**）
  ① **CHG-219**  `.check/record/tmp/fix-evt-line.js`  **64 → 64 行** · `c1ff45310eddffe2445bb9c3e6b08c50` → `e8c01a225b092b74c97bc4da2d605344`
     头注释 `:2` 自述「带**四**条断言」，而 `:13-18` 实际列 ①–⑤ **五**条 → **改 1 个字符**（四 → 五）。
     **实证（不是自述）**：改前件从 `node .check/file-history.js --get .check/record/tmp/fix-evt-line.js 2 --conv 34b3b3a5`
     取回，md5 `c1ff45310eddffe2445bb9c3e6b08c50` **与基线逐字一致** → `diff` = **1 行删 + 1 行增 · 0 处其它改动**；
     `node --check` 通过；行数 64 → 64（字符数不变，正是「单字符替换」的特征）。
     **为什么此刻改**：该文件在**同一轮**里被登记进正式表，登记表里要写「五条断言」——
     若不同时修掉头注释，等于**我在正式表里写下与文件自述相矛盾的话**。
     **★ 这是判据面外的瑕疵**：没有任何判据会抓「注释里写的条数对不对」→ 本条是**如实记录**，
     **不是**「载体变绿的原因」（变绿的原因是下面 ②）。
  ② **CHG-220**  `.check/record/exit-codes.md`  **49 → 50 行** · `44661545f9d431ded59e2f9370c8d952` → `0423476718f8aaa68c16b2551aa7af04`
     在登记表内追加一行：`.check/record/tmp/fix-evt-line.js` = **甲 · 拒跑**。
     **按本表铁律 4 分行号写清两处**（一个文件里 `2` 有多处时必须写明哪一处是哪一套）：
     `:26` = **缺参数** · `:29` = **行号不是正整数** —— **两处同属甲类「拒跑」**（不是两套语义）。
     **按铁律 1 自查**：加行的唯一正当理由是「这个文件**真的在用 `2`**，且**语义没被写下来**」—— 两条都成立。
     **不是为了让 X1 变绿而加行**：恰恰相反，是**载体 X1 报 HIT 强制我登记**。
     登记行 **21 → 22**。**铁律 3**：本表只登记语义、**不改任何文件的退出码** —— 本轮没动它的退出码。
     **它是该铁律第七次真实生效**（前六次：`spec-sync-measure.js` · `spec-ref-coverage.js` ·
     `alloc-idempotent-test.sh` · `audit-exitcode-test.sh` · `andyngo-audit.sh` · `append-iss.js`）。

§3 X1 复绿原文（**单项实测，先于整轮载体**）
  [X1] OK  退出码语义有一处显式登记（ISS-043）  检查 22 处 · 命中 0
  结论：命中 0 处 · 豁免 0 条 · 悬空 0（豁免 0 + 规则 0）
  PROBE_RC=0

§4 载体定稿读数（**记录全部定稿之后跑的**）
  `.check/audit/2026-09-16/audit-234152.txt` · 363 行 · md5 `60f16b37ad832cfaaa7df4ee4d327df7`
  载体退出码 **0**：全部 **18 段** `EXIT=0`（`grep -cE '^EXIT='` = 18 · `grep -v 'EXIT=0'` = **0**）
  · ACCEPTANCE **12 OK / 0 FAIL / 0 DRIFT / 0 UNVERIFIED**（主检查 PASS 8/FAIL 0 · 内置变异 96/96 ·
    四套电池 13/23/18/29 命中 · 自测 55/10/1 · hub 标价「登记 21 · 磁盘 22 · 缺登记 0」· [98] OK · [99] OK）
  · INTEGRITY **14 OK / 0 FAIL / 0 UNVERIFIED**（I1 字段数=8 **OK** —— EVT 056 修正后已复绿）
  · CROSSREF X1–X6 **全 OK** · RECORD-SHAPE **OK · 三份合规 · 豁免 3 条**（豁免数**未变** → 没靠抬基线蒙过去）
  · RECORD-ID-UNIQUE 未登记撞号 0 · RECORD-WARN-INJECT / HUB-PRICE / SPEC-COVERAGE / SPEC-REF-COVERAGE /
    RECORD-CLI-BIDIR / ALLOC-IDEMPOTENT / AUDIT-EXITCODE-BIDIR 全 `EXIT=0`
  · **ENV-NOT-A-NOOP：仓库 .sh 里「env + 变量赋值」= 0 处**；本机 `env` 仍是**空操作存根**
    （`/c/Users/<user>/.local/bin/env` · `env sh -c 'exit 7'` → rc=0）—— **判据如实报出，不伪装成绿**（ISS-119）
  **X5 的 2 条 STALE 是既存的，不是本轮引入**：`andyngo-integrity.js`（最后记录于 CHG-055）·
  `tests/i5-bidirectional-test.sh`（最后记录于 CHG-056）—— 两件都是 **DEC-031 冻结件**，
  「冻结 ⇒ 永不改动 ⇒ 记录必然过期」→ **STALE 在这里是预期形状**；且 **X5 只报告不判红**。

§5 ★ 我自己的过程偏差（**如实，不藏在末尾**）—— 本轮共 6 处，全部由实测抓出
  ① **同轮并行 Edit 静默丢更新**（已登记 **ISS-120**）：一条消息里对 `write-lock.js` 发 **7 个 Edit**，
     **7 个全返回 `Successfully edited file`**，实际**只落 1 个**（`grep -c ''` = 178，只 +1）。
     丢的含 `logEvent` 的**定义**、落的含它的**调用** → `node --check` **通过**，但 `release` 会 `ReferenceError`
     —— **检查通过、跑起来才炸**。**修**：改为**逐条串行 + 每改一步立刻核验**（实测 +1 / +4 / +23 行逐步确认）。
  ② **本机 `env` 被空操作存根遮蔽**（已登记 **ISS-119**）：我写 `L="env WRITE_LOCK_LOG=… node …"` →
     `$L` **全程空操作且 rc=0** → **锁从没被创建** → 自测第 17 项内层守卫看不见锁 → **递归**
     （实测 **60 个 `bash.exe`**；`TaskStop` 只杀最外层，靠按命令行逐个 `Stop-Process` 才清干净）
     → 首跑 **26/28**、挂住 2m17s。**修**：改用 `export WRITE_LOCK_LOG=…`（**shell 前缀赋值实测有效**）+ 加判据（载体第 13 段）。
  ③ **第二次「看起来是被测件坏了、其实不是」**：改成 export 后首跑 **15/16 FAIL**（`release` 之后锁没被放掉）。
     **修**：**手工复现同一序列完全正常** → 转去查外部 → 发现**上一次被掐断的运行的孤儿进程（60 个）
     仍在抢同一把锁**；清干净后 **28/28**。**教训**：**判据红了先别改被测件，先问「还有谁在动它」**。
  ④ **EVT 056 被我写成 99 字段**（理由写进了 EVT 行）→ 载体 `[I1] 字段数 = 8` **FAIL** → **rc=1**。
     **修**：按先例（CHG-165 / CHG-184 ·「I1 没有豁免机制」）**就地改行**，用带五条断言的脚本实测
     `OK 第 56 行 · 字段 99 → 8 · 字符 918 → 128 · 行数 57 不变`；**同步修正正文文件**（否则 `alloc.sh` 幂等失效）；
     落 CHG-218。**不丢信息**：那段理由原样在 CHG-217 里。
  ⑤ **本留证自己的正文文件第一版把反引号转义成了 `\``（3 处）** —— 与项目其他记录的**裸反引号**写法不一致。
     自查发现时 **CHG-220 尚未落账** → 重写，**零成本**。**这说明「先核验再落账」的顺序是对的**。
  ⑥ **行数口径记忆偏差**：我以为 `changes.md` 是 226 行而 `file-history` 的 v58 是 225 行 ——
     实测 v58 = `9a306d49…` / **225 行**、v59 = `6f5369a4…` / **226 行**（当前），**CHG-218 把 225 → 226**，
     **当前值就是定稿值，无漂移**。**教训同 `MEMORY.md` 的「先看磁盘实况、不信记忆」。**
  ⑦ **时间口径**：系统提示给的 `current_time` 是 **09-17 00:48 GMT+8**，而锁 `since=23:15:16Z` + `age` 推算真实 UTC ≈ **23:39Z**
     —— 两者差约 **7 小时**。**按提示取值会写错 ts**。**修**：一律 `date -u` 实测（`23:48:14Z`），
     并与锁的 `since + age` **交叉核对**（23:15:16 + 1509s = 23:40:25 ✓ 一致）。

§6 仍未做的（**不报完成**）
  ① **ISS-118 仍 `open`**：本轮修的是「让缺口**可见**」，**不是**「让纪律**可靠**」——
     日志**不参与判定** · **仍无「改了但没人持锁」的自动比对判据**（对表要人做）·
     `WRITE_LOCK_LOG` 可覆盖 → **不防篡改** · 第一条持有区间不完整 · 只增不删、**无轮转**。
  ② **ISS-119 环境侧未修**：`~/.local/bin/env` 仍在 PATH 第 11 位（早于 `/usr/bin` 第 14 位）——
     **环境的事，不在仓库可写面内**；仓库侧**已有**判据（载体第 13 段）。
  ③ **ISS-120 未加判据**：它是**工具调用层**的行为（同轮并行 Edit），**不在本仓库判据面内**。
  ④ **载体没有「预期段数」的硬编码断言** —— `18` 是**运行时报出来的**，不是被断言的；
     段数只能靠人工读末行「全部 18 段 `EXIT=0`」确认。**这是本载体自身的已知缺口。**
  ⑤ 仍待裁决（乙类）：ISS-114 最终处置（纯报告 vs 加门禁）· ISS-112 的 9+2 行回填 · ISS-099 · ISS-102 · ISS-035/036。

===== 留证结束 =====
