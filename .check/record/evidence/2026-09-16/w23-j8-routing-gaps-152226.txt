W23 J8 留证 · 路由覆盖矩阵的 3 个洞全部补上（ISS-004 的 R1/R2/R3）
时间：2026-09-16T07:22Z（本地 15:22）· 作业：W23 J8 · 类型：**乙类（动权威文本）+ 用户已授权**
用户原话：`直接协助我解决问题。请问再询问。将问题解决为止。保留两个命令名 + 给「开始」找一行接`

──────────────────────────────────────────────────────────────────────
一 · 这个作业是什么
──────────────────────────────────────────────────────────────────────
`ISS-004 open`【**路由覆盖矩阵有 3 个洞（规格内部不一致，未修）**】：
- **R1** frontmatter 声明触发词「开始」，但判事件表 11 行里**没有任何一行接它**
- **R2** 同一事件两个命令名 —— SKILL.md 判事件表写 `/andyngo task`、规格速查卡写 `/andyngo start <任务>`
- **R3** RESUME 与 EVOLVE **有事件无显式入口**（SKILL.md 与速查卡的 9 个入口均缺 `/andyngo resume`、`/andyngo evolve`）

用户裁决：**取超集** —— **保留两个命令名 + 给「开始」找一行接**（不是二选一）。

──────────────────────────────────────────────────────────────────────
二 · ★ 改前先做的一件事：确认「谁真的缺」
──────────────────────────────────────────────────────────────────────
**读 SKILL.md 后发现 R1 在 SKILL.md 侧早已解决**：
第 19 行已经是 `| 做/建/写 + 新对象；启动/开始 | TASK | A |` —— **「启动/开始」已接上**（W23 J2 修的）。
第 43 行已经是 ``显式：`/andyngo start <任务>` → TASK（`/andyngo task` 同义）`` —— **两个名字已保留**。
第 44 行已含 `/andyngo resume` `/andyngo evolve`。
→ **所以 R1/R2/R3 三个洞都只剩「规格侧」** → **SKILL.md 零改动**（见 §五）。
**这一步省掉了三处无谓改动** —— 「谁缺」必须先实测，不能按 ISS-004 当时的描述直接动手。

──────────────────────────────────────────────────────────────────────
三 · 三处改动（**逐字 + diff 证据**）
──────────────────────────────────────────────────────────────────────
文件：`C:\Users\<user>\Desktop\andyngo build.md.txt`（**用户桌面文件，不在项目内**）
改前 `e31bb2f1bcf29297f4458caf1cec9df3` / 1222 行 → 改后 `e612175054331cf7aad93ca4911e0a90` / **1224** 行

**改动 ①（R1 · 第 156 行）—— 判事件表补「启动/开始」**
```diff
- | 做/建/写 + 新对象 | TASK | A |
+ | 做/建/写 + 新对象；启动/开始 | TASK | A |
```
**逐字与 SKILL.md 第 19 行一致** → 两处判事件表从此同形。
**为什么追加到现有行而不是新增一行**：「判事件表 **11 行**」这个数字被多处引用
（SKILL.md 第 38–41 行的 REJECT 说明、多份 SEAL）；新增一行会把它变成 12 行、**打破那些引用**。
追加到现有行 = **行数不变、语义取超集**。

**改动 ②（R2 · 第 168 行）—— 显式入口保留两个命令名**
```diff
- 显式：`/andyngo task` → TASK
+ 显式：`/andyngo task` / `/andyngo start <任务>` → TASK（**两个名字同义，取超集**）
```
**保留了规格原有的 `task` 在前**（最小改动），**追加** `start <任务>`；
「两个名字同义」与 SKILL.md 的「（`/andyngo task` 同义）」**语义一致**。
**注意：一个名字都没删** —— 这是用户「保留原本的要求」硬约束的直接落地。

**改动 ③（R3 · 第 1195 行后）—— 速查卡插入两个显式入口**
```diff
  收尾          /andyngo seal
+ 继续          /andyngo resume
+ 自改良        /andyngo evolve
  记问题        /andyngo issue <描述>
```
**标签取自规格自己的判事件表「用户的话」列**（`继续/接上次` → RESUME · `自改良` → EVOLVE），
**不是我自己起的名字**。**对齐**：与同表其他行一致（2 汉字 + 10 空格 / 3 汉字 + 8 空格，命令列对齐）。
**纯插入**（diff 记 `1195a1196,1197`，`a` = append，**原行一个字节未动**）。

──────────────────────────────────────────────────────────────────────
四 · diff 全文（**证明只改了这三处**）
──────────────────────────────────────────────────────────────────────
```
156c156
< | 做/建/写 + 新对象 | TASK | A |
---
> | 做/建/写 + 新对象；启动/开始 | TASK | A |
168c168
< 显式：`/andyngo task` → TASK
---
> 显式：`/andyngo task` / `/andyngo start <任务>` → TASK（**两个名字同义，取超集**）
1195a1196,1197
> 继续          /andyngo resume
> 自改良        /andyngo evolve
```
**三处、四行（3 改 + 2 增 − 0 删）** —— 除这三处外**零差异**（diff 无其它 hunk）。

──────────────────────────────────────────────────────────────────────
五 · SKILL.md 零改动（**实测核对**）
──────────────────────────────────────────────────────────────────────
`md5sum skills/andyngo/SKILL.md` → **`f0c9c34a25b3493753c7ce71f45d8eb8`** / **115 行**
= **与 `CHG-101` 记录的值逐字符一致** → 本作业**未碰 SKILL.md 一个字节** ✓
**理由**：R1/R2/R3 在 SKILL.md 侧**本来就已解决**（见 §二），改它属无谓改动。

──────────────────────────────────────────────────────────────────────
六 · **保留原本的要求**（用户三条硬约束之一，逐项核对）
──────────────────────────────────────────────────────────────────────
- **两个命令名都在**：`/andyngo task` **与** `/andyngo start <任务>` —— **一个都没删** ✓
- **「开始」有行接了**：TASK 行接住（与 SKILL.md 同形）✓
- **两个新入口用规格自己的标签**（`继续` / `自改良`），**没有另起名字** ✓
- **原 11 行判事件表**：**行数不变**（只在一行内追加），其余 10 行一字未动 ✓
- **速查卡原 9 行**：**逐字未动**，新 2 行是**插入** ✓
- **SKILL.md / references / 冻结件**：**一个字节未动** ✓
- 结论：**只做加法**（1222 → 1224 行）—— 符合「完整运作的前提下，用最小的改动」。

──────────────────────────────────────────────────────────────────────
七 · 验证
──────────────────────────────────────────────────────────────────────
- **`diff` 归档件 ↔ 现文件** → **只有 3 个 hunk**（见 §四）· rc=1（有差异，符合预期）
- **行数**：1222 → **1224**（+2 = 恰好是插入的两行）
- **重读三处**：`sed -n '156p;168p;1194,1200p'` → 三处内容与对齐**目视确认正确**
- **无残留引用**：`grep -n '9 个入口|9 条|九个入口'` → **0 命中**（改「9 → 11 条」不会留下不一致的说法）
- **归档**：`.check/record/evidence/2026-09-16/andyngo-build.md.txt.pre-w23j8-routing-151619`
  —— 改前 `cp`，**md5 与源文件逐字符一致** `e31bb2f1bcf29297f4458caf1cec9df3`（**先归档后改**）
- **写权锁**：`acquire w23-j8 我声明=25min` → 收尾 `release`
- **记录层判据**：`probe-record-shape` / `andyngo-integrity` / `newid --check` / `probe-crossref --scan`
  —— 规格文件**不在它们的检查面内**（项目外文件），故不受影响；仍全部跑一遍确认**未因本作业变红**

──────────────────────────────────────────────────────────────────────
八 · 残余（如实）
──────────────────────────────────────────────────────────────────────
1. **规格第 168–170 行（3 条摘要）与速查卡（现 11 条）条目数仍不同** ——
   但这是「路由器摘要」vs「一页速查全表」的**结构差异**，不是矛盾；**本作业按最小改动未动它**。
2. **`/andyngo task` 与 `/andyngo start` 的等价性只有文字声明，没有判据** ——
   若将来有人只改一处，两处会再次发散（**本作业补的是内容，不是防复发机制**）。
3. **规格文件在项目外**，`file-history` / 冻结件表 / 判据检查面**都不覆盖它** ——
   它**没有自动留版本**，本次能回溯**全靠本作业手工 `cp` 归档**。**这是本作业暴露的一个结构性缺口。**
4. `ISS-004` 原文说「3 个洞」，本作业**在规格侧补齐**；SKILL.md 侧早已齐（§二）—— 但
   **没有判据能自动发现「两处入口表条目数不一致」**（同第 2 条）。

──────────────────────────────────────────────────────────────────────
九 · 仍不能验的
──────────────────────────────────────────────────────────────────────
1. **`routing-coverage.js` 未重跑** —— ISS-004 的判据是
   `node <temp>/routing-coverage.js skills/andyngo/SKILL.md "andyngo build.md.txt"`，
   而那个脚本在**临时目录**（`<temp>`），**本作业未找到它** → **未能用原判据复验**。
   本作业的验证是 **diff + 行数 + 目视 + 与 SKILL.md 逐字对照**，**不是原判据**。
2. **「用户说『开始』时真能路由到 TASK」未实测** —— 需要一次真的 `/andyngo 开始 …` 调用。
3. **规格文件没有版本管理**（残余第 3 条）→ 下次改它仍要手工归档。
