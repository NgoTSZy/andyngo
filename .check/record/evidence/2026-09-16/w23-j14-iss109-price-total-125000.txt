W23 J14 · ISS-109 留证 —— `price.js --check` 补上「表头声明合计」的对照
================================================================================
时间：2026-09-16T12:49Z（UTC）· 载体报告：`.check/audit/2026-09-16/audit-124341.txt`
开工前锁：free（`write-lock.js status`）· 写锁：`W23-J14-ISS109` 我声明 60min
冻结件 3/3 逐字符一致（开工前 + 收尾各一次）：
  `.check/andyngo-integrity.js`   e8d1386e91e983c4961f5796a2486558
  `.check/check-protocol.js`      4f0aa2d0c63c2d1a46bd3e86cdbeebc6
  `.check/tests/i5-bidirectional-test.sh` 5a2eea14e8a80bf89557ecaf146965ff

§0 问题（ISS-109）
--------------------------------------------------------------------------------
`routes.md:6` 声明「**21 本正文合计**（表内逐行相加）：主文件 **51,226** · 含 refs **139,508**」。
`price.js --check` 的 `want` Map 只由 `RE1`/`RE2` 从**表格行**抽取 → **表头那句从未进入比对**。
实测（ISS-109 首现时）：声明停在 `51,200 / 138,265`，表内逐行相加已是 `51,226 / 138,361`
（差 26 / 96 = 落后一次 `andyngo` 行的更新），而 `--check` 报「数值不一致 0」**全绿**。

§1 前提验证（改之前，先确认判据有立足点）
--------------------------------------------------------------------------------
`node -e` 用同两个正则解析真 `routes.md`：
  re1 命中 14 行 · re2 命中 7 行 = **21 行** · 去重后 `want.size` = **21**（**无重名覆盖**）
  逐行相加 main = **51226** · all = **139508**  ↔  声明 51,226 / 139,508  →  **差 0 / 0**

§2 改前归档（归档在前）
--------------------------------------------------------------------------------
`.check/record/evidence/2026-09-16/price.js.pre-iss109-204021`
  md5 `dca5ce8feb6ebdeb5449d066067952ee` / **138 行** · `cmp` 与改前 `skills/hub/price.js` 逐字节一致

§3 尺子（先给尺子再改 —— `price.js` 此前一条双向测都没有）
--------------------------------------------------------------------------------
新增 `--inject`：**11 条双向向量**（A 解析 4 · B 对照 4 · C 假红防线 2 · D 接线 1）
```
=== price.js 自测（--inject · 双向向量）===
  ok   A1 真格式（带千分位逗号）
  ok   A2 无千分位逗号 / 无粗体尾注
  ok   A3 声明行整个缺失 → 必须显式 FAIL（不得静默通过）
  ok   A4 只写主文件、缺「含 refs」→ 不半解析，报 FAIL
  ok   B1 一致 → 不报
  ok   B2 ★ISS-109 实测形状：声明落后一行（表内 51226/139508，声明 51200/138265）→ 报
  ok   B3 声明超前 → 报
  ok   B4 本数不符（声明 2 本、表内 1 行）→ 报
  ok   C1 表格行带粗体 + 尾随注释（`**800** ⚠ 最贵`）→ 仍解得出数字
  ok   C2 表格行是裸数字（无粗体）→ 仍解得出数字
  ok   D1 真 routes.md 走同一条管线 → 解出声明行且表内非空（一致性归 `--check` 管）
  11 过 / 0 不过
```
**第一版尺子自己红过一条**：A1 声明「1 本」却给了 2 行 → 判据报「表头声明 1 本 ≠ 表内 2 行」。
**向量自己写错、判据报得对**，修正后 11/11。（本项目「尺子自己也会红」的第 3 次。）

§4 反向验证（拿改后的判据跑真树副本，5 个向量）
--------------------------------------------------------------------------------
做法：`cp -r skills $T/skills` → 扰动 `$T/skills/hub/routes.md` → `node $TN/skills/hub/price.js --check`
（**`$TN` 必须 `cygpath -m` 转换** —— 见 §7）
```
① 控制（未扰动）                      → rc=0 · 「表头合计 51226/139508 ↔ 表内逐行相加 51226/139508 一致（21 行）」
② 合计退回 ISS-109 实测陈旧值 51200/138265 → rc=1 · 「表头合计 51200/138265 ≠ 表内逐行相加 51226/139508（差 26 / 1243）」
③ 整行删掉（缺声明）                   → rc=1 · 「表头**找不到「N 本正文合计」那一行** —— 合计必须有声明，缺了判据就是瞎的（ISS-109）」
④ 只改表内一行数字（3,125 → 3,126）、合计不动 → rc=1 · **两条判据同时报红**（逐行 + 合计），数值不一致 = 2
⑤ 删掉一条表行（national-fund-dispatch）、合计不动 → rc=1 · 「表头声明 21 本 ≠ 表内 20 行」+ 合计差 -3125/-19056
```
④ 是 ISS-109 的**真实形状**（改了行、没改合计）—— 改前它全绿，改后它报红。

§5 端到端
--------------------------------------------------------------------------------
真树：`node skills/hub/price.js --check` → **rc=0**，输出新增一行
  `表头合计 51226/139508 ↔ 表内逐行相加 51226/139508 一致（21 行）`
载体：`.check/audit/2026-09-16/audit-124341.txt` → **11 段全部 EXIT=0**（HUB-PRICE 段两条：`--check` + `--inject`）
`acceptance.js` 第 [10] 项**未改一行就自动抓到** —— 因为合计不符计入**同一个 `bad` 计数器**
  （反向实测：`数值不一致 1` → rc=1 → 第 [10] 项 FAIL）

§6 口径（判据不混比，写进注释与文档）
--------------------------------------------------------------------------------
只比「表头声明 ↔ 表内逐行相加」（两个都是**表内量**）。
**不引用**脚本自报的 22 本 `round(Σ)` —— 脚本合计 = `round(Σxᵢ)`、表里每格 = `round(xᵢ)`，
舍入位置不同，混比就是**假红**。`routes.md:12-16` 已明文禁止引用那个数。
`parseTable` 求和**按「行」不按「去重后的名字」** —— 用去重 Map 会把「同一本被写进两节」静默吃掉。

§7 本轮踩到的形状（与 ISS-110 同族，**发生在我的临时命令里**）
--------------------------------------------------------------------------------
反向验证第一版：`node "$T/skills/hub/price.js"` → `Error: Cannot find module 'C:\tmp\tmp.…'`
**bash 认 POSIX `/tmp/...`，原生 `node.exe` 不认** —— 与 ISS-110 / DEC-038 **完全同一形状**。
修：`TN=$(cygpath -m "$T")` 后再传给 node。
**附带核查（只读）**：`grep -rn 'node ' --include='*.sh' skills .check` 逐条看过 ——
树内**0 处** DEC-038 形状的活实例（`andyngo-record.sh` 的 `win()` 定义在 43 行、用于 44/49 行、调用在 54 行，顺序正确）；
`andyngo-audit.sh:216` 的 `node … "$SPEC"` **不带 `win`** 是**故意的**，其自述（`:209-210`）说「路径容错做在判据自己身上」
—— 已核实为真（`spec-ref-coverage.js:97` 有 `toWin()`、`:179` 有它的向量、`:196` 实际应用）。
**所以本轮那次只发生在我的临时命令里，不是树内的缺陷。**

§8 顺带发现（未修，已登记）
--------------------------------------------------------------------------------
**ISS-112**：`record-shape.js:57` 的 `min`（字段数**下限**）只抓「整段被吃掉」，
抓不住「**前四个声明字段被吃掉**」—— 实测 `ISS-105/106/107/109` 四行字段 2 直接是 `【**`。
本轮只补了 **ISS-109 自己的行**（本任务必要动作）；另三行未补，因为缺的「严重度」是**作者的分类判断**（乙类），
不能从文本机械推出 —— 补齐等于替作者发明元数据。**顺序必须是「先定值、再加判据」。**

**ISS-113**：`alloc.sh` 的**分隔符**参数传错（ISS 应为 `-`，我传了空格）→ 取号器按 `^ISS ` 扫已有号、
**一条都扫不到** → 从 **001** 重开，落盘一行 `ISS 001 …`（前缀与号之间是空格）。
撞号检查**响了**（`WARN … ISS-001×2`），但**是 WARN、写已落盘**。
处置：① 坏行取证 `iss112-wrong-sep-badline-125239.txt`（3313 字节）
      ② 坏行移除（`issues.md` 126 → 125 行，末行回 `ISS-111`，`^ISS-001 ` 计数回 1）
      ③ 带坏行的整份文件归档 `issues.md.with-badline-125239`
      ④ 用正确分隔符重跑 → `ALLOC ISS-112`，**无 WARN**

§9 记录终态
--------------------------------------------------------------------------------
```
issues.md      125 → 127 行  ead65a8ed2dab3995f08da96f0dd03ce   （ISS-109→resolved · +ISS-112 · +ISS-113）
decisions.md    50 →  51 行  892b3b75c10dbdb6b540869ac6870820   （+DEC-040）
changes.md     156 → 161 行  （+CHG-149…153）
eventlog        40 →  41 行  （+EVT 041）
```
改动件指纹：
```
skills/hub/price.js                      138 → 284 行  dca5ce8feb6ebdeb5449d066067952ee → 461b1bd9b0d0e18506f0068e6c17b916
skills/hub/routes.md                     167 → 169 行  bf209dd6dbde975524b189d31c9f6b10 → 31bdd63a9e4a75b6bab34515e53d31ee
skills/andyngo/scripts/andyngo-audit.sh  250 → 258 行  88c8852866cd0ff8a6a2ebd2235c58b7 → 213c5cf74d3882c4634fee7427b7ca2a
```

§10 保留（规格 §验证 3/5 测的就是它们）
--------------------------------------------------------------------------------
`price.js` 的 CLI 形态（`--check` / `--desc` / 无参数）· 输出行 `登记 N 本 · 磁盘 M 本 · 数值不一致 bad · 缺登记 miss`
· `rc = bad || miss ? 1 : 0` · `hub` 豁免（路由入口本身不在自己的表里登记）· `walk` 只收 `.md`（`.sh` 不计价）
—— **一字未改**。`routes.md` 的口径声明段（`:12-16`）**一字未改**（它们是判据「不混比」的依据）。
