W23 J18 · 留证：判据自己的维护说明方向写反了（ISS-145）
========================================================================
时间：2026-09-17T12:54Z ~ 12:56Z（UTC）· 写锁 W23-J18-ISS123（since 12:46:47Z · pid 9504）

一、发现路径（不是专门去找的）
------------------------------------------------------------------------
收尾时核对 `crossref-baseline.json` —— 因为 `probe-crossref.js --baseline` 写进文件里的
`_how_to_update` 说「**每写一条新 CHG 都要重跑 `--baseline`**，否则新行会落进「历史」而不受判」，
而本轮刚写了 CHG-339/340 → 按说明应该重跑。重跑前先读 `x4()` 的判法本体 —— 方向对不上。

二、实测（逐条可复核）
------------------------------------------------------------------------
| 项 | 值 | 怎么取的 |
|---|---|---|
| `x4()` 判法本体 | `if (id > maxId) { seen++; … } else if (!ok) histBad++;` | 读 `.check/probe-crossref.js:297-318` |
| 当前 `changesMaxId` | `CHG-116` | `cat .check/record/crossref-baseline.json` |
| X4 **实际受判**行数 | **224 处** | `node .check/probe-crossref.js --report` → `[X4] … 检查 224 处 · 命中 0` |
| 历史行不合规数 | 10 条（`≤ CHG-116`） | 同上（append-only，不判红） |
| 若照说明重跑 `--baseline` | `changesMaxId` → `CHG-340` ⇒ 受判面 **224 → 0** | 由判法本体直接推出 |

⇒ `_how_to_update` 里「否则新行会落进『历史』而不受判」**因果写反**：
**不重跑**才让新行受判，**重跑**才把存量移出判据面。

三、危害的形状与当前实害
------------------------------------------------------------------------
- **形状**：它是**判据自己的**维护说明。读者会以为照做是「让新行受判」的**必要动作**，
  实际是在**静默缩小判据面**（同族：ISS-074 的「白名单不能变成把判据弄瞎的入口」·
  ISS-105 的「一个不会响的守门人比没有守门人更坏」）。
- **当前实害 = 0**：`changesMaxId` 自 `CHG-116` 起**从未被抬过**（读数即证），
  所以现状落在**更严**的那一侧（224 行全在判据面内）。

四、同一份文件里的第二件事（随本条一起裁决）
------------------------------------------------------------------------
`crossref-baseline.json` 的 `evidence` 表**只有 `2026-09-15` / `2026-09-16`** ⇒
X3「evidence 文件数只增不减」的扫描面**盖不到 `2026-09-17`**（当日 evidence 被删不会报红）。
根因与上一条相同（基线自 `CHG-116` 起未抬）。
★ **两件事绑在同一条命令里**（抬基线同时补 `2026-09-17`、同时把 224 行移出 X4 判据面）——
**这正是本条需要裁决、而不该由执行者单方面定的原因**。

五、本轮**未做**的事（如实记）
------------------------------------------------------------------------
1. `probe-crossref.js` **一个字节都没动**（虽不在冻结三件之列）。
2. `crossref-baseline.json` **未重跑、未修改**（现状更严，且抬基线会缩小判据面）。
3. 建议修法：**改说明文字、不改判法** —— 把 `_how_to_update` 改成如实描述这个取舍
   （「基线抬升会让其下的行**不再受判**（存量豁免）；本仓库的实际口径是**不重跑**」）。

六、本轮由此踩到的一个真实教训（记下来）
------------------------------------------------------------------------
我把 EVT 022 与 EVT 021 **指向同一个留证文件**，被 `andyngo-integrity.js` 的
**I5「事件行 ↔ 作业留证 双向单射」** 当场抓住：
`FAIL D2 被重复引用: w23-j18-iss099-123-136-close.txt ｜ A=21 B=22 豁免=0`。
⇒ **一条事件 = 一份留证**，不能两件事共用一份。修法：为本条单独建这份留证，EVT 022 改指它
（`fix-evt-line.js`，断言行数不变 + 恰好 8 字段）。
