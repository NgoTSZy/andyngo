# 留证 · W23 J18 · 一次**负结果**：我去找缺陷，发现守门人早就在那儿

**日期**：2026-09-17 · **作业**：W23 J18（作业 17）
**落账**：ISS-144（新增 · `medium` · `open`）· CHG-338（`issues.md`）· EVT 020
**写锁**：`W23-J18-ISS144`（45 min · `since=2026-09-17T11:03:59Z`）

> **本轮没有造任何判据、没有改任何源文件。** 唯一的写盘是这条记录。
> 这不是「没干活」—— **它是本轮唯一正确的产出**（理由见 §4）。

---

## 1. 出发点

上一轮（作业 16）留下的边界：**全仓注释里的「文件:行号」引用**只修了一处文件。
按长期指令 `丙类 · 根本不该由人判：应该由判据判的 → 去加判据，不是去问人`，
我扫了 31 条 `open`，逐条否决后选中「**记录层跨行号复用**」——**因为它机械可判**。

## 2. 测量（这一步是真的，读数是新的）

```
issues.md   : 146 行 / 143 唯一号  → ISS-035 ×2 · ISS-036 ×2 · ISS-037 ×2
changes.md  : 337 行 / 337 唯一号  → 干净
decisions.md:  43 行 /  41 唯一号  → DEC-021 ×2 · DEC-022 ×2
eventlog/*  : 12/12 · 57/57 · 19/19 → 干净
```

**5 对重复号。** 而现有判据只查**同一行内**的号重复（`ISS-082 ISS-082 …`，规则 ④）。
⇒ 我当时写下的结论是：**「跨行号复用没有判据守着」**。

**那个结论是错的。**

## 3. 证伪它只用了一条命令

`ISS-042`（`resolved`）逐字写着：

> **原号 ISS-035**（01:16Z 写入那条），因 01:15Z 的 W15 J1/J2 已占用 035/036/037 …
> → **撞号以「被冻结件源码引用者」优先**，本自动化三条改号：**035→042、036→043、037→044**（原三行不删）。

`ISS-047` 指向了工具与豁免表。实测：

```
$ node .check/record/newid.js --check
EXEMPT issues.md : ISS-035×2  ← 已逐条登记
EXEMPT issues.md : ISS-036×2  ← 已逐条登记
EXEMPT issues.md : ISS-037×2  ← 已逐条登记
EXEMPT decisions.md : DEC-021×2  ← 已逐条登记
EXEMPT decisions.md : DEC-022×2  ← 已逐条登记
OK     changes.md : 无重复号        （eventlog 三份同）
结论: 未登记撞号 0 · 已登记历史撞号 5 · 悬空豁免 0 · 豁免表 5 条
→ exit 0
```

而且它**早已接进载体**：`skills/andy/scripts/andy-audit.sh:152` 的 **`RECORD-ID-UNIQUE`** 段，
本轮载体实测该段 **`EXIT=0`**；连它的尺子也在（`:169` `RECORD-WARN-INJECT`）。

⇒ **机制存在 · 已接进载体 · 已跑绿 · 有逐条豁免表 · 有悬空豁免检测。**
**我差点为一件已经解决的事造第二条判据。**

## 4. ★ 为什么「什么都没做」是这一轮正确的产出

`ISS-098`（`resolved` · 2026-09-16T10:00Z · W23 J12）逐字写着：

> **我造了一条重复判据，已逐字节回退**（过程登记，不省略）。
> …新规则**跑绿了**（`--inject` 注入 6/6 · 反向 6/6 · 边界 1/1 · 文件级 6/6 · 计数 1/1），
> **反向验证也过了** … **然后发现它是重复判据** …
> **处置**：三个文件**逐字节回退** … **净变更为零，故不记 CHG**。
> **根因：「读到过」不等于「问过」**

`DEC-035` 因此立规：

> **加判据前必须先回答一个问题：「这条判据有没有同源兄弟？」**
> … **「加判据前先查有没有同源兄弟」成为一条开工前置检查**
> （与 automation §0.4 的冻结件 md5 核对并列）

**本轮 = ISS-098 的完整重演。** 两次的差别**只在运气**：

| | ISS-098（2026-09-16） | 本轮（2026-09-17） |
|---|---|---|
| 何时发现 | **写完之后**（跑绿 + 反向也过） | **动手之前** |
| 代价 | **逐字节回退三个文件** | **一条命令** |
| 净变更 | 零 | 只加这条记录 |

⇒ **`DEC-035` 那条「开工前置检查」没有被执行，也没有任何东西会提醒你执行它。**
它住在 `decisions.md` 里，而 **`decisions.md` 不是每次开工都会读的东西**
（本会话从头到尾**没读过它**——这就是它能被完整重演的原因）。

## 5. 临时替代（本轮已做，也是唯一写盘之外的动作）

把**开工预检**写进 `MEMORY.md` 的「判据的纪律」段 —— 那是**每次会话都进上下文**的地方，
不像 `decisions.md` 要主动去读。给出**可复制的两条命令**：

```bash
# ① 载体段名 = 判据主题索引（本轮实测 22 个）
grep -ohE '=== [A-Z0-9-]+ ' skills/andy/scripts/andy-audit.sh | sed 's/=== //;s/ $//' | sort -u
# ② 尺子文件名即主题
ls .check/tests/
```

本轮实测段名（22 个）：

```
ACCEPTANCE  ALLOC-IDEMPOTENT  ANDY-ACCEPTANCE-6  ANDY-DREAM  ANDY-FMT
AUDIT-EXITCODE-BIDIR  BATTERY-RELATIVE-E2E  BATTERY-RELATIVE-JUDGE  CROSSREF
ENV-NOT-A-NOOP  HUB-PRICE  INTEGRITY  JUDGE-SELFTEST  RECORD-CLI-BIDIR
RECORD-ID-UNIQUE  RECORD-SHAPE  RECORD-WARN-INJECT  RULER-CALLPOINT
SPEC-COVERAGE  SPEC-REF-COVERAGE  WRITE-LOCK-SELFTEST  X1-FRESHNESS
```

**两条命令就能回答「这条判据有没有同源兄弟」。**

## 6. 顺带量到的一条计数口径（本轮我据此误判过一次）

`open` **31 条**里有 **2 条是「占位行」**：

| 行 | 行首 | 是什么 |
|---|---|---|
| 53 | `ISS-035 open` | `EX-ID-001` 登记的**占位 B**，内容已由 **ISS-042**（`resolved`）承载 |
| 54 | `ISS-036 open` | `EX-ID-002` 登记的**占位 B**，内容已由 **ISS-043**（`resolved`）承载 |

**它们保持 `open` 是 `DEC-026`「不删行、不改行、不重编号」的刻意后果，不是漏关。**
⇒ **活的 `open` = 29。** 下次给 open 清单做三分类时**先扣掉占位行**。

> ★ 我原本这一轮的计划是「**关掉 ISS-035/036**」—— 查完才发现，
> 我要关的正是这两条**占位行**，而**它们按规矩不许改**。
> **又一次「先读原文」救了我。**

## 7. 边界（不报完成）

1. **`DEC-035` 的前置检查仍然是文本** —— 本轮只把它搬进了「每次都读」的 `MEMORY.md`，
   **没有机制强制它**（一个人不读 `MEMORY.md` 就还是不会做）。
   ⇒ ISS-144 保持 `open`，临时替代已写明。
2. **`newid.js --check` 覆盖的只有 5 个文件**（`issues/changes/decisions` + 三份 eventlog）
   —— 别的记录面（如 `evidence/` 的文件名、`archive/`）不在它的扫描面内。**未量**。
3. 本轮**没有**对 `open` 清单里其余 27 条做任何处置。
4. **我这一轮没造判据，也没有「顺手」改任何东西** —— 这是刻意的（见 §4）。
