=== 留证 · W23 J18 · ISS-074（EVT 的 wave/job 形状 → 判据守着）===
时间：2026-09-17T09:39Z
写者：B（会话 W23-J18-ISS074 · 写权锁 45min）
落账：CHG-327（.check/tests/evt-field-shape.js · 新增）· CHG-328（skills/andy/scripts/andy-audit.sh）·
      CHG-329（.check/record/exit-codes.md）· EVT 016

【1】起点：ISS-074 说「EVT 的 `wave`/`job` 格式在漂移，而**没有任何判据覆盖它**」
现场：`eventlog/2026-09-16.txt` 里 EVT 001–018 写 `W11 J1`，而 **EVT 019/020 写 `22 1` / `22 2`**（丢了前缀）。
I1–I7 只查「字段数 / seq 连续 / 路径存在 / ts 单调 / 双向单射 / status 合法」——
**形状没有任何判据** ⇒ 这次漂移**全绿通过**。
★ 而 ISS-074 **自己规定了做法**：「**先扫一遍看实际有几种形状，再决定**」——
  同时明确「**本轮不修**」（理由：先写只报告不判红的探针、**实测误伤面**再定，同 ISS-050 的教训）。

【2】按它自己的做法**先实测**（这一步是它要求的，不是我自己加的）
`grep -oE '^EVT [0-9]+ [^ ]+ [^ ]+ [^ ]+' … | awk '{print $4" "$5}' | sed -E 's/[0-9]+/N/g' | sort | uniq -c`
⇒ **全量 84 条：`WN JN` × 82 · `N N` × 2**
⇒ **误伤面 = 精确 2 行**，且都是 `2026-09-16.txt` 的 EVT 019/020（**append-only 的历史行**）
⇒ 结论：**可以判红**（白名单 2 条覆盖历史行即可，不需要退化成「只报告不判红」）。

【3】新判据 `.check/tests/evt-field-shape.js`（**新增** · 147 行 · `0e9bec4a…` · CHG-327）
  **R1** 每条 EVT 行的第 4 字段 `^W[0-9]+$` · 第 5 字段 `^J[0-9]+$`
  **R2** **多余豁免**：白名单条目若**已合规**或**不存在** → 报红（**该删**）
       ★ 没有 R2，白名单就是「**把判据弄瞎**」的入口（同族：`ruler-callpoint.js` 的 R4）。
  三态 `rc=2`：找不到 eventlog · **或一条 EVT 都解析不出**（防「**空集 PASS**」的假绿，
       同族 `card-entry-coverage.sh` 的空判据守门人）。
  `--inject` **4/4**：R1（新增 `22 9`）· **R2**（把已合规的行塞进白名单）· 正例（新增 `W99 J9`）·
       **假红守门人**（改一条合规行的 ts —— 无关变异不得报红）。
  ★ **基线断言**：`--inject` 前先判「基线必须全绿」，否则 `rc=2` 拒跑（而不是报假红）。
  ★ **副本验证**：清空白名单 → **真实数据精确报出那 2 行**（`EVT 019 → 「22 1」` · `EVT 020 → 「22 2」`）
       ⇒ 证明它**对着真实数据会响**，不只是注入夹具会响。

【4】★★ 首次跑载体：**rc=1 · 2 段非零** —— 而根因**是我自己漏了一步**
两段：
  ① `[X1] HIT … 命中 1 处` —— `.check/tests/artifacts-index.js` 含 `exit 2` 但**未在 `exit-codes.md` 登记语义**
     （另有 2 个 `FRESH`：本轮与上一轮新写的 `hardcoded-count.js` / `evt-field-shape.js`，同样未登记）
  ② `x1-freshness-test.sh` 的**双向测 2/5** —— **同一个根因**（真实数据有缺口 → 整体 rc≠0 → ③④ 断言不符）
★ 这是 MEMORY 里的「**铁律**」（新增含 `exit 2` 的文件必须登记语义）—— **我加判据时漏了**。
★ 而 **X1 替我抓到了**：它的「新鲜未判」机制（**ISS-127**）把两个刚写的文件标成 `FRESH`、
  把已定稿的 `artifacts-index.js` 标成 `HIT` —— **一个判据抓到了另一个判据的漏登记**。

【5】补登（CHG-329）
`.check/record/exit-codes.md` **61 → 64 行** · `c02e8028…` → `4313e48d…`
按**铁律 2** 补登 3 条（**第十/十一/十二次真实生效**）：
  `artifacts-index.js`（`:72`）· `hardcoded-count.js`（`:77`）· `evt-field-shape.js`（`:88`）—— 全为**甲 · 拒跑**。
★ **不是「为了让 X1 变绿而加行」**（铁律 1）：三个文件都**真的在用 `process.exit(2)`**（grep 逐条确认），
  语义**确实没写下来**。
★ **改前 md5 取自文件历史 v16**（`--get … 16 --conv 34b3b3a5`）—— **核对 md5 逐字相符**（`c02e8028…`）才采用
  （ISS-059 的纪律：多块要 `--conv`；归档后必须核 md5）。

【6】修复后的复跑（**判据级**）
  `node .check/probe-crossref.js` → **rc=0** · `[X1] OK 检查 37 处 · 命中 0`
  `bash .check/tests/x1-freshness-test.sh` → **rc=0** · **5/5 条符合期望**

【7】接进载体（CHG-328）
载体 **645 → 662 行** · `e300ed6c…` → `84853c44…` · **段位总数 27 → 28**（三处同步）。
插入用**一次原子 Edit + 跨多行锚点**，插完**读回核对层级**（新段在 `fi` 之后、`} >> "$REPORT"`(633) 之内）。
★ **`ruler-callpoint.js` 这次没报红**（我提前接好了）—— **20 → 21 文件全接**。
★ 新段首次跑即 **`EXIT=0`**（84 条 EVT · 豁免 2 条**全部真的被用到**）。

【8】实测读数
验收：**rc=0 · 12 OK / 0 FAIL / 0 DRIFT / 0 UNVERIFIED** · 冻结件 **3/3 未动**。
载体（**修复前**那次）：`rc=1` · 段位总数 **28（24 跑 · 4 未跑成）** · 4 段 skip = 3 段 `ANDYNGO_SPEC` 环境性
  + 1 段 `write-lock-test.sh 拒跑（有人正持锁）`。
★ **修复后载体的复跑读数见日志补记**（本轮先落账、后复跑，故此处不含）。

【9】仍然未做的（**不报完成**）
① 判据只判**形状**（有没有 `W`/`J` 前缀 + 数字），**不判**「wave/job 与作业实际对不对」。
② 扫描面 = `eventlog/*.txt`；**别处的 EVT 它看不见**。
③ 白名单只有 2 条，且由 **R2 反查** —— 但它仍是**人工维护**的：新出现的合法例外要有人去加。
④ 载体 `andyngo-audit.sh`（**v4.x**）**不扩面** ⇒ 那条路径上这个判据**仍然没接**。
⑤ 载体这一轮**没在无锁窗口跑** ⇒ `WRITE-LOCK-SELFTEST` 段 `SKIP`（**没跑成 ≠ 通过**）。

=== 留证结束 ===
