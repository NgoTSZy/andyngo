w23 · j18 · ISS-138 —— R1 的「注释盲」：注释冒充调用点
生成 2026-09-17T06:57Z · 会话 W23 · 作业 J18 · 写锁 W23-J18-ISS138（60min）
====================================================================

一、现场（判据自己揭出来的，不是我想到的）
--------------------------------------------------------------------
ISS-135 建 `ruler-callpoint.js` 时，R1 只排除了 `.md`（「文档里提到 ≠ 执行」），
**没排除 `.js` / `.sh` 的注释**。实测（本轮修好之前）：

  x1-freshness-test.sh            接  2 处  ← .check/battery-relative.js · skills/andy/scripts/andy-audit.sh

其中 `.check/battery-relative.js` 那一个「调用点」是**一行注释**
（`battery-relative.js` 的注释里提到了 `x1-freshness-test.sh`）。

★ **那一轮「18 个文件全接」的结论碰巧是对的** —— 不是因为它判对了。
  ⇒ 这**不是假绿事故**，是一个**还没响的洞**（ISS-138 原文的判断，本轮被证实）。

二、修法：一份口径两处用
--------------------------------------------------------------------
* 新建 **`.check/strip-comments.js`**（32 行）—— 把 `probe-crossref.js` 的局部函数
  `stripComments(src, isSh)` **搬**出来，两个判据 `require` 同一份。
  理由：**复制会静默分叉**（`fileset.js` 文件头记的实测代价）；先例 `newid.js` 的
  `flattenBody()` 抽出来给 `alloc.sh` 用（ISS-125）。
* `ruler-callpoint.js` 的 R1：`if (t.includes(name))` → `if (stripComments(t, /\.sh$/.test(e.name)).includes(name))`
* `probe-crossref.js`：本地函数 → `require('./strip-comments.js')`（逻辑一字未改）
* `--inject` 6 → **9** 条：⑦ 整行 shell 注释 → 仍必红 · ⑧ `.js` 注释（块+行）→ 仍必红 ·
  ⑨ **行尾注释的真实调用 → 必须 rc=0**（不许过度剥）

★ **故意不剥字符串字面量**：`bash -c "…x.js"` 是**真执行**，剥字符串会造**假红**
  （假红比逃漏贵：它会训练人忽略红色）。残余风险（`echo ".check/tests/x.sh"` 这类
  字符串里的假调用点仍会被算上）如实写在 `strip-comments.js` 文件头。

三、修好之后，真盘扫描**当场从绿变红**（这是本轮最值钱的一条）
--------------------------------------------------------------------
  judge-selftest.sh             **未接**（0 个可执行调用点）
  write-lock-test.sh            **未接**（0 个可执行调用点）

逐条核实（不是凭判据一句话下结论）：
  `write-lock-test.sh` 的全部提及：`write-lock-guard.sh:5,38`（注释）·
    `write-lock.js:188`（注释）· `andy-audit.sh:363` / `andyngo-audit.sh:363`（注释）·
    自身第 99 行（按设计排除）· `MEMORY.md:58`（.md，不在扫描面）
  `judge-selftest.sh` 的全部提及：`spec-ref-coverage.js:100`（注释）·
    `andy-audit.sh:205/214/236/238` / andyngo 同（注释）· 自身第 3 行（排除）·
    `.check/audit/`（跳过）
  另：载体是**逐个按名字显式调用**（无通配符）⇒ R1 的「调用点」口径正确。

⇒ 两把尺子**确实从未运行**。这正是 ISS-134 那一类缺陷，被 R1 的「注释盲」藏住了。
⇒ 处置 = **接进载体**（第 21 / 22 段 · 段位总数 **23 → 25**）。理由：
  载体第 202–215 行已为**同一个形状**（`card-entry-coverage.sh` 的「有判定力却没人跑」）
  做过同样的选择；豁免表纪律①明写「**不得为了让检查变绿而新增豁免**」。
⇒ 已开 **ISS-139**（resolved）记录这件事。

四、两处 `rc=2` 都映射成 `SKIP`（不是 `EXIT=2`）
--------------------------------------------------------------------
* `write-lock-test.sh` 在**有人持锁**时拒跑（rc=2）。实测（我持有 W23-J18-ISS138 时）：
    write-lock 自测：拒跑（rc=2，**未动任何文件**）
  本载体的正常工作流就是「取锁 → 改 → 跑载体 → 落账 → 放锁」，即**绝大多数时候锁是 held 的**；
  若把 rc=2 记成 `seg`，这一段会**每次都亮 `EXIT=2`** —— 而**总是响的警报等于没有警报**。
* `judge-selftest.sh` 的规格（`andyngo build.md.txt`）在项目外，本机没有 ⇒ rc=2 拒跑。
  同 SPEC-COVERAGE 段的既有理由：**不是每台机器都有那份规格**，找不到就 SKIP、不报 FAIL。

五、我自己引入的一个口径分叉（同轮修掉）
--------------------------------------------------------------------
内层自测会打一行**行首 `SKIP `**（来自 `write-lock-guard.sh:65`），
而 `^SKIP ` 是**计数契约**（`skip()` 的注释 + `audit-exitcode-test.sh` ⑤）。
原样透传 ⇒ 报告里 `^SKIP ` 行数 **5** 与 `SKIPPED` 计数器 **4** **对不上**（实测）。
改法：**先捕获再缩进 4 格** —— 证据保留（人看得见 rc=2 与「未动任何文件」），
但**不再是契约行**。实测：原始行首 `SKIP ` = 1 → 缩进后 = **0**。

六、反向断言（把被修的东西剥掉，必须复现旧缺陷）
--------------------------------------------------------------------
夹具：`.check/tmp/mk-nofix.js`（生成器）→ `.check/tmp/rc-nofix.js`（把 `stripComments(...)`
换回 `t.includes(name)`，断言「恰好替换 1 处」）。

  node .check/tmp/rc-nofix.js --inject
  ⇒ **PASS 7 / FAIL 2 · RC=1**，失败的**正是 ⑦⑧**（真阳性：注释里提到 → 本应报红却报绿）；
     而 **⑨ 仍 PASS**（假红守门人：行尾注释的真实调用 → rc=0）—— 证明 ⑨ 与修复无关。

修复版对照：**PASS 9 / FAIL 0 · RC=0**。

七、判据复跑（全部实测）
--------------------------------------------------------------------
  ruler-callpoint --inject      **9/9** · RC=0
  ruler-callpoint（真盘）        **RC=0** · 18 个文件全有可执行调用点或已登记豁免（2 条）
  probe-crossref --inject       **29/29** · RC=0（实现搬家的回归检查）
  probe-crossref --scan         **RC=0** · X1 **检查 34 处 · 命中 0** · 悬空 0
  x1-freshness-test.sh          **5/5** · RC=0（34 → 35）
  probe-record-shape --scan     **RC=0** · 三份记录全部合规 · 豁免 0 条
  andyngo-integrity.js          **21 OK / 0 FAIL / 0 UNVERIFIED** · RC=0
  newid.js --check              **未登记撞号 0**
  acceptance.js                 **12 OK / 0 FAIL / 0 DRIFT / 0 UNVERIFIED** · RC=0
  完整载体 andy-audit.sh         **rc=2**（4 处既有 SKIP）·
                                **段位总数 = 25（21 段跑了 · 4 段没跑成）· 核心断言 OK** ·
                                **21 个 `EXIT=0` · 无 `EXIT=[1-9]` 段** ·
                                **`^SKIP ` = 4 == `SKIPPED` · `^NOTE ` = 0**
  冻结件                        **3/3 未动**

八、仍然未做的（如实记）
--------------------------------------------------------------------
① 本机没有 `andyngo build.md.txt` ⇒ `JUDGE-SELFTEST` 段**每次都 SKIP**；
   要真跑得设 `ANDYNGO_SPEC`。**「段存在」≠「段跑了」**。
② 载体的常规工作流持有锁 ⇒ `WRITE-LOCK-SELFTEST` 段只在**无锁窗口**真跑。
   实测（无锁时 · 2026-09-17T06:58Z，本文件写完之后补测）：rc=0 · **28 项全过**。
   ★ 本行原写「16 项」—— 那是**从 `MEMORY.md` 抄来的腐烂值**，被这次实测纠正。见第九节。
③ `andyngo-audit.sh`（v4.x 载体）**不扩面**，未同步这两段 ——
   所以 v4.x 那条路径上，这两把尺子**仍然没接**。这是**明确的残余**，不是遗漏。
④ `strip-comments.js` **不剥字符串字面量**（理由见第二节）—— 残余风险已写在文件头。

九、实测纠错：三处「16 项」是腐烂值（ISS-140）
--------------------------------------------------------------------
写本文件第八节 ② 时，我写了「rc=0 · 16 项全过」—— **这个数我并没有实测**，
而是从 `MEMORY.md:58`（`自测：… （16 项）`）**抄来的**。随后真去跑了一次：

  bash .check/tests/write-lock-test.sh          # 无锁窗口
  ⇒ RC=0 · PASS 行 28 · FAIL 行 0 · 末行 `write-lock 自测：28 项全通过`

真源：`write-lock-test.sh:215` 自己打印的计数 = **28**（`CHG-212` 记的「+5 → 28 项」）。
⇒ **「16 项」已经腐烂了**（至少从 2026-09-16 起就是 28）。它出现在 **3 处非历史文件**：
  ① `skills/andy/scripts/andy-audit.sh`（**我刚加的第 21 段**，4 处 —— 从 MEMORY.md 抄的）
  ② `MEMORY.md:58`（用户级长期记忆）
  ③ `issues.md` 的 ISS-139（我刚落的）
（`.check/record/changes.md` / `issues.md` 里的**历史行**（CHG-081/082/090/091 · ISS-060/061）
 记的「16 项」「18 项」**不改** —— 它们在当时是对的，append-only。）

**处置**：① 载体里**改成「不写死项数」**（让工具自己报数，载体只给语义）——
  这不是「把 16 换成 28」，是**把这个值从载体里删掉**（写死的值会腐烂，指针不会）；
  ② `MEMORY.md` 改成指针形式；③ ISS-139 用 append 补一条校正（不改原行）。
**新开 ISS-140**：这类「散文里的硬编码计数」**没有任何判据守着**。

=== 留证结束 ===
