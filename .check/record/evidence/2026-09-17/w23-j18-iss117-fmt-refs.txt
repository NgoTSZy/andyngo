# 留证 · W23 J18 · 注释里的「文件:行号」引用：**量过了，也修了一处文件**

**日期**：2026-09-17 · **作业**：W23 J18（作业 16 · ISS-117 家族延伸）
**落账**：CHG-337（`.check/tests/andy-fmt-consistency.js`）· ISS-143（新增，`open`/`low`）· EVT 019
**写锁**：`W23-J18-ISS117B`（45 min，`since=2026-09-17T10:42:35Z`）

---

## 0. 这一轮要回答的问题

`ISS-117`（`resolved`）处置的是**两个载体注释里的坏行号引用**。它的**家族延伸**是：

> 全仓还有多少「注释里写 `文件:行号`」的**活引用**？它们腐烂了吗？

长期指令说得很清楚：

> `丙类 · 根本不该由人判：应该由判据判的 → 去加判据，不是去问人`

所以这一轮的**第一件事不是去改，是去量** —— 量出「**这个形状能不能被机械判定**」。
**能判就造判据；不能判就不造**（造出来是一把永远绿的尺子，比没有更坏）。

## 1. 量法（一次性侦察脚本，**不入库**）

脚本：`C:/Users/<user>/AppData/Local/Temp/ref-probe.js`（用完即弃，**故意不进仓库**：
它只在「要不要造判据」这个决策上服务一次，进了仓库就是又一处要同步的东西）。

它扫**注释行**里的「`文件:行号`」，用**多解析根 + 全仓 basename 索引**解析目标文件，
再打印**目标第 N 行的实际内容**供人读。

> ★ 第一版**解析根不足**，把 52 处里的 25 处报成「目标文件找不到」——
> 复盘发现**多数是我的工具差**（简名 / `engine/` 前缀 / 项目外文件）。
> 加 `BYBASE` 全仓 basename 索引 + 更多 `ROOTS` 后重扫，**A 类降到 0**。
> **教训：量出的是工具差时，不能当成缺陷。**

## 2. 测量结果（52 处）

| 类 | 形状 | 处数 | 判定 |
|---|---|---|---|
| **A** | 行号**超出**目标文件长度 | **0** | 无缺陷 |
| **B** | 目标文件**找不到** | 13 | 多为解析差 / 项目外文件 —— **未逐条判定是工具差还是真缺失** |
| **C** | 文件在、行号**在范围内** | 39 | 需**读内容**才知道指对没指对 |

## 3. ★ 关键判断：**A 类为 0 ≠ 健康**

真正的腐烂形状是「**行号在范围内、但指向别的内容**」——

```
原引 `SKILL.md:118`     → 行号在范围内，但那一行根本不是「9 字段」的出处
```

所以**能机械判定的只有 A 类**，而 A 类实测为 **0** ⇒ 若为这个面造判据，
它会**永远绿**。`SOUL.md`：

> **一条只会说 OK 的判据，和没有判据长得一样。**

⇒ **本轮决定：不造判据**（这是一个**判断**，不是「没做」）。

## 4. C 类里查出 6 处**真腐烂**（逐条读内容）

全部在 `.check/tests/andy-fmt-consistency.js` 的 **ISS-132 立论清单**里：

| 引用说 | 实际位置 | 结论 |
|---|---|---|
| `SKILL.md:118` 写 9 字段 | `:132`（标题）/ `:145`（正文） | 已漂移 |
| `andy-event-protocol.md:4` 写 7 字段 | `:18`（「★ 本 skill 是 9 字段，不是 7 字段」） | 已漂移 |
| `andy-dream.md:28` 字段名 `<parent_seq>` | `:40` | 已漂移 |
| `andy-record.md:27` CLI 5 个位置参数 | `:26` | 已漂移 |
| `andy-dream.md:41` `score` 来源 | `:41` | 文件对、内容需人看 |
| `SKILL.md:55` 「真的写进了 `eventlog/`」 | `:70` | 已漂移 |

**处置 = 换成可 grep 的字符串，不是「把行号改对」** —— 改对会**再腐烂一次**：

> `SOUL.md`：**写死的值会腐烂，指针不会。**

## 5. 另 3 处**实测仍准**（也一并换掉）

| 引用 | 实测 |
|---|---|
| `andy-auto.md:118` | 第 118 行 = `**REJECT 胜** —— 依据是 2026-09-16 实测…（`EVT 024`）。` ✓ 逐字对上 |
| `andy-dream.md:40` | 第 40 行含「**不是 `parent_seq`**」对照句 ✓ |
| `andy-dream.md:5` | 第 5 行 = `required: `parent` 推导 · `score` 推导 · 显著改进判据` —— 该处是**历史事件引文**（描述 2026-09-17 那次并行 Edit 静默丢更新），**不是活指针** |

⇒ 换完后该文件「`文件:行号`」引用**归零**，并附**可自验的判法**：

```
grep -nE '[A-Za-z0-9_.-]+\.(md|js|sh):[0-9]+' .check/tests/andy-fmt-consistency.js
# 改前 9 处命中 → 改后 0 处命中（rc=1）
```

## 6. 实测（改的是注释，判定逻辑按模式匹配、未动 —— 但**必须实测**）

| 检查 | 结果 |
|---|---|
| `node --check` | **rc=0** |
| 判据本体 | **rc=0**（扫描面 **30** 文件不变 · 锚点仍 `skills/andy/SKILL.md:133` · `OK skills/andy 内部对事件行字段的说法一致（5 处声明 + 1 处实例）`） |
| `--inject` | **6/6 PASS** —— 含 ⑤ **假红守门人**（无关变异 → 必须仍 0 违规） |
| 改前指纹交叉核对 | `file-history --list` → `v2` / 会话 `34b3b3a5` / **269 行** / `000d503b881ae55628cc883bc099f23d` —— 与改前实测**一致** |
| 改后 | **277 行** / `4d8a4a3f5548cf686e5b68412b2a9e46` |

## 7. 我**不**报完成的地方（边界）

1. **剩余 43 处（C 类 30 + B 类 13）未核内容** —— 已开 **ISS-143**（`open` / `low`）记这个**已知未做的面**。
2. **B 类 13 处未判**是「工具差」还是「真缺失」—— 侦察脚本已弃，重做时按 ISS-143 写的方法直接读。
3. 只修了**一处文件**（`andy-fmt-consistency.js`）；其余文件的注释引用**一处未动**。
4. **不造判据**是判断，不是遗漏；理由见 §3（A 类为 0 ⇒ 会永远绿）。

## 8. 这一轮真正的产物

**不是**「改了 9 行注释」，而是**两个判断**：

1. **「注释里的行号引用会腐烂」这个形状，机器判不了** ⇒ 不造判据（省下一把永远绿的尺子）。
2. **「量出 0 处」要先问「我的尺子够长吗」** —— 第一版脚本报 25 处「找不到文件」，
   真相是我的解析根不足（**工具差 ≠ 缺陷**）。
