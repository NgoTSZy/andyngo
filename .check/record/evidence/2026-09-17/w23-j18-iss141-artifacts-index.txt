=== 留证 · W23 J18 · ISS-141 的另一半（产物轻量登记 + 判据）===
时间：2026-09-17T08:39Z
写者：B（会话 W23-J18-ISS141B · 写权锁 90min）
落账：CHG-316（skills/andy/SKILL.md）· CHG-317（skills/andy/scripts/andy-audit.sh）·
      CHG-318（.check/tests/artifacts-index.js · 新增）· CHG-319（artifacts/INDEX.md · 新增）·
      CHG-320（skills/hub/routes.md）· EVT 014

【1】用户裁决（逐字）
「加一条轻量登记（只记 path + 一句理由，不进 eventlog.和 ISS-140 是同一个形状。要造」
→ 两条：① **加轻量登记** ② **要造判据**（「和 ISS-140 是同一个形状」= 「**散文里的约定没有判据守着**」）。

【2】新增件
① `Documents/workbuddy/artifacts/INDEX.md`（**新建**）—— 产物登记。
   格式 `- \`<slug>\` · <一句理由> · <日期>`（一行一条 · 按日期倒序）· **不进 eventlog**。
   当前登记 2 条：`iphone18promax`（2026-09-17）· `snake-nyc`（2026-09-16）。
② `.check/tests/artifacts-index.js`（**新建**）—— 判据。
   **R1** 孤儿目录（有目录无登记）· **R2** 悬空登记（有登记无目录）。
   `--inject` **3 条**：剥掉一条登记（必红 R1）· 加一条悬空（必红 R2）· **假红守门人**（两边顺序颠倒 → **不得**报红）。
   ★ **纯函数判定体 + 内存变异**：`--inject` **全程不碰磁盘**（不复制、不改夹具）。
   ★ **三态**：`rc=2` = 找不到产物目录 / 登记档 → **拒跑**（**没跑成 ≠ 通过**）。
   ★ **定位不写死用户名**：`os.homedir()` + `process.env.ANDY_ARTIFACTS` 可覆盖。

【3】★ 判据自己先被抓了一次（`ruler-callpoint.js` 按设计工作）
新判据放进 `.check/tests/` 之后，`ruler-callpoint.js` **rc=1** 报：
「**R1 未接**：`.check/tests/artifacts-index.js` **可执行调用点 0 个**，且**未在豁免表登记**」
→ 接进载体后回绿。**这就是「判据不跑等于没有判据」（ISS-105）的机制化** ——
不需要我记得去接线，是尺子自己抓的。

【4】接进载体
`skills/andy/scripts/andy-audit.sh` 追加 **ARTIFACTS-INDEX** 段（`rc=2` → 映射 `skip`），
**段位总数 25 → 26**（三处同步：`-ne 26` · `期望 26` · `= 26（`）。
★ 插入用**一次原子 Edit + 跨多行锚点**（`old_string` 含 `fi` + 后续 `echo` 块）——
**不是** `ins-line.js`：`  fi` 不唯一，单行锚点表达不出「if/else 块的 `fi` **之后**」（**ISS-133 的根因**）。
★ 插完**再读一遍那一段核对层级**：新段（578–589 行）在 `fi`(576) **之后**、`} >> "$REPORT"`(598) **之内**，
  缩进 2 空格与同级一致 ⇒ **没有**被套进「只有某文件缺失时才执行」的分支。`bash -n` 只挡一半，所以靠读回。

【5】规格升级
`skills/andy/SKILL.md`：原先写的「**这是先例约定，不是规格**」→ **升级为规格**
（必须登记 + 格式 + 判据位置 + **判据的边界**）。行数 **221 → 226**。

【6】标价链（第二轮）
`andy` **3,125/16,857 → 3,253/16,985**；表头合计 **54,544/156,558 → 54,672/156,686**（**+128**）。
`node skills/hub/price.js --check` **rc=0**：「表头合计 54672/156686 ↔ 表内逐行相加 54672/156686 一致（22 行）·
数值不一致 0 · 缺登记 0」。★ **手算的合计由脚本证实**。

【7】判据复跑
（见会话报告 / 记录层；`--check` 已 rc=0）

【8】仍然未做的（**不报完成**）
① 判据**只保证「目录 ↔ 登记」一一对应** —— 它**不检查**那句理由写得对不对，
   也**不检查**产物本身能不能用。这条边界**写在判据自己的文件头里**（不只写在日志里）。
② 载体 `andyngo-audit.sh`（**v4.x**）**不扩面** ⇒ 那条路径上这个判据**仍然没接**。
③ 产物登记是**给人读的**，不参与任何自动决策 —— 它是「**可发现性**」的修复，
   **不是**「正确性」的修复。别把它当成质量保证。

=== 留证结束 ===
