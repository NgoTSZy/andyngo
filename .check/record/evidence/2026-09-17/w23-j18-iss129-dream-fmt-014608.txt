================================================================================
留证 · w23-j18 · ISS-129 / ISS-130 / ISS-131
策略回放（DREAM）脚本链：读端护栏缺失 + 字段错位 + 归档判重口径
时间窗：2026-09-17T01:41Z ~ 01:48Z（本地 09:41 ~ 09:48）
写锁：W23-J18-ANDY-DREAM（40min，我声明）
================================================================================

§0 这次任务从哪来
--------------------------------------------------------------------------------
Human A 转来一份「dream-rsi.com 分析 + 对 /andyngo 的整合建议」，并自述
「**这些新增假的**」，问三件事：① 如何配置并部署？② 每次成功后存进一个叫
「记忆」的文件？③ 还是变成文件夹协助整理？

按仓库教条「**先看磁盘实况、不信描述**」，先做事实核查，再回答。

§1 事实核查：哪些是真的、哪些是假的
--------------------------------------------------------------------------------
| 分析里说的 | 磁盘实况 | 判定 |
|---|---|---|
| `references/andyngo-sources.md` 有 `L4 · UI 模式与灵感` | 存在（`:107`）；**且 `skills/andy/references/andy-sources.md` 与它 md5 相同**（`e42c8caa6d03eb691cfd864d3414dce7`） | **真**（有两份逐字相同的副本） |
| `andyngo-evolve.md` 存在 | 存在，**但只有 26 行**，**没有**「策略回放回归」节 | **半真** |
| 「新增 `andyngo-replay.md` 事件」 | `skills/andyngo/` 下 `回放/replay` **0 命中** | **假**（在 andyngo 下） |
| — | **但 `skills/andy/`（v5.0，31 文件）已完整实现**：判事件表 `:39` 有 `DREAM` 事件 · `andy-dream.md`（98 行）· `andy-dream.sh`（50 行）· `andy-archive.sh`（32 行）· `SKILL.md` PHASE 4 = `EVOLVE → DREAM → SEAL` · 9 字段协议 | **★ 真，Human A 不知道** |
| 「L1 增加策略回放机制」 | 已实现（`andy-dream.md` §1「Dream-RSI 改的是 L1 的派发策略，不碰 L3」） | **真** |
| 「无需改动 L3」 | 与 `andy-dream.md` §4 第四条约束「**不改 L3**」逐字一致 | **真** |

**⇒ 结论：分析里说的「新增」不是假的，是「已经建好了、名字叫 DREAM 不叫 replay」。**
**⇒ 但「部署」卡在一个硬约束上 —— 见 §2。**

§2 实测抓到的三个真缺口（**全部静默，全部 rc=0**）
--------------------------------------------------------------------------------
在临时副本里跑（`PROJECT_ROOT` 指向 mktemp 目录，**零污染主树**）：

### 缺口 A · 读端没有格式护栏（ISS-129）
写端 `andy-record.sh:85-98` 有护栏（末行字段数 ≠ 10 → `rc=2` 拒跑），**读端 `andy-dream.sh` 没有**。
实测（从 `~/.workbuddy-ai` 跑，归档 73 条 **8-token** 事件）：

    === 发现树（parent_seq 关系）===
      001 <- OK          ← `$6` 在 8-token 行里是 **status**，不是 parent；整张树全是 OK
    === score 分布 ===
      条数 65 · 最小 .check/andyngo-integrity.js · 最大 skills/hub/routes.md · 均值 0.0
                          ← `$7` 在 8-token 行里是 **artifact_ptr**，把文件路径当分数排

**而脚本 rc=0、一行错误都不报。** ⇒ 本项目最怕的形状：**看起来完成**。

### 缺口 B · score 取错字段（ISS-130）—— **护栏挡不住这一条**
即使输入是**合法的 v5.0 流**（10 token），`andy-dream.sh:41` 仍按 `$7` 取 score，
而按 `andy-dream.md` §2 的字段定义，`$7` 是 **status**、`$8` 才是 score。
实测（6 条合法流，其中 1 条 status=FAIL）：

    改前 → 条数 6 · 最小 FAIL · 最大 OK · 均值 0.0
    改后 → 条数 5 · 最小 70   · 最大 100  · 均值 87.0   ← 与手算一致

⇒ 输入合法，错的是**解析位置本身**。格式护栏对它**无效**。

### 缺口 C · 归档判重口径跨文件（ISS-131）—— **每天误报**
`andy-archive.sh` 在**拼接后**判重。实测三个日期文件：

    2026-09-15.txt: 001–012 · 条数 12 · **日内重复 []**
    2026-09-16.txt: 001–057 · 条数 57 · **日内重复 []**
    2026-09-17.txt: 001–004 · 条数  4 · **日内重复 []**
    跨文件重复：001,002,003,004,005,006,007,008,009,010,011,012

根因：seq 是**按日从 001 起**的（`andy-record.md` §序号规则：「三个序号全部从 001 开始，各自独立」）
⇒ **跨日重号是设计使然，不是缺陷**。原口径把它当真错误报 ⇒
「**总是响的警报等于没有警报**」（SOUL.md / ISS-105），会让下一个会话忽略真 WARN。

§3 改了什么（5 个文件 · 全部最小增量）
--------------------------------------------------------------------------------
| 文件 | 行数 | md5 前 | md5 后 |
|---|---|---|---|
| `skills/andy/scripts/andy-dream.sh` | 50 → **84** | `7e05de8cb1105550d976525abc9e5466` | `dd8f66ec5cde3163463675372c551236` |
| `skills/andy/scripts/andy-archive.sh` | 32 → **41** | `3ae2329a0e8be38060b1188f35404b0e` | `30db048273df064c66f0ac9098f9a50b` |
| `skills/andy/scripts/andy-audit.sh` | 419 → **444** | `27b481d89b958fd7077d1f0c9356a38a` | `0ef2244e4044343e46a77ef5c1388644` |
| `skills/andy/references/D-evolution/andy-dream.md` | 98 → **118** | `69444815b5fd516aed4ce5c7c7a049da` | `5756e9aa24c34925838cb774bb1a5f02` |
| `.check/tests/andy-dream-test.sh`（**新建**） | 0 → **130** | — | `a6efc2bc8ff6fbb2426565ad8dd4ff14` |

（md5 的实测复核见 §8 —— 上表两个值在初稿里写错过，已按实测改正。）

1. **`andy-dream.sh` 加格式前置检查**（照抄写端 `andy-record.sh:85-98` 的**形状**，不发明新形状）：
   末行字段数 ≠ 10 → `rc=2` 拒跑 + 说明 8/10 两种口径 + 指出两个根变量名字不同。
2. **`andy-dream.sh` 的 `score` 从 `$7` 改 `$8`**（并把 10 个位置写进注释）。
3. **`andy-archive.sh` 判重改为逐文件**：跨日重号不再报，**日内重号（真错误）照报**。
4. **`andy-dream.md`** 补 §2 的**字段位置表**（`$6=parent` `$7=status` `$8=score`）+
   §6 判读表加一行（字段数 ≠ 10 → `rc=2`）+ 写清「为什么默认跑不起来是设计不是缺陷」。
5. **新建尺子 `.check/tests/andy-dream-test.sh`**（11 项断言，含**反向**），
   并接进载体 `andy-audit.sh` **第 14 段**（`ANDY-DREAM`）。
   *段号是位置别名（DEC-039），所以**追加在末尾**，不插入。*

**为什么改的是「读端」而不是「让读端支持 8 字段」**：
`andy/SKILL.md:144` 明文写「**所以默认 `RECORD_ROOT=$PWD/.check/record`** —— 从 `~/.workbuddy-ai`
跑会撞上既有日志（护栏会拒），从别处跑会开新根。**这是设计，不是缺陷。**」
⇒ 设计意图就是**拒**。读端此前**不拒**（而是静默读错），**与设计意图相反**。
所以我补的是「让设计意图在读端也成立」，**不是**加一个新功能。

§4 验证（全部实测，非推理）
--------------------------------------------------------------------------------
### 四场景（临时目录，零污染）

| 场景 | 期望 | 实测 |
|---|---|---|
| 真实 3 天 8-token 流 | 归档不误报 + 回放拒跑 | `归档: 73 条` **无 WARN** ✓ · `UNVERIFIED 归档件是 8 字段 → 拒跑` ✓ |
| 10-token v5.0 流 | `rc=0` + 字段位置正确 | 发现树 6 行全对（`001 <- 000` … `006 <- 005`）· **`条数 5 · 最小 70 · 最大 100 · 均值 87.0`** ✓ |
| 无归档 | `rc=1` | `FAIL 无历史事件流，先跑 archive` ✓ |
| 日内重复 | 仍报 WARN | `WARN 2026-09-17.txt 日内有重复 seq: 002` ✓ |

### 尺子（`.check/tests/andy-dream-test.sh`）

    ① 8-token 拒跑（2）              PASS
    ② 10-token 放行（0）              PASS
    ② 发现树 6 行全为 seq <- parent 形态（6） PASS
    ③ score 分布（条数5·最小70·最大100·均值87.0） PASS
    ④ 无归档（1）                     PASS
    ⑤ 跨日重号不报 WARN（0）           PASS
    ⑥ 日内重复报 WARN（1）             PASS
    ⑦ 10-token 归档条数（6）           PASS
    ⑧ 剥掉后 FMT_N 残留应为 0 处（0）   PASS
    ⑧ 无护栏时 rc（旧行为=0，即静默）（0） PASS
    ⑧ 复现旧缺陷：6 行把 status 当 parent  PASS
    合计：PASS 11 / FAIL 0 · LADDER_RC=0

**⑧ 是这次尺子最关键的一项**：它把护栏用 `sed` 剥掉，再跑**同一条 8-token 流** ——
**必须复现旧缺陷**（实测复现 6 行）。少了它，① 的绿只证明「没红」，
**不证明「绿是因为护栏在起作用」**。本仓库教义：**一条只会说 OK 的判据，和没有判据长得一样。**

§5 已知边界（**不许当成已解决**）
--------------------------------------------------------------------------------
1. **「回放有效」仍未验证。** `andy-dream.md` §7 自己写着：在真正跑出一次
   「候选策略显著改进并被采纳」之前，本层**是设计，不是事实**。
   本次修的只是「脚本没坏」，**不是「回放有用」**。
2. **默认跑不起来（设计使然）**：本机 `eventlog/` 是 8 字段（`andyngo-record.sh` 写），
   而本 skill 写 10 字段。混写会让 `andyngo-integrity.js` 的 I1（`if (f.length !== 8)`
   —— **精确等于 8**）永久报红。**要回放必须用独立根积累 10 字段流**（≥5 条）。
3. **两个根变量名字不同**：写端认 `RECORD_ROOT`，归档/回放认 `PROJECT_ROOT`。
   默认两者同指 `$PWD`；**显式设其中一个时必须两个都设**，否则读写不是同一个根。
   （本次未改这个 —— 它只在「显式设一个」时才有害，改它属于接口变更，超出最小改动。）
4. **`skills/andy/` 内部格式自相矛盾**（**本次未改，留给下一个会话**）：
   `SKILL.md:118` 说 9 字段（10 token），而 `andy-event-protocol.md:4` 说 7 字段（8 token）。
   两处都写在同一个 skill 里。**这是文档冲突，不是代码缺陷**，但会让读它的人判错口径。
5. **平台侧漏记**：`andy-audit.sh` 在 `file-history` 里的最新版本块是 **418 行**
   （`f018823c9b11f7ec97893a17404ef6cd`，01:05:05），而**改前磁盘是 419 行**。
   ⇒ 有一次改动**没产生版本块**。§3 表里 `andy-audit.sh` 的「md5 前」是我
   **用 `sed` 重建**（删掉我追加的段）得到的 `27b481d89b958fd7077d1f0c9356a38a`，
   并用 `diff` 交叉核对为「**25 增 / 0 删**」（纯追加，所以重建版 = Edit 前的确切内容）。

§6 我的偏差（如实记录）
--------------------------------------------------------------------------------
1. **我先写了「431 行」这个数**（从 Grep 输出的最大行号推断），实际改前是 **419 行**。
   发现方式是重建后 `wc -l` 不符 —— **如果我没重建，这个错数会进 CHG 行**。
   教训：**行号 ≠ 行数**；Grep 的最大命中行号不是文件行数。
2. 场景 1 的 `DREAM_RC=141` 是 `| head -2` 提前关管道的 **SIGPIPE 假象**，
   不是脚本的退出码。单独实测是 `RC=2`。**我把这个假象写进过中间输出** ——
   报告里必须说清，否则下一个人会去查一个不存在的 141。

§7 未做（明确留给下一个会话 / 待 Human A 裁决）
--------------------------------------------------------------------------------
1. **`andy-event-protocol.md` 与 `SKILL.md` 的字段数冲突**（§5.4）—— 需要人裁决以哪边为准。
2. **`RECORD_ROOT` / `PROJECT_ROOT` 统一**（§5.3）—— 属于接口变更，未做。
3. **让 `andy-dream.sh` 支持 8 字段流**（现推 `parent`、`score` 写 `--`）——
   **技术上可行**（`parent` 推导只需 `seq` + `wave`，8 字段里都有），
   但与 `SKILL.md:144` 的「设计意图是拒」相反 ⇒ **需要 Human A 裁决，我不单方面加功能**。
4. **`.check/record/archive/` 未在主树创建** —— 本次所有实测都在 `mktemp` 临时副本里跑，
   **主树零污染**（主树 `archive/` 至今不存在）。

§8 指纹复核（实测，2026-09-17T01:48Z）
--------------------------------------------------------------------------------
改动后（`md5sum` 逐字符核对）：

    dd8f66ec5cde3163463675372c551236  skills/andy/scripts/andy-dream.sh                50 → 84 行
    30db048273df064c66f0ac9098f9a50b  skills/andy/scripts/andy-archive.sh             32 → 41 行
    0ef2244e4044343e46a77ef5c1388644  skills/andy/scripts/andy-audit.sh              419 → 444 行
    5756e9aa24c34925838cb774bb1a5f02  skills/andy/references/D-evolution/andy-dream.md 98 → 118 行
    a6efc2bc8ff6fbb2426565ad8dd4ff14  .check/tests/andy-dream-test.sh                  0 → 130 行（新建）

改动前：

    7e05de8cb1105550d976525abc9e5466  skills/andy/scripts/andy-dream.sh        （file-history v2 块，与磁盘相同）
    3ae2329a0e8be38060b1188f35404b0e  skills/andy/scripts/andy-archive.sh      （file-history v1/v2 块）
    27b481d89b958fd7077d1f0c9356a38a  skills/andy/scripts/andy-audit.sh        （**sed 重建**，见 §5.5）
    69444815b5fd516aed4ce5c7c7a049da  skills/andy/references/D-evolution/andy-dream.md

**初稿里我写错两个值**（`…abc6e5466` 应为 `…abc9e5466`；尺子 `…addd4ff14` 应为 `…ad8dd4ff14`），
已按本节实测改正。**留证里的指纹一旦写错，后面所有引用都会指向一个不存在的版本** ——
这正是本仓库反复写的「**写死的值会腐烂**」。

§9 终态载体读数（2026-09-17T01:59Z · 报告 `audit-015608.txt`）
--------------------------------------------------------------------------------
    [ 1] 主检查 check-protocol.js   OK   PASS 8 / FAIL 0 / UNVERIFIED 0
    [10] hub 标价一致性             OK   登记 22 本 · 磁盘 23 本 · 数值不一致 0 · 缺登记 0
    结论：12 OK / 0 FAIL / 0 DRIFT / 0 UNVERIFIED
    完整性：21 OK / 0 FAIL / 0 UNVERIFIED
    CROSSREF：命中 0 处 · 豁免 0 条 · 悬空 0
    RECORD-SHAPE：OK 三份记录全部合规 · 豁免 0 条
    第 14 段 ANDY-DREAM：合计 PASS 11 / FAIL 0
    非零 EXIT 行：**0 处**（全部 15 段 EXIT=0）
    载体退出码 **2（拒跑）**：2 处 SKIP，其余 15 段 EXIT=0

**两处 SKIP 是既有的、正常的**：`SPEC-COVERAGE` 与 `SPEC-REF-COVERAGE` 报
「未找到规格（可用 ANDYNGO_SPEC 指定路径）」—— 本机没有 `andy.txt` 规格文件。
**不是本轮引入的**，也不是缺陷（载体 `rc=2` 的语义就是「有段没跑成」）。

**★ 一条必须写下的中间态说明**：本轮**第二份**载体报告 `audit-015018.txt` 是 **`rc=1`**
（`ACCEPTANCE` 与 `HUB-PRICE` 两段非零）。**那不是缺陷** —— 它是
「`exit-codes.md` 已登记、但 `routes.md` 标价未重算」的**中间态快照**：
- 当时 `[10]` 红（`x andy 表 2907/15691 ≠ 实测 2907/16193`）→ 已由 **CHG-262** 修
- 当时 X1 已绿（登记在跑载体之前完成）
**处置**：先修 `routes.md` 标价 → 复跑 → 本报告（`audit-015608.txt`）全绿。
**留在这里的理由**：中间态报告**不删**（同「不改历史，只追加」），否则下一个人看到
`audit-015018.txt` 的 `rc=1` 会以为有一处未解的失败。

================================================================================
