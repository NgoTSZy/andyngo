W23 J18 · 留证：ISS-145 已修（说明文字改成与实现一致，判法未动）
========================================================================
写锁 W23-J18-ISS145（since 2026-09-17T13:04:05Z · pid 19444）

一、修的是什么
------------------------------------------------------------------------
`crossref-baseline.json` 的 `_how_to_update` 那句维护说明**把因果写反了**：

| | 文字 | 与实现的关系 |
|---|---|---|
| 原文 | 「**每写一条新 CHG 都要重跑** `--baseline`，否则新行会落进「历史」而不受判」 | ✗ **相反** |
| 新文 | 「判法本体是 `id > maxId` ⇒ **不重跑时新行一直受判**（更严）；**重跑会把 `changesMaxId` 抬到当前最大号，其下的行从此不再受判**（存量豁免）。⇒『要不要抬基线』是**设计取舍**，不是『让新行受判』的必要动作」 | ✓ 与 `x4()` 一致 |

**依据**：`x4()` 判法本体 `if (id > maxId) { seen++; … } else if (!ok) histBad++;`（`.check/probe-crossref.js:297-318`）；
`--report` 实测 **`[X4] … 检查 224 处`**（当时）⇒ 224 行在受判面内。

二、为什么必须改**两处**
------------------------------------------------------------------------
那句话**同时存在于两个地方**：
1. `probe-crossref.js` 第 471 行的常量（`--baseline` 时写出去的那份）
2. `.check/record/crossref-baseline.json` 的 `_how_to_update`（**已落盘、读的人真正看到的那份**）

只改常量 ⇒ 留下一份看不见的旧话，**且下一次 `--baseline` 会把旧话写回常量**。

三、机械核对（不靠「我读过」）
------------------------------------------------------------------------
| 项 | 结果 |
|---|---|
| `node --check .check/probe-crossref.js` | **rc=0** |
| 两处字符串是否逐字相同 | **相同**（各 **283** 字符 —— 脚本从 JS 常量与 `JSON.parse` 各取一份比对） |
| `JSON.parse` | 成功 |
| `evidence` 字段 | **仍 2 天**（`2026-09-15`=25 · `2026-09-16`=65）—— 逐字未变 |
| `changesMaxId` | **仍 `CHG-116`** ⇒ **没有误触发抬基线** |
| `probe-crossref.js --scan` | **rc=0** · `[X4] 检查 226 处` · `[X3] 检查 3 处` ⇒ **判法一个字节都没变** |

四、★ 刻意没做的事（这才是本条的边界）
------------------------------------------------------------------------
**没有重跑 `--baseline`** —— 它会把 `changesMaxId` 抬到 `CHG-342`，⇒ `CHG-117…342` 共 **226 行从此不再受判**。
所以这里是**手改字段**，而不是「跑一遍让它自己写」。**抬不抬基线仍是你的裁决**（见 ISS-146）。

五、我修正了自己上一轮的判断（如实记）
------------------------------------------------------------------------
上一轮我把 ISS-145 记成「**乙类**（要与『要不要抬基线』这个设计取舍一起定）」——
**那个耦合不成立**：那句错话**在任何一种基线口径下都是错的**（判法本体与它无关）
⇒「**把错话改成对话**」是**甲类**；「要不要抬基线」才是乙类。
⇒ 本轮只做甲类那半，乙类那半**原样留给你**（`ISS-146`）。
