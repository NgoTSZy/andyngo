作业 W23 J19 · 2026-09-18T00:04Z · B
主题：载体「正常缺席」修复 + 剥离后**第一次**跑载体暴露的三个连带红

【一、触发】
用户消息「Please continue…」（续 J18）。J18 的乙案剥离（本机只保留 /andyngo，2026-09-17 22:32）之后**从没跑过载体** —— 本轮是第一次跑。
★ 这条本身是本作业最大的发现：**载体是唯一能发现 I1/I7/口径分叉的通道**，而它在剥离后一直没跑。

【二、改前基线（实测）】
- 载体 skills/andyngo/scripts/andyngo-audit.sh · md5 8159184feedc25e9ee73ad428b327ee4 · 512 行
  取证：`.check/tmp/publish` 副本把脱敏处还原后 md5 **逐字相同** + `file-history` v18 互证（两条独立来源）
- .check/tests/judge-selftest.sh · md5 9a6953241b097346501c874a9ec02389 · 115 行（副本取证）
- .check/record/eventlog/2026-09-16.txt · md5 d8da5251f1fc894a57ac09c623c7a407 · 57 行（file-history v48）
- .check/record/eventlog/2026-09-17.txt · md5 969d2448461bb0123280c4942fcc41fa · 25 行（J18 实测记录）
- acceptance.js（本轮首跑）：11 OK / 0 FAIL / 0 DRIFT / 0 UNVERIFIED · rc=0 · 7m7s（昨天同一条 2m19s；acceptance 自身 CPU 仅 0.14s ⇒ 时间在 spawn/IO，且 07:44 是当日首次跑=冷缓存）
- 冻结件 3/3 md5 逐字相符（e8d1386e91e983c4961f5796a2486558 · 4f0aa2d0c63c2d1a46bd3e86cdbeebc6 · 5a2eea14e8a80bf89557ecaf146965ff）

【三、载体第一次跑（只改了 HUB-PRICE 段）的读数】
- rc = 1（不是 2）· 共 20 段 EXIT= 行 · 2 段非零 · 2 处 SKIP · 1 行 ABSENT
- ABSENT skills/hub/price.js —— hub 已移出本机（正常缺席，不计 SKIP；移回后本段自动恢复）  ← 修复生效
- SKIP 未找到规格 ×2（SPEC-COVERAGE · SPEC-REF-COVERAGE）
- ACCEPTANCE 段 EXIT=1：19 OK / 2 FAIL —— 即 [I7] 与 [I1]（见下）
- JUDGE-SELFTEST 段 EXIT=2（拒跑被记成失败 —— 见 ISS-149）
- 段位总数 = 29（运行时 $SEGS）★ 与「=== 标题数 21」不是同一个量，不要混

【四、改后实测】
- 载体 md5 a21f322d320c0dd07ec7c083ccb8c0df · 536 行（两次改动：512→518→536）
- judge-selftest.sh md5 5b2d8b3b0805685cba0864d13b663063 · 120 行
- eventlog 2026-09-16.txt md5 9fc08ea3c69c3cf29e95378786ff7031 · 57 行（行数不变）
- eventlog 2026-09-17.txt md5 39281b3b57943ee9fc94d5ec3dfea83f · 25 行（行数不变）
- `node .check/andyngo-integrity.js` → **21 OK / 0 FAIL / 0 UNVERIFIED · rc=0**（I1 与 I7 由 FAIL 转 OK）
- `bash .check/tests/audit-exitcode-test.sh` → **5/5 · rc=0**（⑤ 假根 SKIP 19 → 18，尺子确实反映了被删掉的那个 skip 点）
- `node .check/tests/hardcoded-count.js` → rc=0
- 载体（全量重跑）：见下方【六】

【五、修复清单（6 项）】
1. 载体 HUB-PRICE 段：`else skip "无 skills/hub/price.js"` → `else echo "ABSENT …"`（不计 SKIP、不打行首 `SKIP `）
2. `.check/tests/judge-selftest.sh:37`：行首 `SKIP ` → 缩进 5 格（契约行不再碰撞）
3. 载体 JUDGE-SELFTEST 段：**先探规格**，探不到就 skip（根本不跑判据）；探到才跑 + 捕获输出 + 缩进 4 格（照 v5.0）
4. `.check/record/eventlog/2026-09-16.txt` EVT 035/041 第 7 字段 → `--`
5. `.check/record/eventlog/2026-09-17.txt` EVT 025 正文 → 迁入其引用的留证文件后，行截断为恰好 8 字段
6. 发布副本反斜杠路径脱敏 8 处（file-history.js 1 · host-log.js 1 · immunity-hook.js 3 · issues.md 3）

【六、待办（本轮未做，如实记）】
- 发布副本落后源树 3 个文件（crossref-baseline.json · decisions.md · id-collision-exemptions.md）—— 见 ISS-151
- `immunity-hook.js` 的 3 处硬编码本机路径是**代码**（不只是注释）⇒ 脱敏后仍跑不了；它是否该进发布件是**发布范围**判断，留给用户
- 规格文件 `andyngo build.md.txt` 不在本机任何候选路径 ⇒ 载体**仍 rc=2**（2 处环境性 SKIP）⇒ 要用户裁决
- 载体全量重跑的结果（本文件写作时尚未完成）

【七、我自己的错误（如实记）】
- 改 EVT 025 时**从名字猜了 `mk-line-edit.js` 的语义**（当成「整行替换」，实为「把<片段>替换成<新片段>」）⇒ 该行被改成 60 字段 / 537 字符 ⇒ 当场用 Python（`.check/tmp/trunc-evt.py`，`newline=''` 保换行符）逐字截断回退。
- 它与 J18 那次「绕过 `fix-evt-line.js` 的 8 字段护栏」是**同一个坏习惯的两面**：**护栏在响的时候，我没有回头看它为什么响**。

【八、记录】
CHG-358 · CHG-359 · CHG-360 · CHG-361 · CHG-362 · ISS-148 · ISS-149 · ISS-150 · ISS-151 · EVT 001（`eventlog/2026-09-18.txt`）
★ **更正（本文件初版写错）**：CHG-358/360/361/362 与 ISS-149/150 的正文里写的是「见 EVT 026」—— 那是我按 09-17 文件的末号**预测**的，而 `newid.js` **按文件**分配编号：`2026-09-18.txt` 是**新文件** ⇒ 实际号是 **EVT 001**。**正文 append-only，不改**，更正记在这里。（同族：本项目「正文里的号按预测写死、落盘后核对实得号」这条纪律，我这次**预测了但没有核对**。）

【九、追加 —— 用户裁决后的收尾（同一作业，不新开 EVT）】
**背景**：J19 收尾时摆了两项给用户裁决（① 远端 `d6c2d1f` 里的 8 处本机路径要不要改写历史 ② 规格文件缺件怎么办）。用户裁决：**① 合并为干净提交 + 强推 · ② 改用环境变量固定路径**。

--- ① 远端历史重写（已完成）---
- 实测**三个提交全部含本机路径残留**：`826c88e` **5 处** · `d6c2d1f` **8 处** · `67eb217` **0 处** ⇒ 「只合并后两个」不够，必须**重写全部历史**。
- 做法：先留本地备份分支 `backup-pre-clean`（= `67eb217`）→ `git checkout --orphan` + 单次提交 ⇒ **单个干净根提交** `0c97319`（其树与 `67eb217` **逐字相同**，`git diff --stat` 为空）。
- 强推：`--force-with-lease` ⇒ `+ d6c2d1f...0c97319 main -> main (forced update)` · **rc=0**。
- **远端实况核验**（`fetch` 后扫 `origin/main` 树）：本机路径残留 **0 处** · 文件 **257** · 提交数 **1**。
- **诚实边界**：旧对象（`826c88e` / `d6c2d1f` / `67eb217`）可能仍以**悬空 SHA** 在 GitHub 侧存在，直到服务端 GC —— 强推**不保证立即抹除**。

--- ② 规格定位路径钉死（已完成）—— CHG-363 / ISS-152 ---
- 载体原先在 `SPEC-COVERAGE` 与 `JUDGE-SELFTEST` **两段各抄一份**候选表，且第三候选是**写死的本机绝对路径**（而本文件是**发布件**）⇒ 抽出 **`find_spec()` 唯一实现**；候选顺序钉为 `ANDYNGO_SPEC` → `.check/spec/` → 工作区根 → `$HOME/Desktop`；新建 `.check/spec/` 作**固定投放点**。
- **反向断言六项全过**（把 `find_spec` 从脚本外提出来单测）：① 三处全无 → `rc=1` 空 · ② 只有工作区根 → 命中 · ③ 加了 `.check/spec/` → **优先命中它** · ④ `ANDYNGO_SPEC` 指向不存在文件 → **原样返回、不回退** · ⑤ `ANDYNGO_SPEC` 空串 → 回退候选表 · ⑥ 载体里 `/c/Users/<user>` 计数 **0**。
- **实测**：行数 **536 → 549** · `bash -n` rc=0 · `audit-exitcode-test.sh` **5/5 · rc=0** · 载体重跑见同目录后续报告。
- **仍 rc=2**：规格文件在本机**四处全无**（工作区根 / 桌面 / 文档 / 备份归档；CHG-106 证明 09-16 它还在桌面）⇒ 三处 SKIP 是**诚实的缺失信号**，不是件丢了。

--- ③ 本轮第三个错误（如实记）---
- 落 ISS-152 时我**手写并多带了一个 ts**（把 CHG 的 `printf '%s %s' "$TS"` 模板照搬到了 ISS），而 `ISS-<号> <状态> <首现> <最后更新> <严重度> …` 的 **f2 是状态、不是 ts** ⇒ `probe-record-shape.js` 的 **[I8] 当场报红**（`第 2 字段（状态）不是合法枚举：2026-09-18T00:17Z`）。
- **修法**：`mk-line-edit.js` + `fix-line-edit.js`（**行首片段替换**）去掉多余的 ts ⇒ **行数不变** · 行首 70 → 52 字符 ⇒ `probe-record-shape.js --scan` **rc=0**。
- **护栏是好的**：同一轮里 `alloc.sh` 还因为我把**分隔符写成空格**而 **rc=4 拒跑**（ISS/CHG/DEC 用 `-`，只有 EVT 用空格）—— **两次都是护栏先响，我才发现自己错了**。
- **教训**：**模板要按前缀分开看** —— CHG 的 f2 是 ts、ISS 的 f2 是状态；「照抄上一个成功的命令」在这里**正好错**。

--- ④ 本轮新增记录 ---
CHG-363 · ISS-152。**不新开 EVT** —— 「同一作业的收尾修复追加到已有留证（不新开 EVT）」是允许的（I5：一次作业一个 EVT）。

【十、载体第二遍读数 + 一个新缺陷（ISS-153）】
**读数**（`.check/audit/2026-09-18/audit-001624.txt` · 5m42s）：19 段 `EXIT=` · **1 段非零** · 3 处 `SKIP` · 载体 **rc=1**。
- 与**第一遍**（`audit-000533.txt`）**同版本**对照：段数 19 不变 · `SKIP` 3 不变 · 非零 **2 → 1**（`INTEGRITY` 的 I1/I7 已转 OK；`JUDGE-SELFTEST` 已由 `seg` 改 `skip`）。
- **唯一非零段 = `ACCEPTANCE`（`EXIT=2`）**，且**不是本轮造成的**：两遍的差异**只在 `[99] hook 被宿主调用`** —— 第一遍 `11 OK / 0 FAIL / 0 UNVERIFIED`（`EXIT=0`），第二遍 `10 OK / 0 FAIL / 0 DRIFT / 1 UNVERIFIED`（`EXIT=2`）。原因是宿主日志**扫描窗口**的最新日志（`2026-09-18T00:20:29.303Z`）**早于** hook **自述末行**（`00:20:29.410Z`）⇒ 两个计数**不可比** ⇒ 判据**主动**报「这一项没测（不是通过，也不是失败）」。
- **⇒ 新缺陷 ISS-153**：`acceptance.js` 的 rc=2 是**对的**（本仓库统一口径 `0 通过 / 1 失败 / 2 拒跑`）；错的是载体的 **`seg()`** —— 它把**任何非零 rc** 都累加进 `FAILED`，于是「内层说『没测成』」被翻译成「载体说『有段**确定**不合格』」。**这正是 ISS-149 的原话**（「拒跑被记成失败」），而 ISS-149 的修法是**逐例打补丁**（载体先探依赖、探不到就 `skip`，只覆盖 `write-lock-test.sh` / `judge-selftest.sh` 两个**已知**实例）⇒ **通用规则没进 `seg()`**，第三个实例（`acceptance.js`）当场复现。**⇒ ISS-149 的关闭不完整**：三样齐里缺 ③ 反向断言 —— 当时没问「**还有别的段会返回 2 吗**」。
- **未修的理由（如实记）**：修法要动 `seg()` 的**分类口径**，而 `^SKIP ` 计数契约本身**受判据守着**（`audit-exitcode-test.sh` ⑤ 的 `grep -c '^SKIP '`）⇒ 必须**同时**改判据 + 做**双向**反向断言（假失败仍 → rc=1 · 假拒跑 → rc=2），而单遍载体回归约 **6 分钟**，本轮余量不足。**报不确定比报完成重要。**

**发布副本全量比对（本轮新增的一次性工具 `.check/tmp/pub-diff2.py`）**：口径 = **双向归一化后比字节**，**不是**「把源树按固定顺序替换一遍」—— 后者实测**误判 20 处**，因为同一段本机路径在**不同文件里脱敏粒度不同**（有的整段换成 `<项目外目录>`，有的只把用户名换成 `<user>`），而且源树里同一路径有**五种写法**（`C:/…` · `/c/…` · `c:\…` · `C:\\…`（JSON 双反斜杠）· `C:\c\Users\…`（POSIX 路径拼给原生 node.exe 的产物））。实测 **257 个副本文件**：逐字相同 **189** · 仅路径差异（脱敏，故意）**67** · 实质不同 **1**（`skills/andyngo/SKILL.md` 的发布专用单行 `description`，**故意**）。**⇒ 副本与源树已一致。**

**打包**：`package_skill.py` 重建 `dist/andyngo.zip` —— **40248 → 40774 B** · **22 条目** · 根前缀 `andyngo` · 含本机路径的条目 **0** · 含 `.check` 的条目 **0** · `quick_validate.py` → `Skill is valid!` rc=0。


【十一、ISS-153 已修（同一作业，不新开 EVT）】

**触发**：用户裁决「合并为干净提交 + 强推」与「改用环境变量固定路径」执行完后，本轮按「减少对话，可以用任何方法全速进行任务」继续收尾 —— ISS-153 的修法在【十】里已写明（「未修的理由」= 余量不足），本轮补上。

**改了什么（三处，都不动段名、不动段数）**：
1. `skills/andyngo/scripts/andyngo-audit.sh` 的 `seg()`：`local rc=$?`；`$1` 是**纯数字**则取它当 rc（其余参数当尾巴拼进 `EXIT=` 行 —— **`EXIT=` 行与段名形状一字未改**）；按 rc 分流 —— `0` 通过 · `2` 计入 `SKIPPED` **并打一行行首 `SKIP `**（维持 ⑤⑦ 的 `grep -c '^SKIP '` 计数契约）· 其它非零计入 `FAILED`。
2. `:84` 的陈旧段标签 `=== INTEGRITY (I1-I5) ===` → `(I1-I7)`（判据早已到 I7）。
3. `.check/tests/audit-exitcode-test.sh` 加 **⑥ 假拒跑** + **⑦ 口径一致**（文件头「五组断言」→「**七组断言**」）。

**双向反向断言（这是 ISS-153 原文自己写明的验收条件）**：
- **改前**（同一把尺子 × 未改的载体）：`FAIL ⑥ 段返回 2 → 载体 rc=1（**应为 2** …）` · **9/10 · rc=1** ⇒ **缺陷精确复现**（不是推测）。
- **改后**：**10/10 · rc=0**，逐条 —— ① 到达性 `EXIT=1` ✓ · ② 假失败 → rc=1 ✓（**假失败仍归 FAILED**，没被顺手改成 SKIP）· ③ 假成功 → rc=2（门不是恒红）· ⑤ 假成功跑 18 处 SKIP · ④ 报告形态未变（全部是 `EXIT=<数字>`）· ⑥-到达性 `EXIT=2` ✓ · ⑥ 假拒跑 → **rc=2** ✓ · ⑦ `fakefail` 18==18 · `fakeok` 18==18 · `fakeskip` 19==19。

**实测读数（改后）**：

| 对象 | 行数 | md5 |
|---|---|---|
| `skills/andyngo/scripts/andyngo-audit.sh` | 549 → **571** | `4c196e0b…` → **`e893b55e8487793905d9dc268b71d854`** |
| `.check/tests/audit-exitcode-test.sh` | 144 → **194** | `68ff8375…` → **`a19ff918ccdd87bbaf7ba97cf44599a7`** |
| `.check/record/issues.md` | 167 → **167** | `4d9d75f7…` → **`4877ed02695d41452ce80f3cc8afded3`** |
| `.check/record/changes.md` | 372 → **375** | — |

★ **尺子「改前」基线是从哪来的（方法，不是猜的）**：`file-history --list` 只到 `v3 = 144 行`（平台侧漏记了后面的块），而 **`145 行` 是我先前的预期值、不是实测值**。真实基线取自**发布副本**：`.check/tmp/publish/.check/tests/audit-exitcode-test.sh` = **144 行 / `68ff837512fb5a60e4bbcfc8c29c00e7`**，与源树 diff = **1 行改 + 51 行增**（144+51−1 = **194** ✓，与实测行数自洽）⇒ 副本当时与源树逐字一致，可作改前指纹。**教训**：`file-history` 的块会漏记，**改前指纹要以「磁盘上真能取到的那个版本」为准**（与「以改前实测的 md5 为准」同一条纪律）。

**记录层判据（全绿）**：`probe-record-shape.js --scan` → `OK 三份记录全部合规 · 豁免 0 条` rc=0 · `record/newid.js --check` → `未登记撞号 0 · 悬空豁免 0` rc=0 · `probe-crossref.js --scan` → `命中 0 处 · 豁免 0 条 · 悬空 0` rc=0 · `andyngo-integrity.js` → **28 OK / 0 FAIL / 0 UNVERIFIED** rc=0 · `check-protocol.js` → rc=0。

**边界（如实记）**：本节的 10/10 是**尺子**的读数（尺子内部会造三个假根各跑一遍载体逻辑），**不是**载体整体重跑 —— 载体整体读数见【十二】。
