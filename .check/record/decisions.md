# 决策记录

格式：`DEC-<序号> <时间> <决策> <依据> <替代方案> <后果> <关联事件>`
专家术语：ADR

序号从 001 起。不改历史，只追加。

---

DEC-001 2026-09-15T13:52Z 修复 `andyngo-record.sh` 两处规格缺陷（`EV="${7:-}"` → `EV="${5:-}"`；printf 实参去掉多余的 `"${6:---}"`） 依据：规格第四部分验证第 5 项期望 `rc=1`，实测 `rc=0`；事件流实测每次多写一行。修复后双向测：该绿的绿（1 行 / rc=0）、该红的红（rc=1 + `FAIL evidence_ptr 不存在`）、原始脚本反向复现两处缺陷（rc=0 且写入 2 行）。 替代方案：(a) 原样保留、只报 FAIL 不修 —— 被否，规格执行指令要求「完成功为止」；(b) 同时给 `$4` 加 `--` 默认值 —— 被否，非最小改动，留作残余风险记录。 后果：脚本偏离规格原文 2 处；原始版本已归档为 `.check/record/evidence/2026-09-15/andyngo-record.sh.as-spec-135229` 可随时比对。 EVT 001/002
DEC-002 2026-09-15T13:52Z 本次「引导式构建」由 L1 亲自落盘 22 个文件，不派子代理 依据：规格《执行指令》第 1–2 步明确要求「请现在执行：创建全部文件 / 逐个写入内容」，这是对架构条文「L1 从不执行」的显式引导期豁免。 替代方案：派 22 个子代理各写一个文件 —— 被否，单次跳转实测约 9.87 秒纯延迟，22 次约 217 秒纯开销，且子代理回传只有一行 EVT，无法承载逐字节规格正文。 后果：本轮的 `事件数 == 派发数` 判据不适用于引导期，AUDIT 须区分「引导期」与「运行期」。 EVT 001
DEC-003 2026-09-15T13:54Z 修复 `andyngo-audit.sh` 的路径拼接（改为 `( cd "$ROOT" && node .check/acceptance.js )`，用相对路径调 node） 依据：规格原样跑出 `MODULE_NOT_FOUND: C:\c\Users\...`，退出码 1。修复后实跑产出真实 acceptance 输出。 替代方案：(a) 强制要求调用方传 Windows 路径（`PROJECT_ROOT='C:\Users\...'`）—— 被否，等于把跨平台负担转嫁给使用者；(b) 用 `cygpath -w` 转换 —— 被否，cygpath 非 POSIX 通用，且引入新依赖。 后果：脚本偏离规格原文 1 处；as-spec 版已归档 `.check/record/evidence/2026-09-15/andyngo-audit.sh.as-spec-135432`。 DEC-001
DEC-004 2026-09-15T13:58Z 新增 `.check/andyngo-integrity.js`，把规格 §3.3/§3.4 的「完整性校验」4 条判据落成可执行载体 依据：规格把「seq 连续 / evidence_ptr 存在 / ts 单调递增 / 事件数==派发数」列为「AUDIT 的一部分」，却只给了文字判据、没给载体 —— 一条没有载体的判据等于没有判据。双向测：当前事件流 4 OK/0 FAIL；修复前的污染日志 0 OK/4 FAIL（I1 抓到 ISS-002 的两行垃圾、I3 抓到 ISS-001 放行的 `/nonexist`）。 替代方案：(a) 不建，只在报告里手写结论 —— 被否，手写结论不可复跑；(b) 塞进 `skills/andyngo/scripts/` 当第 4 个脚本 —— 被否，会让规格验证第 1 项的文件计数从 22 变 23，破坏「按逐行清单执行」的前提。放 `.check/` 不进入该计数面。 后果：`.check/` 下多一个非规格文件，已在 CHG-006 登记。 DEC-002
DEC-005 2026-09-15T13:58Z 路由覆盖的 3 个洞（ISS-004）**只报不修** 依据：R1/R2/R3 全是**规格内部**的不一致，改任何一边都要动 `SKILL.md` 正文或规格原文 —— 前者是「唯一注册文件」的正文，后者是权威规格本身。两者都不该由执行者单方面改。 替代方案：直接把速查卡的 `start` 改成 `task`（或反之）—— 被否，规格没给判据说明哪个才是正名，两个名字各有出处（表 vs 速查卡）。 后果：ISS-004 保持 open，进入 RESUME 未结项列表，等裁决。 DEC-002
DEC-006 2026-09-15T14:09Z **用户裁决后解除 DEC-005 对 R2 的冻结**：规格内部矛盾时，取「两处互相印证」的那一边，且**取超集 —— 任何名字都不删** 依据（不是我的判断，是从规格内部读出来的）：§3.1 自己的 frontmatter 触发词与速查卡命令名**一一对应** —— 启动↔`start`、修↔`fix`、验收↔`verify`、安全↔`security`、部署↔`deploy`、复盘↔`retro`、收尾↔`seal`、记问题↔`issue`；唯 `task` 在 frontmatter 里**没有任何对应触发词** → `task` 是异类。此旁证由 W6 独立验收给出，比我原先的「速查卡是用户契约」更硬。另实测：卡上 8 个名字，判事件表只接住 2 个（`issue` `task`），缺口 **7** 个，不止 R2 记录的 1 个。 替代方案：(a) `task`→`start` 直接改名、删掉 task —— 被否，会丢掉 §3.1 原文写过的名字；(b) 反向以 `task` 为准、改速查卡 —— 被否，速查卡是 8 行完整清单而 §3.1 只有 1 行，且速查卡是用户手里那份；(c) 只修 R2 记的那 1 个名字 —— 被否，实测缺口是 7，只修 1 个等于没消除「可用性硬伤」。 后果：`SKILL.md` 63→64 行；R2 消解（ISS-006 verified）；R1（中文触发词「开始」无对应行）与 R3（`resume`/`evolve` 无入口）**仍 open**。**本修复无规格验收项背书** —— 背书来自速查卡 8 入口与 §3.1 触发词的两处互相印证，属「规格自相矛盾时取一致的那一边」，不是「规格要求的」。 ISS-004/006
DEC-007 2026-09-15T14:19Z 把「派发」定义为「**一次有合规留证的作业**」，I5 由「事件数 == 派发数」改为「**事件行 ↔ 作业留证 双向单射**」 依据：**先测可测性、再定定义**。实测 5 条候选来源**全断**：① 事件流自身（7 行 `job` 全是 `J1`，分不出「派发」与「L1 亲自」）② 宿主 `tasks/<session>/`（6 条 JSON，由 L1 用 TaskCreate 写 = **同源**，且 5 条状态陈旧 ISS-008）③ 宿主对话日志 `sdk/conversations/<session>.log`（实测只记 `Bash 315 / TaskOutput 42 / Read 42 / Write 21 / Grep 21` 五类，`Agent`/`Edit`/`Glob` **一条不记**）④ 宿主 `workbuddyMainThread__*.log`（有 `spawn=99`/`subagent=40` 字样但**全应用级、不可按会话定位**）⑤ `audit-log/<date>.jsonl`（hash 链宿主写，但内容是**文件安全审批** 116 条，不是派发记录）。判据取双向：D1 记账必须有留证 / D2 干活必须记账（无孤儿、无重复引用）/ D3 命名合规或**逐条登记豁免**；另加**悬空豁免**（登记了却找不到对应行）也报 FAIL，防登记表变成单方面清单。 替代方案：(a) 加外部派发账本 `.check/record/dispatch-count.txt` —— **否决，同源**（L1 既是被检查者又是记账者；本项目在 hook 那件事上已吃过同源判据的亏）；(b) 用宿主 `tasks/<session>/` 当分母 —— 否决，实测 6 条 vs 事件流 7 条且状态陈旧；(c) 承认测不了、把 I5 换成一条能测的替代判据 —— 否决，等于放弃原判据意图。 后果：I5 从**恒 UNVERIFIED 变为真判据**（当前 A=5 合规留证 / B=7 事件行 / 豁免 2 条逐条登记并打印）；新增 `.check/record/dispatch-exemptions.md`（豁免必须逐条、必须写原因、必须打印）与 `i5-bidirectional-test.sh`（8 用例）。**豁免表路径可用环境变量 `ANDYNGO_EXEMPTIONS` 覆盖** —— 否则判据自己无法被测试（夹具会写进真记录），做法同 hook 的 `IMMUNITY_TRACE`。 ISS-005
DEC-008 2026-09-15T22:31Z 判据的 UNVERIFIED 条件是「**无可判对象 且 无具体缺陷**」，不是「某个集合为空」 依据：v3 把守门人写成「`A.length === 0` → UNVERIFIED」，双向测 R12/R13 立刻抓到它过宽 —— 那两个夹具里 A=0（唯一留证文件的名字不合约定），但**行侧 D3 明明判出了缺陷**，却被守门人盖成 UNVERIFIED（rc=2 而非 1）。根因：A 集只是 **D2** 的判据对象；D1/D3/D4/D5 是**行侧**判据，不依赖 A。A 为空只让 D2 不可判，不豁免其余四条。 替代方案：(a) 保持「A=0 即 UNVERIFIED」—— 被否，它把「有东西可判且判出了错」降级成「判不了」，是**反向假绿**（把 FAIL 说成 UNVERIFIED，真缺陷失去红灯）；(b) A=0 时干脆跳过 I5 不报 —— 被否，同 (a) 且更糟。 后果：守门人条件改为 `A.length === 0 && f5.length === 0`。可复用原则：**守门人只应保护「依赖该集合的子判据」，不应波及不依赖它的子判据**。 ISS-010
DEC-009 2026-09-15T22:40Z 「**行的留证必须位于本记录根的留证目录内**」入规（I5 新增 D6） 依据：W9 第二轮独立验收复现 —— 两行同指 `<T>/elsewhere/w1-j1-shared.txt`（目录外）时，判据报 `OK A=1 B=3`。根因不是某条判据写错，而是 **A 集（扫留证目录）与 B 集（来自事件行）从未被约束在同一空间**：行可以指向任何地方，而 A 永远不知道。这是 DEC-007「派发 = 一次有合规留证的作业」的直接推论 —— 若「留证」可以在任意位置，那「留证目录」这个概念在判据里就不存在，双向单射无从谈起。 替代方案：(a) 只把 D2 的查重扩展到全部行的 base —— 被否，那只接住「共用」，接不住「单行指向目录外」；(b) 不新增判据、靠 I3 的「文件存在」兜底 —— 被否，I3 只查存在性，目录外的文件当然存在。 后果：I5 增加 D6（相对路径按 I3 的口径 `path.join(HOME, ev)` 解析，两者必须一致）。**模式观察**：W9 两轮共报 8 处缺陷，其中 6 处的形状是「**判据从未检查过某个维度**」，只有 2 处是「检查逻辑写错」。前者的共同点是**审代码看不出来**，只能靠「造一个它没想过的输入」才能暴露 —— 这正是独立验收不能由被验方自查替代的原因。 ISS-010
DEC-010 2026-09-16T00:18Z 把 `.check/andyngo-integrity.js`（完整性校验 I1–I5）**接进 AUDIT 脚本** `skills/andyngo/scripts/andyngo-audit.sh` 依据：规格 §3.3/§3.4 把「完整性校验（I1–I5）」列为 **AUDIT 的组成部分**，而 AUDIT 的载体就是那个脚本 —— 但该脚本**原先只跑 `acceptance.js`**，于是 I1–I5 的载体**没有任何调用者**（只在文档里被提到，全是文字不是调用）。**一段没人调用的判据 = 一条没有判据的规则，只是换了个地方藏着。** 这是本轮我自查发现的真缺口：判据本身写得再严，只要没人跑它，它对系统的影响等于 0。 替代方案：(a) 只在 SEAL 报告里手写 I1–I5 结论 —— 被否，手写结论不可复跑，且 SEAL 是低频的；(b) 新建第 4 个规格脚本 —— 被否，会让规格验证第 1 项的文件计数从 22 变 23，破坏「按逐行清单执行」的前提；(c) 塞进 `acceptance.js` —— 被否，会让验收输出多出 5 项，验收 [1] 的被测对象（22 文件清单）随之变化。 后果：`andyngo-audit.sh` 22 行 → **35 行**（md5 `614befb2deb0e138b14a047f6c30dc9c` → `547677857f12ea47e95a8c1002b80bff`），新增 `=== INTEGRITY (I1-I5) ===` 段，随 AUDIT 一起落进 `.check/audit/<date>/audit-<ts>.txt`。接线前原件归档 `.check/record/evidence/2026-09-15/andyngo-audit.sh.as-spec-pre-wiring-1449`。实测全链路尾部 `EXIT=0`、`10 OK / 0 FAIL / 0 UNVERIFIED`。 ISS-005
DEC-011 2026-09-16T00:19Z 豁免表**按日志分**：`exemptions/<logBase>.md`（原为全局单文件 `dispatch-exemptions.md`）；**明确否决**「让 `exDangling` 去查全部日志的 seq」 依据：跨天后（9-16）判据当场报「悬空豁免: 005, 007」—— 根因是 **seq 是「按日志」编号的，每份日志都从 001 开始**，而 v3.1 只把**留证目录**改成了按 `logBase` 推导，豁免表仍是全局单文件 → 昨天登记的 `EVT 005/007` 在今天那份日志里找不到。**否决另一条修法的理由（比修法本身更重要）**：若改成「`exDangling` 查**全部**日志的 seq」，则今天若也有 seq 005，**昨天针对 005 的豁免会误豁免今天的 005** —— 那是**假绿**，比假红危险得多（假红只让人多看一眼，假绿让人永远不看）。**规则：判据的每一份「按日志分」的输入必须同构地按日志分，不能一半按日志分、一半全局合并。** 替代方案：(a) 保持全局单文件 + 放宽 `exDangling` —— 否决，即上述假绿；(b) 把豁免表并入事件流（在行上加豁免标记）—— 否决，事件流 append-only 且已被 I1 锁死 8 字段。 后果：`dispatch-exemptions.md` → `exemptions/2026-09-15.md`（内容逐字不变，EX-001/EX-002 两条）；两份日志现盘均绿。 ISS-016
DEC-012 2026-09-16T00:19Z 判据**不带参数**时校验 `eventlog/` 下**全部** `.txt`，不再只查「今天」 依据：跨天实测 —— 原写法 `today()+'.txt'`，9-16 早上跑出 `[I0] UNVERIFIED 文件不存在: …/eventlog/2026-09-16.txt`；也就是说 **9-15 那份 12 行日志从 9-16 零点起不再被任何一次 AUDIT 检查**。检查面不是变窄了一点，是**整份历史退出**，而且**退出时不报警**（当天那份日志一建出来，报错就消失了）。**事件流是 append-only 的历史，历史不该因为日历翻页而退出检查面。** 替代方案：(a) 每次调用都显式传全部日志路径 —— 被否，等于把「记得传」当判据，迟早漏；(b) 只查最近 N 天 —— 被否，N 是拍的，且同样会静默丢历史。 后果：不带参数时目标行变为 `目标: .check/record/eventlog/2026-09-15.txt, .check/record/eventlog/2026-09-16.txt`；多份日志的 I2「seq 连续」**按日志各自分组**（实测 9-16 那份报「2 条 seq = 001 002」而不是与 9-15 连成 14 条）—— 若跨日志连成一条序列，第二份日志必然被判「seq 倒退」= 必假红。 ISS-016
DEC-013 2026-09-16T00:23Z 立规：**「跑不成」必须自证，不得伪装成「跑了且不合格」** —— 脚本三态退出码（0 通过 / 1 失败 / 2 拒跑）+ **补守门人必须横向扫同类目录，不得只补被指到的那一个** 依据：ISS-015 修完后我把 `.check/tests/` 下**全部 5 个脚本**逐个裸跑一遍（不带参数），实测 **3/5 有缺陷**：`i5-bidirectional-test.sh` →「有 73 项失败」(rc=1) · `judge-selftest.sh` →「有 8 项失败」(rc=1) · `andyngo-manifest.js` → EBUSY 裸栈 (rc=1)；另两个（口径一/口径二）**本来就拦得住** —— 而它们**恰好是当初被 W6 独立验收逼着补上守门人的那两个**。也就是说：上次「补守门人」只补在**被验收指到的地方**，同一目录下另外三个脚本的同一个洞一直留到今天。**这是「改动只落在一条路径上」的第三种形态**（前两种：CHG-027 引用没跟着改 · CHG-039 冻结值没跟着改）。 替代方案：(a) 只修我自己踩到的那一个 —— 被否，实测证明同目录 3/5 同病；(b) 统一用一个 1 表示「有问题」—— 被否，那会让调用方分不清「我命令写错了」与「被测对象坏了」；本轮我自己就因为这个先怀疑了判据，回头才发现是调用方式错。 后果：三条修法同构（参数可省 + 按脚本自身位置推导 + 推导不自洽 `exit 2` 拒跑且 stderr 说明理由）；五个脚本的冻结 md5 记入 `w12-j2-testguard-hscan-002320.txt`。**可复用原则两条**：① **尺子比被测物更需要守门人** —— 自测/清单脚本撒谎时，人会去改没坏的东西；② **报错必须点名** —— EBUSY 裸栈 vs「无法读取 X（EBUSY）」，同族 ISS-013。 ISS-015/017/018
DEC-014 2026-09-16T00:40Z **用户裁决「二、能」的执行方式**：用 `.backup-skills-20260915-2135/restore.js` **不加 `--force`** 还原 skill 层 —— 默认行为（跳过已存在且不同的）**就是**最小改动 依据：用户指令原文「协助我判断。并长期自动执行完成所有任务。**二、能。完整运作的前提下，用最小的改动。并保留。原本的要求。**」——「二」= 对 SEAL v4 §九 第 1 条二选一（改文档 vs 重建 skill 层）的裁决，「能」= 选**重建 skill 层**。**判定依据是硬证据，不是偏好**：还原后验收整体 `4 OK / 3 FAIL / 1 DRIFT / 4 UNVERIFIED` → **`10 OK / 1 FAIL / 0 DRIFT / 1 UNVERIFIED`**，再修完标价与 R1/R3 后 **`11 OK / 0 FAIL / 0 DRIFT / 1 UNVERIFIED`**。**那些红不是被「改文档」消掉的，是被「文件真的回来了」消掉的** —— 反证「改文档」那条路只会让红在纸面上消失（把判据改成不检查，红当然没了）。 替代方案：(a) 加 `--force` 全量覆盖 —— **否决**，唯一冲突项 `andyngo/SKILL.md` 是**旧版单文件 skill**，而现盘是**按规格新建的 22 文件 `/andyngo` 系统**，两者不是同一个东西，覆盖 = 拿旧版砸掉新建系统（直接违反「保留原本的要求」）；(b) 只还原「被引用到的那些文件」—— **否决**，那等于按悬空引用的名单还原，会把「引用关系」当成「目录清单」用，漏掉没人引用的 skill 且无法判定完整性；(c) 改文档、删掉 40 条悬空引用 —— 否决（即用户否决的那条路）。 后果：`skills/` 25 文件 → **22 个目录 / 72 文件**（写入 47 / 跳过 3 / 冲突 1 未覆盖 / 异常 0）；`skills/andyngo/SKILL.md` md5 前后**一致**（`653570d7ce66e5ff46a1319422073f8c`）→ **新系统一个字节没动**；还原前先做快照 `.backup-skills-pre-restore-20260916-002854`（25 文件，可回滚）。**恢复的副作用是正向的**：`[10] hub 标价` 从「永久 UNVERIFIED（入口不存在）」变成「**真的测出来了**」→ `FAIL`。**恢复的价值不只是「变绿」，是让原本测不了的判据第一次真正跑起来。** ISS-025/026
DEC-015 2026-09-16T00:48Z 立规：**「修法的残留」必须单独搜一遍** —— 修好一个形状 ≠ 这个形状没了；每修完一条，必须回头问「同一个形状还能从哪进来」 依据：本轮实测**三条独立证据指向同一个模式**：① **N1 是 G3 的残留** —— 修「合法 ISO 变体被误判假红」时改成「只 `Date.parse`」，假红没了，**假绿从另一侧进来**（`9/15/2026` 放行、缺 Z 按本机时区解析）；② **N2 是 F3 的残留** —— 把「非 EVT 行」从附注改成 FAIL 时没处理「**全部**非 EVT 行」这个边界，于是「全是污染」被守门人先说成「文件为空」；③ **N4 是 ISS-015 的残留** —— 补「缺参即拒跑」时只验了**目录存在性**、没验**硬编码数据源文件**，同一个谎换了个入口复活（`EXIT=1 ·「有 11 项失败」`）。**共同点：修法总是在「被指到的那一个入口」上生效，而形状本身有多个入口。** 替代方案：(a) 修完就跑全量验收、绿了即收工 —— **否决**，本轮三条残留**全部通过了当时的全量验收**（N1/N2/N4 都是 W9 独立复验才报出来的）；(b) 只依赖独立验收 —— **否决**，那是第二层防线不是第一层，成本高且本轮证明它来得晚。 后果：**操作规则入规**：① 每修完一条，**在同一个判据/脚本里横向搜同类入口**（本轮 DEC-013 已立过这条，N4 证明它执行得不彻底）；② 每条修法在留证里**显式写出「本条是哪个修法的残留」**（`w14-j1` §1–§6 已按此格式写）；③ 独立验收的**指定攻击方向必须包含「上轮修法的边界」**（本轮回派 W9 第四轮时已指定「I6/I7 新洞 · `TS_RE` 越界值 · I2 字符串 seq 比较」）。 ISS-019/020/022
DEC-016 2026-09-16T00:35Z **R1/R3 收口：取超集，规格原文一个字不动** 依据：ISS-014 复核事实 —— **R1** frontmatter 的 13 个触发词中「启动」「开始」在判事件表 11 行里**无任何一行接住**（其余 11 个都有对应）；**R3** `RESUME`/`EVOLVE` 是判事件表里的事件，但**无同名命令名**。用户约束「**保留原本的要求**」= DEC-006 已立的「**取超集 —— 任何名字都不删**」。**两处都不删不改，只补**：① 判事件表 TASK 行补「启动/开始」（不冲突：AUDIT 行的判据是**无参数**这个命令形式，不是「启动」这个词）；② 第 31 行补 `/andyngo resume` `/andyngo evolve`。 替代方案：(a) 把「启动/开始」判给 AUDIT 行 —— **否决**，会让「空启动」与「巡检」抢同一个词；(b) 删掉 frontmatter 里的「启动/开始」—— **否决**，违反「任何名字都不删」；(c) 同时改**规格原文** §六的速查卡 —— **否决，越界**：规格是权威文本，执行者单方面改规格等于自己给自己发许可（同 DEC-005 的处置原则）。**故 R3 只改了一半**（`SKILL.md` 侧补了入口，规格 §六 侧仍缺 → 见「后果」）。 后果：`skills/andyngo/SKILL.md` md5 `653570d7ce66e5ff46a1319422073f8c` → `aa4d141bddca99d72dce980d49835d3b`，**仍 64 行**（就地改 2 处，不加行）；改前原件归档 `.check/record/evidence/2026-09-16/SKILL.md.pre-r1r3-003500`。**R1 消除；R3 半消解**（`SKILL.md` 侧已补，规格 §六 侧待用户裁决）。**验证教训**：R1 首验我用 `grep -w` 字面匹配，把「测一下」「验收」报成 MISS（表里写的是「测」「验」）→ **那是我的判据太窄，不是被测对象的缺陷**；改用**关键字包含关系**（node 实现）后 13 个触发词全部接住、未接住 0 个。 ISS-014
DEC-017 2026-09-16T00:48Z 立规：**判据必须逐列声明检查面 —— 「字段数正确」不等于「每个字段被检查过」** 依据：W9 第三轮 O1 —— 事件行 8 个字段里，`status` 与 `artifact_ptr` **从未被任何判据看过**：I1 只数「字段数 = 8」，I2 查 seq、I3 查 evidence、I4 查 ts、I5 查 evidence ↔ 作业。W9 实测把 `status` 写成 `GARBAGE-STATUS`、把 `artifact_ptr` 指向不存在的文件，**全部判 OK**。**根因不是某条判据写错，而是「检查面」这个概念在实现里从未被枚举过** —— 我写 I1 时的心理动作是「验证格式」，而「格式」在实现里退化成「空格分隔的段数」。同族于 DEC-009 的观察（W9 两轮 8 处缺陷中 6 处的形状是「判据从未检查过某个维度」，**审代码看不出来**）。 替代方案：(a) 把 I1 扩成「逐字段校验」—— **否决**，会让 I1 承担 8 件事、失败时说不清是哪件（违反「一个标签只指一件事」，同 N1/N5 的修法）；(b) 不新增、只在文档里写「status 语义由调用方保证」—— **否决**，那正是现状（无载体 = 无判据，同 DEC-010）。 后果：新增 **I6**（`status` 必须在枚举 `OK/FAIL/ASK/PROGRESS/BLOCK` 内）与 **I7**（`artifact_ptr` 非 `--` 时必须真实存在，解析口径与 I3 一致 —— 两个判据对同一字段用**同一套解析**，否则又是一条「A 集与 B 集不在同一空间」，同 DEC-009）。判据 v3.5 → **v3.6**，判据数 5 → 7。**可复用动作**：写完一条判据，回头把**被测结构的每个字段**点一遍，问「这一列谁在看」。 ISS-024
DEC-018 2026-09-16T00:58Z 立规：**判据的口径由规格决定，不由对称美学决定** —— 规格没写的约束，判据不得发明 依据：W9 第四轮 F-C 报「I7 把**目录**当合法 `artifact_ptr`」。**机制观察成立**（`fs.existsSync(dir)` 确实返回 true，我实测确认），但**结论不成立**：规格原文（`references/andyngo-event-protocol.md` 第 13 行 / 第 23 行）只说 `产物路径；无则 --` 与 `产物写磁盘，路径放 artifact_ptr` —— **没有说必须是文件**。真盘 `EVT 002` 的 `skills/andyngo/` 就是合法实例（W2 产出的是 22 个文件，产物是一棵目录树），且 append-only 不许改写。**实测过收紧成 `isFile` 的代价**：真盘当场变红。**这正是「过紧假红」的第四次** —— G3（`.000Z`）、N5（`1 1`）、N1 的 `+08:00` 讨论、F-C 本身，全都是同一个形状：**判据比规格严，于是把合法输入判成缺陷**。 替代方案：(a) 直接收紧成 `isFile`（顺从 W9 的结论）—— **否决**，会让真盘变红，且理由只是「两个指针应该同构」这种对称性偏好；(b) 完全不动、不回应 —— **否决**，那是「附注式回避」，同 F3 的教训。 后果：① I7 判「存在且必须是**文件或目录**」；② **目录引用逐条点名打印**（不隐藏 —— 这是对 F-C 的实质回应）；③ **两个指针口径故意不同**：I3 严格要求 `isFile`（规格的 D3 命名约定 `w<wave>-j<job>-<slug>.txt` 已强制它是文件），I7 没有这层约定；④ 夹具 R32 把这个口径**钉死**（指向目录必须 OK **且必须点名**），防止下一轮又被「对称性」改回去；⑤ **开放条件**：若 W9 能给出规格原文依据，立刻收紧。**可复用原则**：报出的缺陷，**机制**与**结论**要分开评估 —— 机制成立不代表结论成立；反过来，因为结论不成立就整条驳回，会丢掉机制里真正的信息（这里的信息是「不隐藏」）。 ISS-029
DEC-019 2026-09-16T00:58Z 立规：**空断言与永远红的断言同等有害，且更难发现 —— 每次改判据文案，必须逐条复核夹具里每个 `mustnot` 模式是否仍可达** 依据：本轮我自己踩到（ISS-034）。修 F-A 时把 I4 的 OK 文案从「格式合规且非降」改成「形状合规、数值合法且非降」，跑双向测后发现 **R15/R23/R24 的 `mustnot` 断言全部静默失效** —— `assert_raw "R23" … "格式合规"` 里的 `grep -qE '格式合规'` 在新文案里**恒为假** → `mustnot` 分支**恒走 `pass`**。它**没有报错**，只是从「必须不含 X」退化成「什么都不查」。**★ 上一轮我刚写下「断言不跟着判据改会变成永远红的断言」（R15 的注释），本轮暴露的是镜像的那一半**：**永远红的断言会被看见（它红），永远绿的断言不会（它绿）—— 后者更危险。** 同族于 MEMORY 里那条「一条永远红的断言会训练人忽略红色」：那个说的是**可见的红被忽略**，这个说的是**不可见的绿根本不存在**。 替代方案：(a) 断言只写 `must`、不写 `mustnot` —— **否决**，那就等于放弃了「不该出现的标记不在」这一半，R15/R21 这些用例正是靠 `mustnot` 才证明「不是靠别的判据顶替通过的」；(b) 让 `assert_raw` 在 `mustnot` 模式为空时报警 —— 好，但**管不了「模式存在却永不匹配」**这一种（这才是本轮的形状）。 后果：① 已修 R15/R23/R24/R21，**并在注释里写明为什么改**（否则下一轮又会有人以为它没动过）；② **入规的操作动作**：改判据的任何**输出文案**之后，必须 `grep -n 'assert_raw'` 逐条核对每个 `mustnot` 模式在**改后的输出里仍可能出现**（可达性），不可达即为空断言；③ **回派独立验收时显式点名这一项**（本轮已写进 W9 第五轮的攻击方向 §四）。 ISS-034
DEC-020 2026-09-16T00:58Z 立规：**断言不得自指 —— 单条断言只测它自己要测的那一个性质** 依据：本轮我自己写错（ISS-034 附带）。夹具 R33 要测的性质只有一条：**「未创建的临时目录不得被拒跑」**。我第一版写成「期望 `rc=0`」—— 那等于断言「**整个套件全绿**」。实测 `rc=1`，因为**同一个套件里当时还有 R30/R31 两条别的红灯**（与守门人无关）。→ **自指的断言天然脆弱**：测自己的套件是否全绿，等于把「这一条对不对」和「别的每一条对不对」绑在一起 —— 任何一处无关改动都会让它变红，而它红了以后传达的信息是错的（会让人去查守门人，而守门人是好的）。 替代方案：(a) 保持 `rc=0` 并让 R33 排在最后 —— **否决**，顺序依赖是更隐蔽的耦合；(b) 断言 `rc=1`（「反正总有红灯」）—— **否决**，那是把偶然当规格。 后果：R33 改为 **`rc != 2`**（0 与 1 都算「跑了」，1 只表示套件里别处有红灯）。**可复用原则**：写断言时先问「**这条断言要测的那一个性质是什么**」，然后只测它；凡是答案里出现「所有」「整个」「全部」的，先怀疑它自指。 ISS-034
DEC-021 2026-09-16T01:15Z 立规：**判据的每条约束必须能回答「这条约束的规格依据是哪一行」；答不出来就不许写** —— 并立一条自查动作：**同一类字段的多个判据，口径可以不同，只要每个口径都有各自的依据** 依据：本轮最重的一条（ISS-036）。**我在同一个改动里犯了同一个错两次** —— v3.7 我一边**拒绝** W9 的 F-C（不给 I7 加 `isFile`，理由：规格只说「产物路径」，判据不得发明规格没有的约束），一边**顺手给 I3 加了 `isFile`**，理由写成「I3 严格要求 isFile，因为规格的 D3 命名约定已经强制它是文件」。**W9 用探针 p 直接证伪了那个理由**：一个**被逐条豁免**的目录留证，D3 因豁免而满足，**I3 却照样报红** → 说明 I3 的 `isFile` 根本不是从 D3 推出来的，而是一条独立新增的、规格里没有的约束。**★ 我在 F-C 的处置里刚写下「口径由规格决定，不由对称美学决定」，同一段文字里就违反了它。** 替代方案：(a) 给 I3 也补一条「豁免时可跳过 isFile」的例外 —— **否决**，那是在给一条**无依据的约束**打补丁，补丁越多越像合理；(b) 干脆 I3/I7 都不判存在性 —— **否决**，存在性是规格明文要求的。 后果：**入规三个动作** —— ① 每写一条约束，在注释里写出**规格的行号**（本轮 I3/I6/I7 均已照此写）；② 出现「**为了对称**」「**与 X 保持一致**」这类理由时，**立即停下来查规格**（对称性是审美，不是依据）；③ 口径归位时问**三条**：这个事实**属于哪一类问题**（存在性 / 命名 / 内容）、**规格哪一行**说它、**是否已有别的判据在报它**（防双重计数）。 ISS-036
DEC-022 2026-09-16T01:15Z 立规：**结构化字段必须按列位置读，不得用子串判断状态** —— 且**「换一个子串」不是修法** 依据：ISS-040（W9 第五轮 F-H5）。**我第一次修**：把 `grep -vc ' OK '`（漏数：detail 里的 ` OK ` 把 FAIL 行洗白）换成 `grep -cE '(FAIL|UNVERIFIED)'` → **当场被自己的夹具打回 21 条假红** —— `[I6] … OK  全部 status ∈ {OK/FAIL/ASK/PROGRESS/BLOCK}` 里的 `FAIL` 让**每一条 OK 行**都被算成非 OK。**第二次**：R35 的 `mustnot` 我写 `I6.*FAIL` —— 同一个坑，**`I6.*FAIL` 对该判据永远不可靠**（它的 OK detail 必然列出枚举）。**根因不是「选错了子串」，是「用子串判断一个结构化字段」这件事本身**：`detail` 是自由文本，任何状态词都可能在里面出现。 替代方案：(a) 约束 detail 里不许出现状态词 —— **否决**，detail 的职责就是**如实描述事实**，为了判据好写而限制它说话，是让被测对象迁就判据；(b) 把状态改成一个不会与 detail 冲突的字符（如 `✓/✗`）—— **可以考虑但未采用**，会改动既有输出格式、波及更多消费者。 后果：改为**按列位置读**（输出格式固定：`[Ix] ` + `name.padEnd(26)` + `state.padEnd(12)` + `detail` → **状态在第 31 列起**），并新增 **R36 元断言**把三个口径放在同一段合成输出上跑、**必须只有口径三是对的**。**用 node 而非 awk/grep 的理由**：JS 的 `padEnd` 按**码元**补齐、node 的 `slice` 也按码元 —— 同一套语义；grep 的 `.` 在不同 locale 下按字节或按字符，数不准中文名字。**可复用原则**：负向断言必须用**该分支的专有字样**（如 `非法取值`），或者**按位置读**；凡是要用「状态词」做模式的地方，先问「它会不会在 detail 里出现」。 ISS-040
DEC-023 2026-09-16T01:15Z 立规：**任何「为了让检查能跑」的开关，只要它外部可设，就同时是一个「让检查不跑」的开关** 依据：ISS-039（W9 第五轮 F-H4）。我用**环境变量** `ANDYNGO_BIDI_NO_NEST` 防止 R33 的嵌套调用无限递归 —— W9 两条复现：**(a)** `ANDYNGO_BIDI_NO_NEST=1 bash …` → R33 被**静默跳过**，断言数 264→263，结论照样「**全通过**」（把没跑报成通过，ISS-015 的同一形状）；**(b)** 脚本被复制到别处 → `SELF_DIR` 推导错 → 嵌套调用 `rc=127` → 旧断言 `[ 127 != 2 ]` → **PASS**（子进程根本没启动也算过）。 替代方案：(a) 保留环境变量但加「跳过计数」—— **部分否决**，可见性是必要的但不充分，**开关本身还在**；(b) 干脆不测嵌套（删掉 R33）—— **否决**，那会丢掉 F-F 的回归保护。 后果：① 防递归改用**第 4 个位置参数** `--no-nest`（**环境变量设不了它** —— 要触发必须显式传参，而显式传参的人是**有意**的）；② 断言 **rc ∈ {0,1}** 且**子进程输出必须含结论行**（126/127 一律失败）；③ 新增 `skipped` 计数器，结论行改成 `全通过（0 失败 · 跳过 N 项）` —— **跳过必须可见**。**同族**：`ANDYNGO_EXEMPTIONS`（DEC-007 为让判据可测而引入）也是同类开关，但它**只能改路径、不能关掉判据**，故风险等级不同 —— **判断标准是「这个开关能不能让一条判据整个不跑」**。 ISS-039
DEC-021 2026-09-16T01:16Z **测量必须可归属：跑任何判据/测试前先记被测件 md5，跑完再记一次；两次不一致则该次测量判 UNVERIFIED，不采用其任何断言结果** 依据：本日实测三次被测件在测量期间被第三方改写（详见 ISS-035）—— 第 1 次双向测跑前 `39e1430a…` 跑后 `bed7e8f3…`，若不括 md5 我会把一次**读到半写文件**的崩溃当成「双向测坏了」，去改一个**没坏的**东西。 替代方案：(a) 只记时间戳 —— 被否，时间戳不能证明「这次跑的是哪个版本」；(b) 把被测件复制到临时目录再跑 —— 被否，那测的是副本不是现盘（且 ISS-012 已记过 `seed()` 复制真记录的耦合代价）。 后果：每次回归多两条 md5 命令；`[98]`/`[99]` 这类**每次都会变**的项照旧只作参考、不比对。 EVT 011
DEC-022 2026-09-16T01:16Z **并发写者未裁决前，本自动化只做只读回归，不改任何源文件；未结项只做「事实复核 + 追加勘误」** 依据：ISS-035 实测并发写者存在，且**恰好正在改的两个文件（`.check/andyngo-integrity.js`、`.check/tests/i5-bidirectional-test.sh`）就是本任务书明令「不许动」的冻结件**；同时 11 条 open 里没有一条满足「最小改动能修 ∧ 不在冻结件里 ∧ 不需用户裁决」。 替代方案：(a) 照常推进并改文件 —— 被否，两个会话互相覆盖且**两次都报成功**（ISS-011 的跨会话形态），且会让 W9 第五轮的冻结被测件失效；(b) 等对方收工再跑 —— 被否，无法判定对方何时收工，等待本身不可验证。 后果：本日 CHG-061 = 零源文件改动；「推进」的产出是**事实复核 + 3 条新 ISS + 1 条勘误批 + 1 条方法论决策**，不是代码。**下一轮若并发写者仍在，仍按本条执行。** EVT 011
DEC-024 2026-09-16T01:16Z **【改号登记】原 DEC-021（测量必须可归属：跑任何判据/测试前先记被测件 md5，跑完再记一次；不一致即判 UNVERIFIED、不采用其任何断言）→ 本条。** 内容见 `decisions.md` 中 ts 为 `2026-09-16T01:16Z` 的那条 DEC-021（现为**第二处** 021，第 33 行）。改号原因：01:15Z 的 W15 已占用 DEC-021/022/023。 依据：ISS-042 的撞号实证。 替代方案：不重编号、只报告 —— 被否，编号是记录层的**主键**，主键不唯一则引用不可解析（本任务书自己规定「修正用新记录引用旧 ID」，前提就是 ID 唯一）。 后果：CHG-061 正文里的 `DEC-021/022` **以 DEC-024/025 为准**（CHG-061 正文不改）。 EVT 013
DEC-025 2026-09-16T01:16Z **【改号登记】原 DEC-022（并发写者未裁决前，本自动化只做只读回归，不改任何源文件；未结项只做「事实复核 + 追加勘误」）→ 本条。** 内容见 `decisions.md` 中 ts 为 `2026-09-16T01:16Z` 的那条 DEC-022（现为**第二处** 022，第 34 行）。 依据：同 DEC-024。 替代方案：无（本条为纯登记）。 后果：本日三段作业（w16-j1/j2/j3）**源文件改动均为 0**；若下一轮并发写者仍在，仍按本条执行。 EVT 013
DEC-026 2026-09-16T01:22Z 立规：**记录层 ID 撞号的处置规则 —— ① 新 ID 必须先读后写；② 撞号以先写入者为准；③ 被冻结件源码引用者优先；④ 后写者追加勘误，不重编号、不删行；⑤ 同一批追加一次写完以缩短窗口** 依据：本日实测 5 处撞号（ISS-035/036/037 · DEC-021/022，见 ISS-042）。**仲裁不靠时间戳靠源码引用**：`ISS-035` 被 `andyngo-integrity.js:30,459` 与 `i5-bidirectional-test.sh:521` 按 W15 的含义引用，而本自动化的含义在源码里**零引用** —— **时间戳是自报的、可任意填写；源码引用是别人写的、被冻结的、跑一次就能复现的**（同 DEC-021「仲裁要站在可复核的那一侧」）。 替代方案：(a) 两处都保留、不仲裁 —— 被否，`grep -oE '^ISS-[0-9]+' | sort | uniq -c` 会永远报重复，且**引用无法解析**；(b) 删掉后写的那一行 —— **否决**，违反「不改历史，只追加」；(c) 由执行者单方面决定谁先谁后 —— 被否，本次仲裁用的是**可复核判据**（源码引用），不是先后偏好。 后果：占位 B（本自动化）改号 042/043/044 + DEC-024/025；**记录层从此多一条可执行的查重命令**（`grep -oE '^ISS-[0-9]+' issues.md | sort | uniq -c | awk '$1>1'`）。**★ 本条不解决「取号」本身** —— 两个写者各自「读最大号 +1」仍会撞；根治需加锁，属用户裁决（ISS-042 候选②）。 EVT 013
DEC-027 2026-09-16T01:26Z 立规：**取号必须原子 —— 「先读后写」不够，必须「读与写在同一次命令内 + O_EXCL 互斥」**（**加强 DEC-026①，不是重复它**） 依据：DEC-026① 写的是「新 ID 必须先读后写」，而 2026-09-16 实测证明**先读后写本身正是成因** —— 读与写之间永远有窗口，窗口只能变小、不能消失（5 处撞号就是这么来的，且两种写法都返回成功、零字节丢失）。另实测 `open(...,'wx')` 是**原子创建**（O_EXCL），两个进程不可能同时拿到锁；而「先检查是否存在、再创建」那种两段式写法**是同一个形状**，不能用。 替代方案：(a) 只用「先读后写、把窗口改小」—— **否决**，降低概率不消除成因，而撞号的代价是一整段修号 + 一条勘误链；(b) 记录层全局加锁 —— **未采纳（不是否决）**，它要改记录层协议、属用户裁决面（ISS-042 候选②），而本次修法**不需要它**；(c) 用时间戳或随机数当号 —— **否决**，会破坏「编号可读、可连续核对」这个既有约定。 后果：`.check/record/newid.js` 成为记录层取号的**唯一实现**（一份实现、多个调用者 —— 同 ISS-013 抽 `fmtLoc`、ISS-038 抽 `statOf` 的道理：复制两份会静默分叉）；automation 4ddc05b0 的提示词 §0.2 指向它。**退出码三态分开**：0 成功 / 3 锁超时（未写入）/ 4 参数或路径错（未写入）/ 5 `--check` 发现重复号（跑了且不合格）—— 同 DEC-013。**已知边界：锁只保护「用这个工具的人」**；另一方若用 `>>` 或 Edit，锁拦不住 —— 那一半属 ISS-042。 EVT 014
DEC-028 2026-09-16T02:02Z 立规：**「取数层」也是判据的一部分，必须与判定层同等对待**。依据：`[98]` 的假红（ISS-051）里，**判定逻辑全对**（`causalPair` 的 `null`/`[]` 二分、窗口边界、取最近一次 —— 全有自测覆盖且全绿），**错的是喂给它的数据**：`findSessionAcceptanceRuns` 读错了通道、恒返回 `[]`，而该函数在 47 条自测里**连 import 都没有**。三条推论：① **判据的取数函数必须有覆盖** —— 否则它读错地方时，判定层的一切正确性都是白搭；② **「取不到」与「取到了、内容是否定」必须在返回类型上分开**（`null` vs `[]`），上游不许把两者压成一种；③ **判据读的通道必须能自证归属**（如「该文件里 sessionId 唯一」），证不了就返回 `null` —— **宁可说不知道，不许猜**。**加强 DEC-022**（结构化字段按列位置读、不得用子串判断状态）：本次实测该文件里 `acceptance.js` 出现 **27 次、执行 0 次** —— **子串能证明「提到」，不能证明「执行」**。
DEC-029 2026-09-16T02:55Z 立规：**写下「不可回溯 / 不存在 / 没发生」这类强否定之前，必须先穷尽已知机制。** 依据：ISS-054 的结论被推翻 —— 我查了 `git rev-parse`（→ `fatal: not a git repository`）就写下「改前内容不可回溯」，而产品一直留着 `file-history/`，实测能取回 677 行的那一版（`a1a68b4b…`）。**同一天里同一种形状出现三次**（`MEMORY.md` 用旧快照否定磁盘现状 / ISS-051 用必然为真的否定代替真结论 / ISS-054 用「不是 git 仓库」否定「有版本历史」），**共同形状是把「我没看见」写成「不存在」** —— 所以这不是三个 bug，是一个习惯。**具体化**（规则必须可执行，否则是口号）：① 写「不可回溯」前先跑 `node .check/file-history.js --list <路径>`；② 写「不存在」前先看磁盘实况（W18 已立的「先看磁盘、不信记忆」）；③ 写「没发生」前先问「判据读的是哪个通道」（ISS-051）。**加强而非重复** DEC-022（认形状不认子串）与 DEC-028（取数层也是判据的一部分）—— 本条的落点是**「说出来的否定」本身需要证据门槛**，而前两条落点在判据实现。 替代方案：(a) 只写「以后注意」—— **否决**，纪律会失效（同日 ISS-054 的教训刚证明过）；(b) 给每条强否定都加一条验收判据 —— **否决**，无法机械判定一句话是不是「强否定」，硬做会变成关键词匹配（DEC-022 明确禁止）。 后果：`file-history.js` 成为「不可回溯」类结论的**默认第一步**；ISS-054 结项。 ISS-054/056
DEC-030 2026-09-16T03:08Z 立规：**同一天多个写者改同一棵树时，用写权锁协调，不用「会话在不在 / 谁先谁后」推断。** 依据：ISS-057 —— 记录层 `DEC-025`（「本自动化只做只读回归，不改任何源文件」）与 automation 提示词 §三（「能修的就修」）**直接矛盾**，而**没有任何判据比对这两层**；实测 automation 按 DEC-025 执行 → 「能修的就修」**永不兑现**，而交互会话通常一直开着 → **等于永远退让**。**DEC-025 的顾虑成立**（并行 Edit 互相覆盖且两次都报成功，本项目踩过 3 次 → 静默丢更新），所以**不删该顾虑，换掉它的实现**：不是「退让」，是「**排队**」。**机制**：`.check/write-lock.js` —— O_EXCL 取锁（**原子创建**，同 DEC-027）· **抢占也原子**（先拿 `.write-lock.takeover` 标记，否则两进程可能同时判定过期并同时重建）· **只放自己那把**（`release` 校验 holder）· 退出码 `0` 拿到/放掉 · `2` **拒跑（没有写入）** · `4` 参数错。**TTL 由持有者声明并写进锁文件第 4 字段，所有人按持有者声明的 TTL 判过期。** **为什么 TTL 必须由持有者声明**：第一版按调用方给的 TTL 判，实测「拿 30 分钟 TTL 的调用方**永远看不到**别人的锁过期」—— **一个谁都能给自己发长期通行证的判据等于没有判据**（DEC-023 家族）。**取代** DEC-025 的「退让」实现（**保留其顾虑**，不保留其手段）。 替代方案：(a) 保留 DEC-025 的退让 —— **否决**，等于让 automation 永不改源文件，与「长期自动执行完成所有任务」直接冲突；(b) 直接删 DEC-025、让两个写者随便写 —— **否决**，会静默丢更新（已踩过 3 次）；(c) 按「会话是否开着」选一个主写者 —— **否决**，那是**用推断代替证据**，且会话开着不代表在写。 后果：`acquire`/`release` 成为改源文件的前置/收尾步骤；`rc=2` 时**只读 + 如实写明「未取到写锁，holder=…，age=…」**，**不得静默降级、不得为凑产出而抢锁**。 ISS-057
DEC-031 2026-09-16T03:20Z **复验进行中不得修改被测件；要加新判据就放新文件** 依据：`andyngo-integrity.js`（`e8d1386e91e983c4961f5796a2486558` · 537 行 · v3.8）与 `i5-bidirectional-test.sh`（`5a2eea14e8a80bf89557ecaf146965ff`）是 **W9 第六轮独立复验的被测件，结论未回**。本轮做 ISS-050 时第一版把 I8/I9/I10 加进了 `andyngo-integrity.js`，跑出 `17 OK / 0 FAIL`，**然后回退**：在复验进行中改被测件，会让复验结论**落在已经不存在的版本上** —— 而本仓库**已经因此作废过一次复验**（CHG-061：「双向测第 1 次**因被测件被第三方改写而整次作废**」，见 `w16-j1-converge-regression-011526.txt`）。回退后实测 md5 **复原为 `e8d1386e…`**（537 行，逐字节一致）、`14 OK / 0 FAIL / RC=0`，复验继续有效。新判据改放**新文件** `.check/record-shape.js` + `.check/probe-record-shape.js` —— 新文件不在任何待复验面上，没有这个问题。 替代方案：(a) 改冻结件并在记录里注明「复验需重发」—— 被否，那等于**单方面作废别人的工作**，而重发与否该由复验方判断；(b) 等复验结论回来再改 —— 被否，会让 ISS-050 无限期挂着，而「新判据放新文件」是零代价的第三条路；(c) 只把判据留在探针里、不接进任何自动流程 —— 被否，**没接进自动流程的判据只是「可以跑」，不是「在跑」**（已接进 automation §一.7）。 后果：`.check/` 下多两个非规格文件（`record-shape.js` 120 行 / `probe-record-shape.js` 186 行）；**冻结件清单被写进 automation §0.4**（开工前核对 md5），使这条规则从「我记住」变成「每次开工都验一遍」—— 记忆会腐烂，检查项不会。 EVT 019
DEC-032 2026-09-16T03:54Z 写权锁的起手守卫规则收敛为**一条**：**「已过期」是允许清的唯一理由，名字不参与判定**；并把守卫抽成独立文件 `.check/tests/write-lock-guard.sh` 作为唯一实现 依据：ISS-060 的第一版修法按**名字**放行（`selftest:` 前缀 = 自己上次的残留），实测产生两个更坏的缺陷 —— ① 自指守门人的锁也叫 `selftest:guard`，于是内层不拒跑、把整份测试再跑一遍，**112 个嵌套 bash 进程、每 3.3 秒长一层**（ISS-070）；② 并发下（本项目实测过一条命令被执行两次，ISS-068）后一份会把前一份**正在用的** `selftest:testA` 当成「自己上次的残留」清掉 —— ISS-060 的伤害原样回来。根因是**同一个前缀上挂了方向相反的两条规则**。 替代方案：(a) 保留命名空间但加例外（守卫自己那把不可清）—— 被否，安全会依赖于「`selftest-guard:` 不匹配 `selftest:*`」这种**字符串细节**，而且还得再加一条自洽断言去守它；(b) 给测试加「跳过守卫」的环境变量开关 —— 被否，违反 DEC-023（任何外部可设的开关同时是让检查不跑的开关）；(c) 让守卫自己比对 `age` 与「持有者声明」算过期 —— 被否，同一条规则实现两遍（ISS-007），且不一致的方向是静默的。 后果：守卫改为读 `write-lock.js status` 新打印的 `已过期=` 字段（**判据输入，不是展示字段**，有第 20/21 项专门钉住它）；**失败方向一律取「拒跑」** —— 只有明确读到「已过期=是」或「没有锁」才清，其它一切情形（含 status 输出认不出）都拒跑，因为反过来的写法在 status 格式一变时会**变成「什么都清」**；测试给自己声明的 TTL 从 30 分钟降到 **5 分钟**（实测整份测试约 3 秒跑完，余量 ~100×），把「被中断的残留」的最坏恢复时间从 30 分钟压到 5 分钟；命名空间 `selftest:` 保留但**只用于人读**。守卫抽成独立文件的理由也是**结构性的**：留在测试里则「测守卫」只能「再跑一次整份测试」，**递归在结构上无法排除**。 EVT 021
DEC-033 2026-09-16T03:54Z 写权锁的起手守卫规则收敛为**一条**：**「已过期」是允许清的唯一理由，名字不参与判定**；并把守卫抽成独立文件 `.check/tests/write-lock-guard.sh` 作为唯一实现 依据：ISS-060 的第一版修法按**名字**放行（`selftest:` 前缀 = 自己上次的残留），实测产生两个更坏的缺陷 —— ① 自指守门人的锁也叫 `selftest:guard`，于是内层不拒跑、把整份测试再跑一遍，**112 个嵌套 bash 进程、每 3.3 秒长一层**（ISS-070）；② 并发下（本项目实测过一条命令被执行两次，ISS-068）后一份会把前一份**正在用的** `selftest:testA` 当成「自己上次的残留」清掉 —— ISS-060 的伤害原样回来。根因是**同一个前缀上挂了方向相反的两条规则**。 替代方案：(a) 保留命名空间但加例外（守卫自己那把不可清）—— 被否，安全会依赖于「`selftest-guard:` 不匹配 `selftest:*`」这种**字符串细节**，而且还得再加一条自洽断言去守它；(b) 给测试加「跳过守卫」的环境变量开关 —— 被否，违反 DEC-023（任何外部可设的开关同时是让检查不跑的开关）；(c) 让守卫自己比对 `age` 与「持有者声明」算过期 —— 被否，同一条规则实现两遍（ISS-007），且不一致的方向是静默的。 后果：守卫改为读 `write-lock.js status` 新打印的 `已过期=` 字段（**判据输入，不是展示字段**，有第 20/21 项专门钉住它）；**失败方向一律取「拒跑」** —— 只有明确读到「已过期=是」或「没有锁」才清，其它一切情形（含 status 输出认不出）都拒跑，因为反过来的写法在 status 格式一变时会**变成「什么都清」**；测试给自己声明的 TTL 从 30 分钟降到 **5 分钟**（实测整份测试约 3 秒跑完，余量 ~100×），把「被中断的残留」的最坏恢复时间从 30 分钟压到 5 分钟；命名空间 `selftest:` 保留但**只用于人读**。守卫抽成独立文件的理由也是**结构性的**：留在测试里则「测守卫」只能「再跑一次整份测试」，**递归在结构上无法排除**。 EVT 021
DEC-034 2026-09-16T10:00Z **规格 §3.N 是「构建时快照」，不判「与磁盘逐字一致」；该判的是规格内部一致性** 依据：① 规格头部原文「你是一个 AI 编程助手。请在本项目根目录下，**按以下规格创建**一套完整系统。」—— 它是**构建指令（种子）**，磁盘是长出来的树，**漂移是设计使然**。② 实测 22 段里 18 段逐字一致、4 段有差异，**4 段全部良性**：§3.21（22→97 行）· §3.1（63→127）· §3.3（70→75）是**生长**（磁盘多了内容）；§3.20（23→23 行、6 行不同）是 **DEC-001 早已登记的有意偏离**（「修复 `andyngo-record.sh` 两处规格缺陷…后果：脚本偏离规格原文 2 处；原始版本已归档 `andyngo-record.sh.as-spec-135229` 可随时比对」）—— 规格侧**故意**保留 as-spec 版作基线，**我差点把它当成新缺陷去改规格**。③ **为什么这个量不是合格的判据对象**：要它报红 → 「**只会乱报**」（永久假阳性）；要它恒绿 → 「**只会说 OK**」（= 没有判据）。 替代方案：(a) 同步全部 §3.N 让规格反映当前系统 —— 被否，会**销毁构建时刻的历史记录**，且 §3.1 要 +64 行 → **打断口径一硬编码的 `1185,1207`**（级联）；(b) 加一条「逐字一致」判据 —— 被否，见 ③；(c) 只同步 §3.20（修规格里的自相矛盾：`printf` 写 7 个 token 而 `echo` 报 8 个，与规格自己的 §3.4「EVT 7 字段」冲突）—— 被否，**DEC-001 已把它作为有意偏离登记**，且记 CHG 要写含空格的路径 → **再次踩 ISS-090**（已开未修）。 后果：规格与磁盘的漂移**不判**，但**两条真缺口被单独处理** —— ① 两条零调用者判据已接进载体（ISS-097）；② 口径一与规格行号的隐式耦合已让载体显式对照两口径结论（ISS-099）。**规格本轮零改动**。见 ISS-097 / ISS-099 / DEC-001 / CHG-106 / EVT 037。
DEC-035 2026-09-16T10:00Z **加判据前必须先回答一个问题：「这条判据有没有同源兄弟？」** 依据：本轮我加了一条新规则（`record-shape.js` 的 ⑤ 跨行同号 + 它的双向测与豁免登记），**全部向量跑绿**（注入 6/6 · 反向 6/6 · 边界 1/1 · 文件级 6/6 · 计数 1/1），**反向验证也过了**（移开豁免表 → 8 处命中 rc=1）—— 然后才发现 `newid.js --check` **早已覆盖同一件事，而且逐项更优**（覆盖 5 个文件含 eventlog vs 我的 3 个 · 已有逐条豁免表 · 0/3/4/5/6 三态齐全 · 与写入入口同文件），而 `newid.js` 的文件头**早就完整记着这次事故**（「同一天两个会话写同一批记录文件…ISS-035/036/037 各有两行…DEC-021/022 各有两行」）。**两条同源判据会互相背书**，比一条判据更坏（`record-shape.js` 文件头原话：「**同源判据不算两个判据**」·「若各写一份，它们会**静默分叉**」）。**根因**：**「读到过」不等于「问过」** —— `probe-record-shape.js:28` 那句「实测 `newid.js --check` exit 0」是**我自己写的**，我读过它，却没顺着问「那它在查什么」。这与 DEC-010 的「**提到 ≠ 调用**」同族：**看见了符号，没问它的语义**。 替代方案：(a) 保留 ⑤ 与 `newid.js --check` 并存 —— 被否，两份实现互相背书且豁免要登记两遍（**同一件事两份登记**）；(b) 只删 ⑤ 的豁免表、保留规则 —— 被否，规则本身冗余，且覆盖面更窄（漏 eventlog）；(c) **逐字节回退** —— 采纳，实测三个文件 md5 全部复原到改前值（`9170bb1c` / `d42bf44a` / `69350227`），净变更为零。 后果：**「加判据前先查有没有同源兄弟」成为一条开工前置检查**（与 automation §0.4 的冻结件 md5 核对并列）；`newid.js --check` 改为**接进载体**（缺的是调用者，不是能力）。**归档那一步（出于习惯做的）让「回退干净」变成可证的事，而不是我说的话。** 见 ISS-098 / ISS-097 / CHG-117 / EVT 037。
DEC-036 2026-09-16T10:43:34Z 立规：**给「规格要求的文件」建可达性判据时，口径必须三件套 —— 白名单 + basename 匹配 + 剥注释；且动手前必须先回答「有没有同源兄弟」** 依据：本轮新增 `.check/tests/spec-ref-coverage.js`。**三条口径每一条都是踩出来的**：① 黑名单口径（只排除 `.check/record/`）实测把审计产物（`.check/audit/` 下的 seal）、宿主索引（`file-tree-manifests/`）、临时正文（`.check/tmp/`）全算成了「引用点」→ 22 个文件**全部**「有引用」、零引用=0 —— **这是 ISS-082/087「判据分不清引用与提及」的同族第 7 次**；② 按**全路径**匹配会漏掉 SKILL.md 里写 `A-discovery/x.md` 的引用（它相对 `references/`）；③ 不**剥注释**则「注释里的提及」冒充「会去用它」。**同源兄弟的分工边界必须写进判据头部**：`card-entry-coverage`（**入口名**层，检查面 = 速查卡 9 个入口）/ `spec-sync-measure`（**内容一致性**层，检查面 = 22 段文本）/ 本判据（**文件可达性**层，检查面 = 22 个文件的引用图）。**三者检查面不同**，且 **`card-entry-coverage` 绿 ≠ 本判据绿**：`security` 入口存在（前者绿），但 `andyngo-security-scan.sh` 曾零引用（后者红）—— **「入口在」不等于「入口背后要用的工具有人指向」**。 替代方案：(a) 只做「零代码调用者」扫描 —— 被否，24 个候选里**多数是正常的**（参数加载型 / 文档驱动型 / automation 提示词驱动型 / 一次性诊断型），直接判红就是「只会乱报」；(b) 用黑名单口径 —— 被否，见 ①；(c) 不建判据、只修那两处 —— 被否，那样下次接线遗漏仍无人发现。 后果：判据 `.check/tests/spec-ref-coverage.js`（三态 `0/1/2` + `--inject` 9 项双向测 + `--report` 只读表格 + **结构上排除判据自身**防自证绿）+ 接进 `andyngo-audit.sh` 第 8 段（判据与它的尺子一起跑）。**可复用原则**：**「规格要求它存在」与「有人会读它」是两个判据** —— 后者容易被漏，因为**前者绿了会让人以为后者也绿**。 ISS-101 ISS-103
DEC-037 2026-09-16T11:20Z 立规：**给某条规则加「守门人」（钉住它必须抓不住什么 / 必须抓住什么的断言）时，守门人必须与被守的规则在同一个文件；若分居两处，必须显式写出守门人的检查面，否则它看起来武装着却永不响** 依据：W23 J14 实测 —— `record-shape.js` 文件头承诺「哪天有人加了空括号规则，`probe-record-shape.js --inject` 那一条会立刻报 FAIL」，而**空括号族的规则早已存在于 `newid.js`**（写入时刻 WARN `suspectHits()`），绊线钉的是 `record-shape.js` 的 `checkLine`，**结构上看不见它** → 从不报 FAIL。这条守门人的全部价值就是「在被守的规则出现时响一次」，分居两文件后价值归零，**而它仍打印「已知边界 1/1 确认抓不住」= 看起来是武装着的**。**一个不会响的守门人比没有守门人更坏**：它让人以为这件事被守住了。 替代方案：(a) 把守门人挪进 `newid.js` —— **否决**，`record-shape.js` 那条边界向量本身是对的（它确实该断言 `checkLine` 不抓空括号），问题不在向量、在**它的检查面没写出来**；(b) 直接删掉那句承诺 —— **否决**，边界向量有价值，该保留，只是要写清它覆盖谁；(c) **逐处补写「这条断言覆盖哪个文件的哪个函数」+ 指向真正的尺子** —— 采纳，纯注释改动、零行为变化（`--inject` 6/6+6/6+1/1+1/1 与 `--scan` OK 均不变，已实测）。 后果：与 DEC-035「加判据前先问『有没有同源兄弟』」并列成为**第二条加规则 / 加守门人前的前置检查**：**「这条断言守在哪个文件上？被守的规则在哪个文件？」两者不同 → 检查面必须写出来，或把守门人挪过去**。**同族**：ISS-026「隐形文件」· W23 J13「存在 ≠ 可达」· DEC-010「提到 ≠ 调用」—— 本条是同一族的第三条腿：**守门人在、规则也在，但守门人守的是另一个文件**。**未做的部分**（登记表 + 判据）见 ISS-106。 EVT 039
DEC-038 2026-09-16T12:10Z 立规：**一个值的多个消费点必须走同一条规范化路径；存在性检查必须检查「真正被使用的那一个形态」，而不是同一个变量在别处能被读到的那个形态** 依据：W23 J14 实测（ISS-110）—— 把 `andyngo-record.sh` 改成转调 `newid.js` 的第一版里，`$LOG` 与 `$NEWID` 是**两个都要交给原生 node.exe 的路径**，我只把 `$LOG` 做了 `cygpath -m` 规范化、`$NEWID` 漏了。于是 `[[ -f "$NEWID" ]]`（**bash** 读它，认 POSIX 路径）**通过**，`node "$NEWID"`（**原生 exe** 读它，把 `/c/...` 解析成 `C:\c\...`）**失败**，脚本 rc=1、一行都没写。**它不是「忘了转换」那一类**：漏转换会在**第一次跑**就大声失败（这次确实是），真正的问题在于**守卫给出了错误的保证** —— 它检查的是一个**没有消费者会用的形态**，于是它通过时你得到的不是「这条路走得通」，而是「有一个字符串是存在的」。 替代方案：(a) 只补一次 `cygpath` —— **否决**，下次再加一个路径消费点会同样漏掉；(b) 把 `cygpath` 写在每个消费点前面 —— **否决**，那是「同一条规则各写一份」，会静默分叉（`record-shape.js` 文件头已立此规）；(c) **抽一个 `win()`，两处路径都走它，且存在性检查放在转换之后** —— 采纳，`win()` 同时承担「有转换器才用」（无 `cygpath` 的 Linux/WSL 下原样即正确）。 后果：与 DEC-037「守门人必须与被守的规则同文件」并列成为**同一族的第二条** —— DEC-037 管「守门人守在哪个文件」，本条管「守门人检查的是哪个形态」；两条共同回答「这个守门人通过时，它到底保证了什么」。**检查它是否已做到**：读守卫那一行，问「这个形态是不是**消费它的那个程序**读到的形态」—— 若不是，守卫的位置就要挪到转换之后。 见 ISS-110 / DEC-037 / CHG-138 / EVT 040
DEC-039 2026-09-16T12:10Z 立规：**载体（`andyngo-audit.sh`）的段号是位置别名 —— 只能追加在末尾，不能插在中间** 依据：W23 J14 实测 —— 我要给载体加第 10 段（`RECORD-CLI-BIDIR`），逻辑上最该放在第 5 段（`RECORD-ID-UNIQUE`）与第 9 段（`RECORD-WARN-INJECT`）之后，因为三者都判记录层。**插在那里之前先数了一遍引用点**：`第 8 段` / `第 9 段` 这两个段号在 **5 处以上**被引用（`.check/record-shape.js:86` · `.check/probe-record-shape.js:46` · `.check/probe-crossref.js` 注释 · CHG-121/122 · CHG-127/128/129）—— **插在中间 = 让那 5 处静默失效**（它们指向的段落会变成另一个，而引用文字本身仍然读得通）。于是改为**追加在末尾**，第 8/9 段含义一字不变。 替代方案：(a) 插在逻辑相邻处 + 同步改那 5 处引用 —— **否决**，代价更大且引用点还会继续长；(b) 把段号改成「小标题文本」而不是序号 —— **否决**，那是更大的重构，且序号在 `EXIT=` 行与报告里已被当作定位符用；(c) **追加在末尾** —— 采纳。 后果：**「加段先看引用点」成为给载体加段落的前置检查**；与 DEC-037 / DEC-038 同族 —— 三条都在回答同一件事：**引用点指向的东西，必须真的还是那个东西**。**同族**：ISS-090（含空格的路径在 CHG 里被截断）也是「引用与被引用不一致」的一支。 见 CHG-140 / DEC-037 / DEC-038 / EVT 040
DEC-040 2026-09-16T12:52Z 立规：**文档里被写下的「声明值」（合计 / 汇总 / 表头里的那个数）必须与它的组成部分一起进入同一条判据；判据的检查面要按「读者会引用什么」来定，不是按「解析器容易拿到什么」来定** 依据：W23 J14 实测（ISS-109）—— `price.js --check` 的 `want` Map **只由两个表格正则从表格行抽取**，而 `routes.md` 表头的「**21 本正文合计**：主文件 `51,226` · 含 refs `139,508`」**从未进入比对**，于是它可以**静默腐烂**：实测它停在 `51,200 / 138,265`，而表内逐行相加已是 `51,226 / 138,361`（**差 26 / 96**，恰好是**落后一次 `andyngo` 行的更新**），而 `--check` 报「数值不一致 0」**全绿**。**它不是「漏了一个数」**：判据覆盖了 21 行里的每一行、每一个数字，覆盖得很完整 —— 它漏的是「**被声明出来的那个数**」这一类，因为那个数**不在表格的数据结构里**，它在**散文里**。同族 ISS-094（`--check` 只比数字不比散文）—— 那条是「散文里的数字没比」，这条更窄：**合计本身就是数字，只是没被比**。 替代方案：(a) 把合计那一行从文档里删掉、只留表格 —— **否决**，读者需要合计（它回答「读这张表要付多少」），且 `routes.md:12-16` 已明文说明「不引用 22 本合计」的理由是**口径分叉**、不是「合计不该存在」；(b) 合计不进判据、改成每次手工核 —— **否决**，纪律依赖「我记得」，本仓库已反复验证过它不成立（`alloc.sh` 文件头原话：「纪律依赖『我记得』，机制不依赖」）；(c) **把表头声明纳入 `--check`，并规定「声明行缺失 = 显式 FAIL，不静默通过」** —— 采纳，实测：真树 rc=0 且新增一行对照 · 反向 **5 个向量全部 rc=1**（含整行删掉 → **显式 FAIL**）。 后果：① **「写下任何合计 / 汇总数字时，必须同时把它接进某条判据」成为写作前置检查** —— 否则那个数一出生就没有守护者；② **「没有」不能用「零」表示**：`parseDeclaredTotal` 在找不到声明行时**返回 null 而不是 0**，因为 **0 是合法合计**，返回 0 会让「声明行被删掉」静默变成「合计为 0」的假绿 —— 推广为：**判据里凡「缺失」与「合法值」可能同形的地方，必须用第三种表示（null / 哨兵），不能复用合法值**；③ **口径不能混比**：判据只比「表头声明 ↔ 表内逐行相加」（两个都是表内量），**不引用脚本自报的 22 本 `round(Σ)`** —— 脚本合计 = `round(Σxᵢ)`、表里每格 = `round(xᵢ)`，舍入位置不同，混比就是**假红**（`routes.md:12-16` 已明文禁止引用那个数）；④ 与 DEC-037 / DEC-038 / DEC-039 同族 —— 四条都在回答同一件事：**被引用 / 被声明的东西，必须真的还是那个东西，而且得有东西守着它**。**同族**：ISS-094 · ISS-112（判据比了字段**总数**、没比字段**身份** —— 同一个形状的第二次出现，说明这不是一次意外）。 见 CHG-149 / CHG-150 / ISS-109 / ISS-112 / EVT 041
DEC-041 2026-09-16T15:15:33Z 裁决：`hook-session-start.js` **不删除、不激活，只在文件头补一段说明（使命已完成）** —— 三条候选里取中间那条 依据：W23 J14 实测（ISS-103）—— ① **使命已完成**：它自述用途是「验证 hook 到底能不能工作」，而这个问题已由 `immunity-hook.js` 回答（宿主侧 **1152** 次真 spawn，全部指向 `.check/`）；② **它为什么没被加载，根因在 2026-09-16 换过一次**：注册它的 `~/.codebuddy/settings.json`（2026-09-15 14:03）里**确实有** `SessionStart` → 本文件，但**宿主读的是 `~/.workbuddy-ai/settings.json`**（那份只有 `PostToolUse → immunity-hook.js`）—— 旧根因「官方文档写明的 `/hooks` 面板人工审批闸门」**被反证推翻**：若手改 settings.json 不生效，`immunity-hook.js` 不可能从 0 涨到 1152，而它正是手改进**宿主读的那份**配置里的。 替代方案：(a) **删掉文件** —— 否决，违反用户长期指令「保留原本的要求」（规格内部矛盾取超集、任何名字都不删），且它承载的「判据认不出形状」教训实例会一并消失；(b) **在 `~/.workbuddy-ai/settings.json` 里加 `SessionStart` 把它真的激活** —— 否决，**没有待它回答的问题**（使命已完成），而代价是每次新会话注入一行常驻上下文、且要改宿主行为；「完整运作」已由 `immunity-hook.js` 满足；(c) **补说明、保留文件、不动配置** —— 采纳。 后果：① **该文件从此是「留证」不是「探针」** —— 文件头第二版原文**逐字保留**并加「**【留证，不要再当成现状】**」限定（同 CHG-150 先例），因为那 226/0/17 的错数字正是「判据认不出形状」这个教训的唯一实例；② 文件头新增「第四版」段，写明**当前实测值**（1637 / 1195 = 1152+40+3 / 0 / 139 / 0）与**新根因**；③ `additionalContext` 里两处**失效断言**同轮纠正 —— **同族**于用户级 MEMORY.md 那条教训「清空后写下的永久失效断言必须在同一轮改掉，它带着『不要再照做』的强制语气」；④ **判据视野缺口**（`[99]` 不区分是哪个 hook、且含自指噪声）**不在本文件修**，另立 **ISS-114**；⑤ **新增一条通用坑（本轮实踩）**：**块注释里也会出现 `*/`** —— 我在文件头写了 `logs/**` + `/sdk/conversations/`，那个 `*/` **提前闭合了块注释**，`node --check` 报 `SyntaxError: Unexpected identifier`。**所以「改注释」也要过语法检查**，不能因为「只动了注释」就跳过。 **同族**：DEC-037 / DEC-038 / DEC-039 / DEC-040 —— 四条都在回答「被引用 / 被声明的东西必须真的还是那个东西」；本条回答的是它的兄弟：**「被写下的失效断言必须真的失效掉」**。 见 CHG-168 / ISS-103 / ISS-114 / EVT 045
